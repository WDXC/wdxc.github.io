<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>C++ templates 2nd</title>
    <url>/2021/02/15/C-templates-2nd/</url>
    <content><![CDATA[<h1 id="第一章-Function-Templates"><a href="#第一章-Function-Templates" class="headerlink" title="第一章 Function Templates "></a><center>第一章 Function Templates </center></h1><ol>
<li>C++ 中equality 和 equivalent 的区别</li>
</ol>
<ul>
<li>Equivalent 要求类满足”LessThanCompare”,即这两个实例可以使用”&lt;”进行比较</li>
<li>Equality 要求满足EqualityCompareable,即可以使用”==”比较</li>
</ul>
<ol start="2">
<li>从语意上来说，<code>class</code>和<code>typename</code>它们是Equivalent。由于class存在一定的误导性，所以推荐使用typename来声明模板参数T</li>
</ol>
<ul>
<li>typename 主要用于依赖于另一个模板参数的嵌套类型时进行声明,如下例：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">template &lt;typename param_t&gt;</span><br><span class="line">class Foo</span><br><span class="line">&#123;</span><br><span class="line">  typedef typename param_t::baz sub_t;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
<li>在显式实例化模板时，必须使用class关键字<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">template class Foo&lt;int&gt;;</span><br></pre></td></tr></table></figure></li>
</ul>
<ol start="3">
<li>对于不同类型的实例的实现，都会根据传入的参数来对应实现</li>
<li>模板的二阶段检查(Two-Phase Translation)</li>
</ol>
<ul>
<li>第一阶段检查： 在未生成实例的定义期，检查未使用模板参数的代码正确性<ul>
<li>语法是否有误</li>
<li>是否使用了不发现的变量、函数名</li>
<li>static assertions 检查</li>
</ul>
</li>
<li>第二阶段检查： 在对应生成实例期间，检查依赖于模板参数的代码是否都有效。基本上，所有代码都需要经过第二阶段检查</li>
</ul>
<ol start="5">
<li>当且仅当有一个template type时，传入的值的类型必须与第一个值的类型保持一致。如果需要类型不一致，可通过以下几种方法来处理</li>
</ol>
<ul>
<li>使用staic_cast 来解决<ul>
<li>Eg: max(static_cast<double>(4),7.2); // ok</li>
</ul>
</li>
<li>使用显式声明来避免自动类型推导<ul>
<li>Eg: max<double>(4,7.2); //ok</li>
</ul>
</li>
<li>可以声明多个模板参数</li>
</ul>
<ol start="6">
<li>在C++中，Return Type 是不能从调用者使用该调用的上下文中推断出来的，可以使用如下方法来处理return type</li>
</ol>
<ul>
<li>在实例化时，通过显式指定return type <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">template &lt;typename T&gt;</span><br><span class="line">T max( T a, T b);</span><br><span class="line">...</span><br><span class="line">::max&lt;double&gt;(4,7.2);</span><br></pre></td></tr></table></figure></li>
<li>当定义多个template parameter时，可以通过指定第一个为return type，实例化时仅需显式指定return type就可以了<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">template &lt;typename RT,typename T1,typename T2&gt;</span><br><span class="line">RT max(T1 a,T2 b);</span><br><span class="line">...</span><br><span class="line">::max&lt;double&gt;(4,8.3);</span><br></pre></td></tr></table></figure></li>
</ul>
<ol start="7">
<li><p>在不自定义return type情况下，可以使用如下方法来决定return type:</p>
</li>
<li><p>使用auto进行类型的自动推导</p>
</li>
<li><p>使用自动推导函数    * C++ 11 版本可以使用decltype()函数，其写法如下<br><code> auto add(T x,U y) -> decltype(x+y);</code>    * C++ 14版本可以直接使用auto类型推导，原因：C++14版本使得普通函数具备返回值推导的能力</p>
</li>
<li><p>可以使用std::common_type函数(std::common_type 推导出一个可以覆盖所有所定义类型的通用类型)    * C++ 11版本使用std::common_type()    * C++ 14版本使用std::common_type_t()</p>
</li>
<li><p>在实例化重载template Function 过程中，会根据参数优先匹配非模板函数，不匹配才会寻找对应的模板函数相应实现</p>
</li>
<li><p>为何使用值传递而不使用引用传递的原因</p>
</li>
<li><p>值传递语法更简单</p>
</li>
<li><p>编译器对应优化更好</p>
</li>
<li><p>move语意使得拷贝更方便</p>
</li>
<li><p>有时根本没有move和copy</p>
</li>
</ol>
<h1 id="第二章"><a href="#第二章" class="headerlink" title="第二章"></a><center>第二章</center></h1><ol>
<li>在class template中，使用类名但不加模板参数代表类和模板参数作为它的实际参数,如下例所示<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">template &lt;typename T&gt;</span><br><span class="line">class Stack &#123;</span><br><span class="line">  ...</span><br><span class="line">  Stack (Stack const&amp;);</span><br><span class="line">  Stack&amp; operator&#x3D; (Stack const&amp;);</span><br><span class="line">&#125;;</span><br><span class="line">&#x2F;&#x2F;等价于下面的代码</span><br><span class="line">template &lt;typename T&gt;</span><br><span class="line">class Stack&lt;T&gt; &#123;</span><br><span class="line">  ...</span><br><span class="line">  Stack&lt;T&gt; (Stack&lt;T&gt; const&amp;);</span><br><span class="line">  Stack&lt;T&gt;&amp; operator&#x3D; (Stack&lt;T&gt; const&amp;);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
<li>C++ template 定义变量时需要注意”&gt;”的使用</li>
</ol>
<ul>
<li>C++ 11版本之前，两个相邻的”&gt;”中间需要安插一个空格<ul>
<li>Eg: Stack&lt;Stack<int> &gt;</li>
</ul>
</li>
<li>C++ 11版本及之后的版本，都可以直接写入两个”&gt;”而不需要空格<ul>
<li>Eg: Stack&lt;Stack<int>&gt;</li>
</ul>
</li>
</ul>
<ol start="3">
<li>如果想要特化一个class template，那么需要提前声明template&lt;&gt;和一个具有规范类型的类</li>
</ol>
<ul>
<li>类将作为模板参数</li>
<li>类型将被直接指定声明在类名中</li>
</ul>
<ol start="4">
<li>在一些特殊情况下采用偏特化会更好</li>
</ol>
<ul>
<li>与全特化基本类似，但是偏特化可以定义单个/多个参数</li>
<li>模板参数也需要被直接指定在类名的定义中</li>
</ul>
<ol start="5">
<li>默认class template parameters可以定义为一个容器用来管理数据</li>
</ol>
<ul>
<li>也可以在main()，对于想要使用的容器进行显式指定</li>
</ul>
<ol start="6">
<li>通用的类型别名</li>
</ol>
<ul>
<li>使用关键字 typedef,用法如下<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">typedef Stack&lt;int&gt; IntStack; &#x2F;&#x2F; typedef 关键字</span><br><span class="line">void foo (IntStack const&amp; s); &#x2F;&#x2F; s is stack of ints</span><br><span class="line">IntStack istack[10]; &#x2F;&#x2F; istack is array of 10 stacks of ints</span><br></pre></td></tr></table></figure></li>
<li>自C++ 11版本之后，可以使用关键字<code>using</code><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">using IntStack &#x3D; Stack&lt;int&gt;;  &#x2F;&#x2F;alias declaration</span><br><span class="line">void foo (IntStack const&amp; s); &#x2F;&#x2F;s is stack of ints</span><br><span class="line">IntStack istack[10];  &#x2F;&#x2F;istack is array of 10 stacks of ints</span><br></pre></td></tr></table></figure></li>
<li>IntStack 和 Stack<int> 是两个可互换符号的相同类型</li>
<li>对于已经存在的类型，如果需要定义一个别名。推荐使用typedef,这样会使得代码可读性更好。</li>
<li>C++11之后，我们可以alias templates,用法如下<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">template &lt;typename T&gt;</span><br><span class="line">using DequeStack &#x3D; Stack&lt;T, std::deque&lt;T&gt;&gt;;</span><br></pre></td></tr></table></figure></li>
</ul>
<ol start="7">
<li>class template 参数推导会根据字符串字面来进行推导，如下例<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">  Stack (T const&amp; elem) : elems(&#123;elem&#125;) &#123;&#125; &#x2F;&#x2F; initialize stack with one elemet</span><br><span class="line">  Stack stringStack &#x3D; &quot;bottom&quot;;  &#x2F;&#x2F;Stack&lt;char const[7]&gt; C++17版本推导结果</span><br><span class="line">&#x2F;&#x2F;这种写法会导致在T确定为char const[7]，将无法修改，所以应改写成</span><br><span class="line">  Stack (T elem) : elems(&#123;elem&#125;) &#123;&#125;;</span><br><span class="line">  Stack stringStack &#x3D; &quot;bottom&quot; &#x2F;&#x2F; Stack&lt;char const*&gt; C++ 17自动推导</span><br></pre></td></tr></table></figure></li>
</ol>
]]></content>
      <categories>
        <category>Read Notes</category>
      </categories>
      <tags>
        <tag>theory</tag>
      </tags>
  </entry>
  <entry>
    <title>Data lab</title>
    <url>/2021/01/28/Data-lab/</url>
    <content><![CDATA[<h1 id="实验目标"><a href="#实验目标" class="headerlink" title="实验目标"></a>实验目标</h1><p>通过本次实验来熟悉整型和浮点型的位表达形式</p>
<p>作业内容：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th style="text-align:center">函数名</th>
<th style="text-align:center">内容</th>
<th style="text-align:center">难度</th>
<th style="text-align:center">限制指令数</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">bitXor(int x,int y)</td>
<td style="text-align:center">使用逻辑或和逻辑与代替异或</td>
<td style="text-align:center">1</td>
<td style="text-align:center">14</td>
</tr>
<tr>
<td style="text-align:center">tmin(void)</td>
<td style="text-align:center">返回最小的补码</td>
<td style="text-align:center">1</td>
<td style="text-align:center">4</td>
</tr>
<tr>
<td style="text-align:center">isTmax(int x)</td>
<td style="text-align:center">x是最大的32位补码</td>
<td style="text-align:center">1</td>
<td style="text-align:center">10</td>
</tr>
<tr>
<td style="text-align:center">allOddBits(int x)</td>
<td style="text-align:center">所有的奇数位都为1吗</td>
<td style="text-align:center">2</td>
<td style="text-align:center">12</td>
</tr>
<tr>
<td style="text-align:center">negate(int x)</td>
<td style="text-align:center">不使用负号</td>
<td style="text-align:center">2</td>
<td style="text-align:center">5</td>
</tr>
<tr>
<td style="text-align:center">isAsciiDigit(int x)</td>
<td style="text-align:center">x是ASCII码吗</td>
<td style="text-align:center">3</td>
<td style="text-align:center">15</td>
</tr>
<tr>
<td style="text-align:center">conditional(int x,int y,int z)</td>
<td style="text-align:center">就像 <code>x ? y:x</code></td>
<td style="text-align:center">3</td>
<td style="text-align:center">16</td>
</tr>
<tr>
<td style="text-align:center">isLessOrEqual(int x,int y)</td>
<td style="text-align:center"><code>x&lt;=y? 1:0</code></td>
<td style="text-align:center">3</td>
<td style="text-align:center">24</td>
</tr>
<tr>
<td style="text-align:center">logicalNeg(int x)</td>
<td style="text-align:center">实现<code>!</code>操作</td>
<td style="text-align:center">4</td>
<td style="text-align:center">12</td>
</tr>
<tr>
<td style="text-align:center">howManyBits(int x)</td>
<td style="text-align:center">计算表达式所需的最少位数</td>
<td style="text-align:center">4</td>
<td style="text-align:center">90</td>
</tr>
<tr>
<td style="text-align:center">floatScale2(unsigned uf)</td>
<td style="text-align:center">计算<code>2 * uf</code></td>
<td style="text-align:center">4</td>
<td style="text-align:center">30</td>
</tr>
<tr>
<td style="text-align:center">floatFloat2Int(unsigned uf)</td>
<td style="text-align:center">计算<code>(int)uf</code></td>
<td style="text-align:center">4</td>
<td style="text-align:center">30</td>
</tr>
<tr>
<td style="text-align:center">floatPower2(int x)</td>
<td style="text-align:center">计算<code>(float)x</code></td>
<td style="text-align:center">4</td>
<td style="text-align:center">30</td>
</tr>
</tbody>
</table>
</div>
<h1 id="实验要求"><a href="#实验要求" class="headerlink" title="实验要求"></a>实验要求</h1><p>一共需要补齐13个函数，所有的修改都保存在<code>bits.c</code>中</p>
<h2 id="提示与要求："><a href="#提示与要求：" class="headerlink" title="提示与要求："></a>提示与要求：</h2><ol>
<li>要符合题目编码格式要求</li>
<li>整型的范围是0到255(0xFF),不可以超过限制</li>
<li>只能包含参数和局部变量</li>
<li>一元操作符有<code>!、~</code></li>
<li>二元操作符有<code>＆、|、＋、&lt;&lt;、&gt;&gt;</code></li>
</ol>
<h2 id="禁止的操作有"><a href="#禁止的操作有" class="headerlink" title="禁止的操作有:"></a>禁止的操作有:</h2><ol>
<li>使用任何条件控制语句</li>
<li>定义或使用任何宏</li>
<li>在文件中添加额外的函数</li>
<li>调用任何函数</li>
<li>使用其它操作符，如<code>&amp;&amp;, ||, -, or ?:</code></li>
<li>使用任何形式的类型转换</li>
<li>使用除int外的任意类型(针对整型)</li>
<li>使用除int，unsigned之外的类型(针对浮点型)</li>
</ol>
<h2 id="默认机器的配置："><a href="#默认机器的配置：" class="headerlink" title="默认机器的配置："></a>默认机器的配置：</h2><ol>
<li>使用2’s complent,32位的机器</li>
<li>执行算术右移</li>
<li>移位范围$[0,31]$,如果过大或过小可能会发生不可预测的情况</li>
</ol>
<h1 id="题目及解法"><a href="#题目及解法" class="headerlink" title="题目及解法"></a>题目及解法</h1>]]></content>
      <categories>
        <category>lab</category>
      </categories>
      <tags>
        <tag>OS</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-1</title>
    <url>/2020/01/12/Leetcode-1/</url>
    <content><![CDATA[<h2 id="Qestion"><a href="#Qestion" class="headerlink" title="Qestion"></a>Qestion</h2><p>Give an array of intergets <code>nums</code> and an integer <code>target</code>,return indices of the two numbers such that they add up to <code>target</code>.<br>You may assume that each input would have exctly one solution,and you may use the same element twice.<br>You can return the answer in any order.  </p>
<p> <strong>Example 1</strong></p>
<blockquote>
<p><strong>Input</strong> : nums [2,7,11,15] , target = 9<br><strong>Output</strong> : [0,1]<br><strong>Output</strong> :Because nums[0] + nums[1] == 9, we return [0,1].</p>
</blockquote>
<p> <strong>Examp 2</strong></p>
<blockquote>
<p><strong>Input</strong> : nums[3,2,4],target = 6<br><strong>Output</strong> : [1,2]</p>
</blockquote>
<p> <strong>Example 3</strong></p>
<blockquote>
<p><strong>Input</strong> : nums = [3,3],target = 6<br><strong>Output</strong> : [0,1]</p>
</blockquote>
<p> <strong>ConStraints</strong></p>
<blockquote>
<ul>
<li>2 &lt;= nums.length &lt;= $ 2^{10} $</li>
<li>$-10^{9}$&lt;= nums[i] &lt;= $ 10^{9} $</li>
<li>$-10^{9}$ &lt;= target &lt;= $ 10^{9} $</li>
<li><strong>Only one valid anser exits.</strong></li>
</ul>
</blockquote>
<h2 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h2><h3 id="hash法"><a href="#hash法" class="headerlink" title="hash法"></a>hash法</h3><p>  利用unordered_map数组构造映射,遍历nums[i]时，看target-nums[i]是否存在于hash表中<br>  时间复杂度 O(n),空间复杂度O(n)<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    vector&lt;int&gt; twoSum(vector&lt;int&gt;&amp; nums, int target) &#123;</span><br><span class="line">        unordered_map&lt;int, int&gt; indices;</span><br><span class="line">        for (int i &#x3D; 0; i &lt; nums.size(); i++) &#123;</span><br><span class="line">            if (indices.find(target - nums[i]) !&#x3D; indices.end()) &#123;</span><br><span class="line">                return &#123;indices[target - nums[i]], i&#125;;</span><br><span class="line">            &#125;</span><br><span class="line">            indices[nums[i]] &#x3D; i;</span><br><span class="line">        &#125;</span><br><span class="line">        return &#123;&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<h3 id="暴力破解法"><a href="#暴力破解法" class="headerlink" title="暴力破解法"></a>暴力破解法</h3><p>  暴力破解时间复杂度O($n^{2}$),空间复杂度O(1)<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    vector&lt;int&gt; twoSum(vector&lt;int&gt;&amp; nums, int target) &#123;</span><br><span class="line">        vector&lt;int&gt; ans;</span><br><span class="line">        for(int i&#x3D;0;i&lt;nums.size();i++)&#123;</span><br><span class="line">              for(int j&#x3D;i+1;j&lt;nums.size();j++)&#123;</span><br><span class="line">                  if(nums[i]+nums[j]&#x3D;&#x3D;target)&#123;</span><br><span class="line">                      ans.push_back(i);</span><br><span class="line">                      ans.push_back(j);</span><br><span class="line">                      return ans;</span><br><span class="line">                  &#125;</span><br><span class="line">              &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<h3 id="排序-双指针法"><a href="#排序-双指针法" class="headerlink" title="排序+双指针法"></a>排序+双指针法</h3><p>  先将数组的顺序排好O(nlogn),再利用双指针法遍历一遍O(n)得到结果<br>  为保存下标信息另开一个数组<br>  时间复杂度O(nlogn),空间复杂度O(n)<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    vector&lt;int&gt; twoSum(vector&lt;int&gt;&amp; nums, int target) &#123;</span><br><span class="line">        vector&lt;int&gt; ans;</span><br><span class="line">        vector&lt;int&gt; temp;</span><br><span class="line">        temp&#x3D;nums;</span><br><span class="line">        int n&#x3D;temp.size();</span><br><span class="line">       sort(temp.begin(),temp.end());</span><br><span class="line">       int i&#x3D;0,j&#x3D;n-1;</span><br><span class="line">       while(i&lt;j)&#123;</span><br><span class="line">           if(temp[i]+temp[j]&gt;target)j--;</span><br><span class="line">          else if(temp[i]+temp[j]&lt;target)i++;</span><br><span class="line">          else break;</span><br><span class="line">       &#125;</span><br><span class="line">       if(i&lt;j)&#123;</span><br><span class="line">      for(int k&#x3D;0;k&lt;n;k++)&#123;</span><br><span class="line">          if(i&lt;n&amp;&amp;nums[k]&#x3D;&#x3D;temp[i])&#123;</span><br><span class="line">              ans.push_back(k);</span><br><span class="line">              i&#x3D;n;</span><br><span class="line">          &#125;</span><br><span class="line">         else if(j&lt;n&amp;&amp;nums[k]&#x3D;&#x3D;temp[j])&#123;</span><br><span class="line">              ans.push_back(k);</span><br><span class="line">              j&#x3D;n;</span><br><span class="line">          &#125;</span><br><span class="line">          if(i&#x3D;&#x3D;n&amp;&amp;j&#x3D;&#x3D;n)return ans;</span><br><span class="line">      &#125;</span><br><span class="line">      &#125;</span><br><span class="line">        return ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-10</title>
    <url>/2020/10/28/Leetcode-10/</url>
    <content><![CDATA[<h1 id="Question"><a href="#Question" class="headerlink" title="Question"></a>Question</h1><p> &emsp;&emsp;Implement <code>strStr()</code>.Return the index of the first occurrence of needle in haystack, or <code>-1</code> if <code>needle</code> is not part of <code>haystack</code>.<br> <strong>Clarification:</strong><br> &emsp;&emsp;What should we return when needle is an empty string? This is a great question to ask during an interview.</p>
<p> &emsp;&emsp;For the purpose of this problem, we will return 0 when needle is an empty string. This is consistent to C’s strstr() and Java’s indexOf().</p>
<p> <strong>Example1:</strong></p>
<blockquote>
<p><strong>Input:</strong> haystack = “hello”, needle = “ll”<br><strong>Output:</strong> 2</p>
</blockquote>
<p> <strong>Example2:</strong></p>
<blockquote>
<p><strong>Input:</strong> haystack = “aaaaa”, needle = “bba”<br><strong>Output:</strong> -1</p>
</blockquote>
<p> <strong>Example3:</strong></p>
<blockquote>
<p><strong>Input:</strong> haystack = “”, needle = “”<br><strong>Output:</strong> 0</p>
</blockquote>
<p> <strong>Constraints:</strong></p>
<ul>
<li><code> 0 &lt;= haystack.length, needle.length &lt;= 5 * 10<sup>4</sup> </code></li>
<li><code>haystack</code>and <code>needle</code> consist of only lower-case English characters.</li>
</ul>
<h1 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h1><h2 id="思路一"><a href="#思路一" class="headerlink" title="思路一:"></a>思路一:</h2><p>  &emsp;&emsp;两个字符长度相减+1得到的长度就是需要比较的长度，当needle长度为0时，返回0。当haystack与needle不匹配时，返回-1。当haystack与needle匹配时，返回i的值。时间复杂度为O($n^{2}$),空间复杂度为O(n),具体代码如下：<br>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int strStr(string haystack, string needle) &#123;</span><br><span class="line">        int m &#x3D; haystack.size();</span><br><span class="line">        int n &#x3D; needle.size();</span><br><span class="line">        int r &#x3D; m - n + 1;</span><br><span class="line">        </span><br><span class="line">        if (n &#x3D;&#x3D; 0) return 0;</span><br><span class="line">        </span><br><span class="line">        for (int i &#x3D; 0 ; i &lt; r; i++) &#123;</span><br><span class="line">            int  j &#x3D; i,k &#x3D; 0;</span><br><span class="line">            while (k &lt; n &amp;&amp; haystack[j] &#x3D;&#x3D; needle[k]) </span><br><span class="line">                k++,j++;</span><br><span class="line">            if (k &#x3D;&#x3D; n) return i;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        return -1;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<h2 id="思路二"><a href="#思路二" class="headerlink" title="思路二:"></a>思路二:</h2><p>&emsp;&emsp;KMP(字符串查找算法),lps是needle的索引。这里使用的kmp算法，把needle作为kmp前缀,来进行字符比对。如果字符相同，那么len增加。如果不相等，就要看len的值，如果len的值为0那么就将lps[i]的值设置为0。如果len不为零，那么就把lps[len-1]的值赋给len，这一步是为了防止对应字符中间出现了不相等的，将字符串右移。时间复杂度为O(n); </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int strStr(string haystack, string needle) &#123;</span><br><span class="line">        if (needle.empty()) return 0;</span><br><span class="line">        </span><br><span class="line">        string kmp &#x3D; needle + &quot;#&quot; + haystack;</span><br><span class="line">        int m &#x3D; kmp.size(),n &#x3D; needle.size();</span><br><span class="line">        vector&lt;int&gt; lps(m,0);</span><br><span class="line">        </span><br><span class="line">        for (int len &#x3D; 0,i &#x3D; 1; i &lt; m;) &#123;</span><br><span class="line">            if (kmp[len] &#x3D;&#x3D; kmp[i]) &#123;</span><br><span class="line">                lps[i] &#x3D; ++len;</span><br><span class="line">                if (len &#x3D;&#x3D; n) </span><br><span class="line">                    return i - 2 * n;</span><br><span class="line">                ++i;</span><br><span class="line">            &#125;</span><br><span class="line">            else if (len &#x3D;&#x3D; 0) </span><br><span class="line">                 lps[i++] &#x3D; 0;</span><br><span class="line">            else </span><br><span class="line">                len &#x3D; lps[len - 1];</span><br><span class="line">        &#125;</span><br><span class="line">        return -1;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-11</title>
    <url>/2020/11/02/Leetcode-11/</url>
    <content><![CDATA[<h1 id="Question"><a href="#Question" class="headerlink" title="Question"></a>Question</h1><p> &emsp;&emsp; Given a sorted array of distinct integers and a target value, return the index if the target is found. If not, return the index where it would be if it were inserted in order.</p>
<p><strong>Example 1:</strong></p>
<p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> nums = [1,3,5,6], target = 5 <br>
  <strong>Output:</strong> 2
</p>


<p><strong>Example 2:</strong></p>
<p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> nums = [1,3,5,6], target = 2 <br>
  <strong>Output:</strong> 1
</p>

<p><strong>Example 3:</strong></p>
<p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> nums = [1,3,5,6], target = 7 <br>
  <strong>Output:</strong> 4
</p>


<p><strong>Example 4:</strong></p>
<p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> nums = [1,3,5,6], target = 0 <br>
  <strong>Output:</strong> 0
</p>

<p><strong>Example 5:</strong></p>
<p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> nums = [1], target = 0 <br>
  <strong>Output:</strong> 0
</p>


<p><strong>Constraints:</strong></p>
<ul>
<li><code> 1 &lt;= nums.length &lt;= 10<sup>4</sup> </code> </li>
<li><code> -10<sup>4</sup> &lt;= nums[i] &lt;= 10<sup>4<sup> </code></li>
<li><code>nums</code> contains <strong>distinct</strong> values sorted in <strong>ascending</strong> order</li>
<li><code> -10<sup>4</sup> &lt;= target &lt;= 10<sup>4<sup> </code></li>
</ul>
<h1 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h1><h2 id="思路一"><a href="#思路一" class="headerlink" title="思路一"></a>思路一</h2><p>&emsp;&emsp;<strong>二分法查找(binary Search):</strong> 首先先确定最高位和最低位的值,因为随着low和high的位置不断变化,<br>所以middle的值是不断再改变的。这里用移位(&gt;&gt;1)代替了除法,这样不会增加运算的复杂性。<br>&emsp;&emsp;如果目标值大于中间值,那么low就要变化;相反，如果目标值小于中间值，high就需要变化;如果目标值正好等于中间值就可以直接输出了。<br>时间复杂度为O(logn),具体代码如下:<br> <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int searchInsert(vector&lt;int&gt;&amp; nums, int target) &#123;</span><br><span class="line">        int low &#x3D; 0,high &#x3D; nums.size() - 1,n &#x3D; nums.size();</span><br><span class="line">        </span><br><span class="line">        while (low &lt;&#x3D; high) &#123;</span><br><span class="line">            int middle &#x3D; low + ((high - low) &gt;&gt; 1);</span><br><span class="line">            if (target &gt; nums[middle]) low &#x3D; middle + 1;</span><br><span class="line">            else if (target &lt; nums[middle]) high &#x3D; middle - 1;</span><br><span class="line">            else return middle;</span><br><span class="line">        &#125;</span><br><span class="line">        return low;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<h2 id="思路二"><a href="#思路二" class="headerlink" title="思路二"></a>思路二</h2><p>  &emsp;&emsp;<strong>线性查找(linear search):</strong> 将nums的值挨个赋给i,如果i小于目标值,result就自增。相反如果大于等于目标值，就可以直接跳出循环，输出结果了。时间复杂度为O(n),具体代码如下:<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int searchInsert(vector&lt;int&gt;&amp; nums, int target) &#123;</span><br><span class="line">        int result &#x3D; 0;</span><br><span class="line">        for (auto&amp; i : nums) &#123;</span><br><span class="line">            if (i &lt; target)&#123;</span><br><span class="line">                result++;</span><br><span class="line">            &#125; </span><br><span class="line">            else break;</span><br><span class="line">        &#125;</span><br><span class="line">        return result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-12</title>
    <url>/2020/11/04/Leetcode-12/</url>
    <content><![CDATA[<h1 id="Question"><a href="#Question" class="headerlink" title="Question"></a>Question</h1><p>&emsp;&emsp;The <strong>count-and-say</strong> sequence is a sequence of digit strings defined by the recursive formula:</p>
<ul>
<li><code>countAndSay(1) = “1” </code></li>
<li><code>countAndSay(n)</code> is the way you would “say” digit string from <code>countAndSay(n-1)</code>,which is then converted into a different digit string.</li>
</ul>
<p>&emsp;&emsp;To determine how you “say” a digit string, split it into the <strong>minimal</strong> number of groups so that each group is a contiguous section all of the <strong>same character</strong>.<br>&emsp;&emsp;Then for each group, say the number of characters,then say the character. To convert the saying into a digit string, replace the counts with a number and concatenate every saying.  </p>
<p>For example, the saying and conversion for digit string <code>“3322251”</code>:<br><img src="https://assets.leetcode.com/uploads/2020/10/23/countandsay.jpg" alt="avatar"></p>
<p>Given a positive integer <code>n</code>, return the <code>n<sup>th</sup></code> term of the <strong>count-and-say</strong> sequence.</p>
<p><strong>Example 1:</strong></p>
<p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> n = 1 <br>
  <strong>Output:</strong> "1" <br>
  <strong>Explanation:</strong> This is base case
</p>

<p><strong>Example 2:</strong></p>
<p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> n = 4
  <strong>Output:</strong> "1211"
  <strong>Explanation:</strong> <br>
  countAndSay(1) = "1" <br>
  countAndSay(2) = say "1" = one 1 = "11" <br>
  countAndSay(3) = say "11" two 1's = "21" <br>
  countAndSay(4) = say "21" = one 2 + one 1 = "12" + "11" = "1211" <br>
</p>

<p><strong>Constraints:</strong></p>
<ul>
<li><code> 1 &lt;= n &lt;= 30 </code></li>
</ul>
<h1 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h1><h2 id="思路一"><a href="#思路一" class="headerlink" title="思路一"></a>思路一</h2><p>  &emsp;&emsp;<strong>暴力破解法:</strong> 首先确定基础case “1”, ans用于存储输出的字符。count用于计数,如果想要知道4所对应的值，那么知道了3所对应的数值就行了。如果出现了重复就通过while语句来增加对应的值。时间复杂度为O(n),具体代码如下:<br>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">  class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    string countAndSay(int n) &#123;</span><br><span class="line">        string res &#x3D; &quot;1&quot;;</span><br><span class="line">        </span><br><span class="line">        while (--n) &#123;</span><br><span class="line">            string ans &#x3D; &quot;&quot;;</span><br><span class="line">            for (int i &#x3D; 0 ;i &lt; res.size() ;i++) &#123;</span><br><span class="line">                int count &#x3D; 1;</span><br><span class="line">                while ( (i+1 &lt; res.size()) &amp;&amp; (res[i] &#x3D;&#x3D; res[i+1]))</span><br><span class="line">                    i++,count++;</span><br><span class="line">                ans +&#x3D; to_string(count) + res[i];</span><br><span class="line">            &#125;</span><br><span class="line">            res &#x3D; ans;</span><br><span class="line">        &#125;</span><br><span class="line">        return res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-13</title>
    <url>/2021/01/25/Leetcode-13/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;有一群孩子和一堆饼干，每个孩子有一个饥饿度，每个饼干都有一个大小。每个孩子只能吃一个饼干，且只有饼干的大小不小于孩子的饥饿度时，这个孩子可以吃饱。求解最多有多少孩子可以吃饱。</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p>&emsp;&emsp;输入两个数组，分别代表孩子的饥饿度和饼干的大小。输出最多有多少孩子可以吃饱的数量。</p>
<p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> nums = [1,2],[1,2,3] <br>
  <strong>Output:</strong> 5
</p><br>&emsp;&emsp;在这个样例中，我们可以给两个孩子喂[1,2],[1,3],[2,3]这三种组合的任意一种。</p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;因为饥饿度最小的孩子最容易吃饱，所以我们先考虑这个孩子。为了尽量便利剩下的饼干可以满足饥饿度更大的孩子，所以我们应该把大于等于这个孩子饥饿度的，且大小最小的饼干给这个孩子。满足了这个孩子之后，我们采取同样的策略，考虑剩下孩子里饥饿度最小的孩子，直到没有满足条件的饼干存在。<br>&emsp;&emsp;简而言之，这里的贪心策略是，给剩余孩子里最小饥饿度的孩子分配最小的能饮腹的饼干。至于具体实现，因为我们需要获得大小关系，一个便捷的方法就是把孩子和饼干分别排序。这样我们就可以从饥饿度最小的孩子和大小最小的饼干出发，计算有多少组合可以满足条件。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int findContentChildren(vector&lt;int&gt;&amp; children, vector&lt;int&gt;&amp; cookies) &#123;</span><br><span class="line">        sort(children.begin(),children.end());</span><br><span class="line">        sort(cookies.begin(),cookies.end());</span><br><span class="line">        </span><br><span class="line">        int child &#x3D; 0,cookie &#x3D; 0;</span><br><span class="line">        while (child &lt; children.size() &amp;&amp; cookie &lt; cookies.size()) &#123;</span><br><span class="line">            if (children[child] &lt;&#x3D; cookies[cookie]) ++child;</span><br><span class="line">            ++cookie;</span><br><span class="line">        &#125;</span><br><span class="line">        return child;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-14</title>
    <url>/2021/01/25/Leetcode-14/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;一群孩子站成一排，每一个孩子有自己的评分。现在需要给这些孩子必糖果，规则是如果一个孩子的评分比自己身旁的一个孩子要高，那么这个孩子就必须得到比身旁孩子更多的糖果；所有孩子至少要有一个糖果，求解最少需要多少个糖果。</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p>&emsp;&emsp;输入是一个数组，表示孩子的评分。输出是最少糖果的数量。</p>
<p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> nums = [1,0,2] <br>
  <strong>Output:</strong> 5
</p><br>&emsp;&emsp;在这个样例中，最少的糖果分法是[2,1,2].</p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;做完分饼干的题目，你会不会认为存在比较关系的信心策略一定需要智育或是选择？虽然这道题也是运用信心策略，但是我们只需要简单的两次遍历即可首先把所有孩子的糖果初始化为1；</p>
<ul>
<li>先从左往右遍历一遍，如果右边孩子的评分比左边的高，则右边孩子的糖果数更新为左边孩子的糖果数加1；</li>
<li>再从左往左遍历一遍，如果左边孩子的评分比右边的高，且左边孩子当前的糖果数不大于右边孩子的糖果数不大于右边孩子的糖果数，则左边孩子的糖果数更新为右边孩子的糖果数加1</li>
</ul>
<p>&emsp;&emsp;通过这两次遍历，分配的糖果就可以满足题目要求了。这里的信心策略即为，在每次遍历中，只考虑并更新相邻一侧的大小关系。在样例中，我们初始化糖果分配为[1,1,1],第一次遍历更新后的结果为[1,1,2],第二次遍历更新后的结果为[2,1,2].<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int candy(vector&lt;int&gt;&amp; ratings) &#123;</span><br><span class="line">        int size &#x3D; ratings.size();</span><br><span class="line">        if (size &lt; 2) return size;</span><br><span class="line">        </span><br><span class="line">        vector&lt;int&gt; nums(size,1);</span><br><span class="line">        </span><br><span class="line">        for (int i &#x3D; 0; i &lt; size-1; i++) &#123;</span><br><span class="line">            if (ratings[i+1] &gt; ratings[i]) </span><br><span class="line">                nums[i+1] &#x3D; nums[i] + 1;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        for (int i &#x3D; size -1 ; i &gt; 0 ; --i) &#123;</span><br><span class="line">            if (ratings[i-1] &gt; ratings[i])</span><br><span class="line">                nums[i-1] &#x3D; max(nums[i-1],nums[i]+1);</span><br><span class="line">        &#125;</span><br><span class="line">        return accumulate(nums.begin(),nums.end(),0);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-15</title>
    <url>/2021/02/03/Leetcode-15/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;给定多个区间，计算让这些区间不重叠所需要移除的最少个数。起止相连不算重叠。</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p>&emsp;&emps;输入是一个数组，数组由多个长度固定为2的数组组成，表示区间的开始和结尾。输出一个整数，表示需要移除的区间数量。</p>
<p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong>  [[1,2], [2,4], [1,3]]<br>
  <strong>Output:</strong> 1
</p><br>&emsp;&emsp;在这个样例中，我们可以移除区间[1,3],使得剩余的区间[[1,2],[2,4]]互不重叠。</p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;在选择要保留区间时，区间的结尾十分重要：选择的区间结尾越小，余留给其它区间的空间就越大，就越能保留更多的区间。因此，我们采取的贪心策略为：优先保留结尾小且不相交的区间。<br>&emsp;&emsp;具体实现方法为，先把区间按照结尾的大小进行增序排序，每次选择结尾最小且和前一个选择的区间不重叠的区间。我们这里使用C++的Lambda,结合std::sort()函数进行自定义排序。<br>&emsp;&emsp;在样例中，排序后的数组为[[1,2],[1,3],[2,4]].按照我们的贪心策略，首先初始化为区间[1,2];由于[1,3]与[1,2]相交，我们跳过该区间；由于[2,4]与[1,2]不相交，我们将其保留。因此最终保留的区间为[[1,2],[2,4]]。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int eraseOverlapIntervals(vector&lt;vector&lt;int&gt;&gt;&amp; intervals) &#123;</span><br><span class="line">        if (intervals.empty()) return 0;</span><br><span class="line">        int n &#x3D; intervals.size();</span><br><span class="line">        </span><br><span class="line">        sort(intervals.begin(),intervals.end(),[&amp;](vector&lt;int&gt; A,vector&lt;int&gt; B)&#123;return A[1] &lt; B[1];&#125;);</span><br><span class="line">        </span><br><span class="line">        int total &#x3D; 0,prev &#x3D; intervals[0][1];</span><br><span class="line">        for (int i &#x3D; 1; i &lt; n; i++) &#123;</span><br><span class="line">            if (intervals[i][0] &lt; prev) </span><br><span class="line">                ++total;</span><br><span class="line">            else </span><br><span class="line">                prev &#x3D; intervals[i][1];</span><br><span class="line">        &#125;</span><br><span class="line">        return total;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-17</title>
    <url>/2021/02/08/Leetcode-17/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;在一个培育的整数数组里找到两个数，使它们的和为给定值。书籍有且有一对解。</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p>&emsp;&emsp;输入是一个数组(numbers)和一个给定值(target)。输出是两个数的位置，从1开始计数。</p>
<p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> flowerbed = [2,7,11,15], target = 9<br>
  <strong>Output:</strong> [1,2]
</p><br>&emsp;&emsp;要这个样例中，第一个数数字(2)和第二个数字(7)的和等于给定值(9)。</p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;因为数组已经排好序，我们可以采用方向相反的双指针来寻找这两个数字，一个初始指向最小的元素，即数组最左边，向右遍历；一个初始指向最大的元素，即数组最左边，向左遍历。<br>&emsp;&emsp;如果两个指针指向元素的和等于给定值那么它们就是我们要的结果，如果两个指针指向元素的和小于给定值，我们就把左边的指针右移一位，便利当前的和增加一点。如果两个指针指向元素的和大于给定值，我们把右边的指针左移一位，使得当前的和减少一点。<br>&emsp;&emsp;可以证明，对于排好序且有解的数组，双指针一定能遍历到最优解。证明方法如下：假设最优俀的两个数的位置分别是<code>l</code>和<code>r</code></p>
<ul>
<li>我们假设在左指针在l左边的时候，右指针已经移动到了r；此时两个指针指向值的和小于给定值，因此左指针会一直右移直到到达l。</li>
<li>同理，如果我们假设在右指针在r右边的时候，左指针已经移动到了l；此时两个指针指向值的和大于给定值，因此右指针会一直左移直到到达r。<br>&emsp;&emsp;所以双指针在任何时候都不可能牌(l,r)之间，又因为不满足条件时指针必须移动一个，所以最终一定会收敛于l和r之间。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    vector&lt;int&gt; twoSum(vector&lt;int&gt;&amp; nums, int target) &#123;</span><br><span class="line">        int l &#x3D; 0,r &#x3D; nums.size()-1,n &#x3D; nums.size();</span><br><span class="line">        while (l &lt;&#x3D; r) &#123;</span><br><span class="line">            if (nums[l] + nums[r] &#x3D;&#x3D; target) break;</span><br><span class="line">            if (nums[l] + nums[r] &gt; target) --r;</span><br><span class="line">            else ++l;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        return vector&lt;int&gt;&#123;l+1,r+1&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
</ul>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-16</title>
    <url>/2021/02/06/Leetcode-16/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;你有一个长花坛，其中有一些种植了花朵，有一些没有。花坛里的种的花必须要有间隔(即不能相邻而种)</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> flowerbed = [1,0,0,0,1], n = 1<br>
  <strong>Output:</strong> true
</p><br>&emsp;&emsp;在这个样例中，flowerbed[2]左右没有花朵，可以种植一朵花，符合题目种一朵花的要求，即输出true</p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;对于这题我们先考虑第一朵花怎么种，首先头和尾需要优先进行处理。我们可以对这个数组首尾都添加一个0，从而确定首尾。那我们就可以从原数组的第一个开始进行判断(i=1)，到原数组的最后一个(i=len-1)。根据题目要求，种花的条件是左右都没有花且即将要种的地是空闲的。所以就要求{i-1,i,i+1}都为0时，计数的才会自增1。循环结束，因为首尾添加的0可能会导致花会被多种，因此,(计数&gt;=要求的花)就可以了。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    bool canPlaceFlowers(vector&lt;int&gt;&amp; flowerbed, int n) &#123;</span><br><span class="line">        flowerbed.push_back(0);</span><br><span class="line">        flowerbed.insert(flowerbed.begin(),0);</span><br><span class="line">        </span><br><span class="line">        int i &#x3D; 1,sum &#x3D; 0,len &#x3D; flowerbed.size();</span><br><span class="line">        while (i &lt; len-1) &#123;</span><br><span class="line">            if (flowerbed[i]+flowerbed[i+1]+flowerbed[i-1] &#x3D;&#x3D; 0)</span><br><span class="line">                ++sum,++i;</span><br><span class="line">            ++i;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        return sum &gt;&#x3D; n;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-18</title>
    <url>/2021/02/09/Leetcode-18/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;给定两个有序数组，把两个数组合并为一个</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p>&emsp;&emsp;输入是两个数组和它们分别的长度m和m。其中第一个数组的长度被延长至m+n，多出的n位被0填补。题目要求把第二个数组归并到第一个数组上，不需要开辟额外空间。</p>
<p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3<br>
  <strong>Output:</strong> nums1 = [1,2,2,3,5,6] 
</p></p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;因为这两个已经排好序，我们可以把两个指针分别放在两个数组的末尾，即nums1的m-1位和nums2的n-1位，每次将较大的那个数字复制到nums1的后边，然后向前移动一位。因为我们也要定位nums1的末尾，所以我们还需要第三个指针，以便复制。<br>&emsp;&emsp;在以下的代码里，我们直接利用m和n当作两个数组的指针，再额外创立一个pos指针，起始位置为m+n-1。每次向前移动m或n的时候，也要向前移动pos。这里需要注意，如果nums1的数字已经复制完，不要忘记把nums2的数字继续复制；如果nums2的数字已经复制完，剩余nums1的数字不需要改变，因为它们已经被排好序。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    void merge(vector&lt;int&gt;&amp; nums1, int m, vector&lt;int&gt;&amp; nums2, int n) &#123;</span><br><span class="line">        int i &#x3D; m - 1, j &#x3D; n - 1, pos &#x3D; m + n - 1;</span><br><span class="line">        </span><br><span class="line">        while (i &gt;&#x3D; 0 &amp;&amp; j &gt;&#x3D; 0) &#123;</span><br><span class="line">            if (nums1[i] &gt; nums2[j])</span><br><span class="line">                nums1[pos--] &#x3D; nums1[i--];</span><br><span class="line">            else </span><br><span class="line">                nums1[pos--] &#x3D; nums2[j--];</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        while (j &gt;&#x3D; 0)</span><br><span class="line">            nums1[pos--] &#x3D; nums2[j--];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-19</title>
    <url>/2021/03/04/Leetcode-19/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emps;在二维空间中散布着一些气球。对于每个气球，它们都是在同一水平直径上。所以对于纵坐标就没有那么重要了，x轴(横坐标)是气球的起点和终点，且起点始终小于终点。<br>&emsp;&emsp;沿x轴不同的点会垂直向上射出一个箭头，如果Xstart&lt;= x &lt;= Xend，那么气球将会被箭射爆。根据输入的气球坐标，得到最少使用的箭数。</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p>&emsp;&emsp;输入是一个数组，数组由多个气球的起始和终止坐标所组成。输出一个整数，表示所需最少的箭头数。</p>
<p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> points = [[10,16],[2,8],[1,6],[7,12]]<br>
  <strong>Output:</strong> 2 
</p><br>&emsp;&emsp;在这个样例中，[1,6]与[2,8]重叠，[7,12]与[10,16]重叠，所以我们只需要两只箭就可以了</p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;想要用最少的箭来射击最多的气球。那么就是说对于有重叠区间的气球，我们只需要安排一只箭射击就可以了。所以我们采取的贪心策略为，优先射击无重叠区域的气球<br>&emsp;&emsp;具体实现方法为，先把这个数组进行<code>std::sort</code>排序(默认升序)，排序后的数组为[[1,6],[2,8],[7,12],[10,16]]。按照我们的贪心策略，在循环内初始化为[1,6]。查看与其重叠的区间，即第一个区间的终止位置可以覆盖接下来的起始位置。如果存在更小区间的气球，则更新prev并且对i进行自增。倘若条件不满足total 进行自增。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int findMinArrowShots(vector&lt;vector&lt;int&gt;&gt;&amp; points) &#123;</span><br><span class="line">        sort(points.begin(),points.end());</span><br><span class="line">        int n &#x3D; points.size();</span><br><span class="line">        int total &#x3D; 0,i &#x3D; 0;</span><br><span class="line">        while (i &lt; n) &#123;</span><br><span class="line">            int prev &#x3D; points[i][1];</span><br><span class="line">            while (i &lt; n &amp;&amp; prev &gt;&#x3D; points[i][0]) &#123;</span><br><span class="line">                prev &#x3D; min(points[i][1],prev);</span><br><span class="line">                ++i;</span><br><span class="line">            &#125;</span><br><span class="line">            ++total;</span><br><span class="line">        &#125;</span><br><span class="line">        return total;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-2</title>
    <url>/2020/09/23/Leetcode-2/</url>
    <content><![CDATA[<h2 id="Question"><a href="#Question" class="headerlink" title="Question"></a>Question</h2><p> Given a 32-bit signed integer, reverse digits of an integer.</p>
<p> <strong>Example 1</strong></p>
<blockquote>
<p><strong>iput</strong> : 123<br><strong>Output</strong> : 321</p>
</blockquote>
<p> <strong>Example 2</strong></p>
<blockquote>
<p><strong>Input</strong> : -123<br><strong>Output</strong> : -321</p>
</blockquote>
<p> <strong>Example 3</strong></p>
<blockquote>
<p><strong>Input</strong> : 120<br><strong>Output</strong> : 21</p>
</blockquote>
<p> <strong>Note :</strong><br>&emsp;&emsp;Assume we are dealing with an environment which could only store integers within the 32-bit signed integer range: [$(-2)^{31}$, $2^{31}$-1]. For the purpose of this problem, assume that your function returns 0 when the reversed integer overflows.</p>
<h2 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h2><h3 id="计算法"><a href="#计算法" class="headerlink" title="计算法"></a>计算法</h3><p> 这是一个比较投机的方案，因为使用了数字类型long后，就不会再有溢出的问题，最后再和INT_MAX比较一下就可以了。时间复杂度为O(nlogn)<br> <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int reverse(int x) &#123;</span><br><span class="line">        long res &#x3D; 0;</span><br><span class="line">        for (;x !&#x3D; 0 ; x &#x2F;&#x3D; 10)  res &#x3D; res * 10 + x % 10;</span><br><span class="line">        return (res &gt; INT_MAX || res &lt; INT_MIN) ? 0 : res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-20</title>
    <url>/2021/03/10/Leetcode-20/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;翻转一棵二叉树， 即左右节点互换</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Input:</span><br><span class="line">     4</span><br><span class="line">   &#x2F;   \</span><br><span class="line">  2     7</span><br><span class="line"> &#x2F; \   &#x2F; \</span><br><span class="line">1   3 6   9  </span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Output:</span><br><span class="line">     4</span><br><span class="line">   &#x2F;   \</span><br><span class="line">  7     2</span><br><span class="line"> &#x2F; \   &#x2F; \</span><br><span class="line">9   6 3   1</span><br></pre></td></tr></table></figure>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;这是一道与树相关的算法，简单来说，我们应该先搞清楚当前root节点该做什么，然后根据函数定义递归调用子节点<br>&emsp;&emsp;对于这道题目来说，我们仅需要将二叉树上的每一个节点左右子节点进行交换，最后的结果就是完全翻转之后的二叉树<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;**</span><br><span class="line"> * Definition for a binary tree node.</span><br><span class="line"> * struct TreeNode &#123;</span><br><span class="line"> *     int val;</span><br><span class="line"> *     TreeNode *left;</span><br><span class="line"> *     TreeNode *right;</span><br><span class="line"> *     TreeNode() : val(0), left(nullptr), right(nullptr) &#123;&#125;</span><br><span class="line"> *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) &#123;&#125;</span><br><span class="line"> *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) &#123;&#125;</span><br><span class="line"> * &#125;;</span><br><span class="line"> *&#x2F;</span><br><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    TreeNode* invertTree(TreeNode* root) &#123;</span><br><span class="line">        if (root &#x3D;&#x3D; nullptr) return nullptr;</span><br><span class="line"></span><br><span class="line">        invertTree(root-&gt;left);</span><br><span class="line">        TreeNode* node &#x3D; root-&gt;left;</span><br><span class="line">        root-&gt;left &#x3D; root-&gt;right;</span><br><span class="line">        root-&gt;right &#x3D; node;</span><br><span class="line">        invertTree(root-&gt;right);</span><br><span class="line"></span><br><span class="line">        return root;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-21</title>
    <url>/2021/03/14/Leetcode-21/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;给定一个二叉树的根节点<code>root</code>,需要将其展开为一个链表。</p>
<ul>
<li>展开后的单链表应该同样使用<code>TreeNode</code>，其中right子指针指向链表中下一个结点，而左子指针始终为<code>null</code></li>
<li>展开后的单链表应该与二叉树<code>先序遍历</code>顺序相同。</li>
</ul>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p>&emsp;&emsp;输入是一个二叉树，输出是由二叉树节点所组成的一个链表</p>
<p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> root = [1,2,5,3,4,null,6] <br>
  <strong>Output:</strong> [1,null,2,null,3,null,4,null,5,null,6] 
</p><br>&emsp;&emsp;就是将左节点置空，以右节点的形式表示整个链表</p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;通过递归,左右子树都拉成一条链表。将左节点赋给右节点并将其置空，处理完左子树后。对于右子树，将原右子树接在新右子树之后。这样对于整个二叉树就处理结束了<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;**</span><br><span class="line"> * Definition for a binary tree node.</span><br><span class="line"> * struct TreeNode &#123;</span><br><span class="line"> *     int val;</span><br><span class="line"> *     TreeNode *left;</span><br><span class="line"> *     TreeNode *right;</span><br><span class="line"> *     TreeNode() : val(0), left(nullptr), right(nullptr) &#123;&#125;</span><br><span class="line"> *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) &#123;&#125;</span><br><span class="line"> *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) &#123;&#125;</span><br><span class="line"> * &#125;;</span><br><span class="line"> *&#x2F;</span><br><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    void flatten(TreeNode* root) &#123;</span><br><span class="line">        if (root &#x3D;&#x3D; nullptr) return;</span><br><span class="line">        TreeNode* left &#x3D; root-&gt;left;</span><br><span class="line">        TreeNode* right &#x3D; root-&gt;right;</span><br><span class="line"></span><br><span class="line">        root-&gt;left &#x3D; nullptr;</span><br><span class="line">        root-&gt;right &#x3D; left;</span><br><span class="line"></span><br><span class="line">        TreeNode* temp &#x3D; root;</span><br><span class="line">        while (temp-&gt;right !&#x3D; nullptr) temp &#x3D; temp-&gt;right;</span><br><span class="line">        temp-&gt;right &#x3D; right;</span><br><span class="line"></span><br><span class="line">        flatten(root-&gt;left);</span><br><span class="line">        flatten(root-&gt;right);        </span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-22</title>
    <url>/2021/03/15/Leetcode-22/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;给定一个<strong>完美二叉树</strong>，其所有叶子节点都在同一层，每个父节点都有两个子节点。二叉树定义如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">struct Node &#123;</span><br><span class="line">  int val;</span><br><span class="line">  Node *left;</span><br><span class="line">  Node *right;</span><br><span class="line">  Node *next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>&emsp;&emsp;填充它的每个next指针，让这个指针指向其下一个左侧节点。如果找不到下一个右侧节点，则将next指针设置为<code>NULL</code>。初始状态下，所有next指针都被设置为<code>NULL</code></p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p><img src="https://assets.leetcode.com/uploads/2019/02/14/116_sample.png" alt="avatar"><br>&emsp;&emsp;给定一个二叉树如图A所示，函数应该填充它的每个next指针,以指向其下一个右侧节点，如图B所示。序列化输出按层序遍历排列，同一层节点由next指针连接，’#’标志着每一层的结束。</p>
<p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> root = [1,2,3,4,5,6,7] <br>
  <strong>Output:</strong> [1,#,2,3,#,4,5,6,7,#]
</p>

<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;首先这题主要问题在于使用指向右子树节点的next。简单的只存在左右子树,即这是一个具有两层的树。我们可以直接使用递归来进行处理，大致代码如下<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    Node* connect(Node* root) &#123;</span><br><span class="line">        if (root &#x3D;&#x3D; nullptr || root-&gt;left &#x3D;&#x3D; nullptr) </span><br><span class="line">            return root;</span><br><span class="line">        root-&gt;left-&gt;next &#x3D; root-&gt;right;</span><br><span class="line"></span><br><span class="line">        connect(root-&gt;left);</span><br><span class="line">        connect(root-&gt;right);</span><br><span class="line"></span><br><span class="line">        return root;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>&emsp;&emsp;但是对于多层的树来说，这样的解法就存在一定的缺陷。对于超过两层的树，会分叉多个子节点。继续使用上面的解法就会导致左子树处理完成，而右子树却没有处理的状况。<br>&emsp;&emsp;我们可以使用函数来处理对于多个节点的问题，代码如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    Node* connect(Node* root) &#123;</span><br><span class="line">        if (root &#x3D;&#x3D; nullptr) return root;</span><br><span class="line">        connectTwoTree(root-&gt;left,root-&gt;right);</span><br><span class="line">        return root;</span><br><span class="line">    &#125;</span><br><span class="line">    void connectTwoTree(Node* node,Node* node1) &#123;</span><br><span class="line">        if (node &#x3D;&#x3D; nullptr || node1 &#x3D;&#x3D; nullptr) return;</span><br><span class="line"></span><br><span class="line">        node-&gt;next &#x3D; node1;</span><br><span class="line"></span><br><span class="line">        connectTwoTree(node-&gt;left,node-&gt;right);</span><br><span class="line">        connectTwoTree(node1-&gt;left,node1-&gt;right);</span><br><span class="line"></span><br><span class="line">        connectTwoTree(node-&gt;right,node1-&gt;left);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-23</title>
    <url>/2021/03/15/Leetcode-23/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;给定一个字符串，请你找出其中不含有重复字符的<strong>最长子串</strong>的长度</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> s = "abcabcbb" <br>
  <strong>Output:</strong> 3
</p><br>&emsp;&emsp;因为无重复，所以最后的b需要被舍去。因此最长子串是”abc”，所以其长度为3</p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;这道题目我们可以使用滑动窗口的做法，从而避免使用暴力解法所导致的$O(n^{2})$的空间复杂度。滑动窗口其实主要就是使用双指针中的左右指针的技法。<br>&emsp;&emsp;初始化<code>left = right = 0 </code>，把索引左闭右开的区间<code>[left,right)</code>称为一个「窗口」。我们先不断增加right指针扩大窗口<code>[left,right]</code>,对应的键值也会增加，直到键值大于1时。此时，我们停止增加<code>right</code>,转而不断增加<code>left</code>指针缩小窗口,从而找到真正的不重复的子串。具体代码如下<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int lengthOfLongestSubstring(string s) &#123;</span><br><span class="line">        unordered_map&lt;char,int&gt; windows;</span><br><span class="line"></span><br><span class="line">        int left &#x3D; 0,right &#x3D; 0;</span><br><span class="line">        int res &#x3D; 0; &#x2F;&#x2F; 用于记录结果</span><br><span class="line"></span><br><span class="line">        while (right &lt; s.size()) &#123;</span><br><span class="line">            &#x2F;&#x2F;C是将要移入窗口的字符</span><br><span class="line">            char c &#x3D; s[right];</span><br><span class="line">            right++;</span><br><span class="line">            &#x2F;&#x2F; 更新数据</span><br><span class="line">            windows[c]++;</span><br><span class="line">            &#x2F;&#x2F; 判断窗口是否需要收缩</span><br><span class="line">            while (windows[c] &gt; 1) &#123;</span><br><span class="line">                char d  &#x3D; s[left];</span><br><span class="line">                left++;</span><br><span class="line">                windows[d]--;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            res &#x3D; max(res,right-left);</span><br><span class="line">        &#125;</span><br><span class="line">        return res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-3</title>
    <url>/2020/09/25/Leetcode-3/</url>
    <content><![CDATA[<h2 id="Question"><a href="#Question" class="headerlink" title="Question"></a>Question</h2><p> Determine whether an integer is a palindrome. An integer is a palindrome when it reads the same backward as forward. </p>
<p> <strong>Example 1</strong> </p>
<blockquote>
<p><strong>Input : </strong> 121<br><strong>Output : </strong> true</p>
</blockquote>
<p> <strong>Example 2</strong></p>
<blockquote>
<p><strong>Input : </strong> -121<br><strong>Output : </strong> false<br><strong>Explanation : </strong> From left to right, it reads -121. From right to left, it becomes 121-. Therefore it is not a palindrome.  </p>
</blockquote>
<p> <strong>Example 3</strong></p>
<blockquote>
<p><strong>Input : </strong> 10<br><strong>Output : </strong> false<br><strong>Explanation : </strong> Reads 01 from right to left. Therefore it is not a palindrome.</p>
</blockquote>
<h2 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h2><h3 id="思路一"><a href="#思路一" class="headerlink" title="思路一"></a>思路一</h3><p>  简单的把数字进行一个逆转，最后判断反转后的数字和原来的数字是否一样。时间复杂度O(n)<br>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    bool isPalindrome(int x) &#123;</span><br><span class="line">        if (x &lt; 0) return false;</span><br><span class="line">        long res &#x3D; 0;</span><br><span class="line">        long s &#x3D; x;</span><br><span class="line">        for (;s !&#x3D; 0 ; s &#x2F;&#x3D;10)  res &#x3D; res * 10 + s %10;</span><br><span class="line">        return (res &#x3D;&#x3D; x) ? true : false;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<h3 id="思路二"><a href="#思路二" class="headerlink" title="思路二"></a>思路二</h3><p>我们可以不需要等数字全逆转结束，只需要逆转一半看是否符合条件就可以了</p>
<ul>
<li><p>如何一半停止</p>
<ul>
<li>如果 x&lt;=r 时就可以停止了</li>
<li>如果是偶数长度，并且是回文，那么 x == r</li>
<li>如果是奇数长度，并且是回文，那么 x &lt; r，这时候r比x多一位数</li>
</ul>
</li>
<li><p>停止后判断是否是回文</p>
<ul>
<li>如果偶数长度，判断 r == x;</li>
<li>如果奇数长度，判断 r/10 == x;</li>
</ul>
<p>时间复杂度为O(n)</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    bool isPalindrome(int x) &#123;</span><br><span class="line">        if (x &lt; 0 || (x !&#x3D; 0 &amp;&amp; x %10 &#x3D;&#x3D; 0)) return false;</span><br><span class="line">        int res &#x3D; 0;</span><br><span class="line">        while (x &gt; res) &#123;</span><br><span class="line">            res &#x3D; res * 10 + x % 10;</span><br><span class="line">            x &#x2F;&#x3D; 10;</span><br><span class="line">        &#125;</span><br><span class="line">        return (res &#x3D;&#x3D; x) || (res&#x2F;10 &#x3D;&#x3D; x);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
</ul>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-4</title>
    <url>/2020/09/28/Leetcode-4/</url>
    <content><![CDATA[<h2 id="Question"><a href="#Question" class="headerlink" title="Question"></a>Question</h2><p>Roman numerals are represented by seven different symbols: <code>I</code>, <code>V</code> ,<code>X</code>,<code>L</code>, <code>C</code>, <code>D</code> and <code>M</code>.</p>
<div class="table-container">
<table>
<thead>
<tr>
<th style="text-align:center"><strong>Symbol</strong></th>
<th style="text-align:center"><strong>Value</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">I</td>
<td style="text-align:center">1</td>
</tr>
<tr>
<td style="text-align:center">V</td>
<td style="text-align:center">5</td>
</tr>
<tr>
<td style="text-align:center">X</td>
<td style="text-align:center">10</td>
</tr>
<tr>
<td style="text-align:center">L</td>
<td style="text-align:center">50</td>
</tr>
<tr>
<td style="text-align:center">C</td>
<td style="text-align:center">100</td>
</tr>
<tr>
<td style="text-align:center">D</td>
<td style="text-align:center">500</td>
</tr>
<tr>
<td style="text-align:center">M</td>
<td style="text-align:center">1000</td>
</tr>
</tbody>
</table>
</div>
<p>For example, two is written as <code>II</code> in Roman numeral, just two one’s added together. Twelve is written as, <code>XII</code>, which is simply <code>X</code>+ <code>II</code>. The number twenty seven is written as <code>XXVII</code>, which is <code>XX</code> + <code>V</code> + <code>II</code>.</p>
<p>Roman numerals are usually written largest to smallest from left to right. However, the numeral for four is not <code>IIII</code>. Instead, the number four is written as <code>IV</code>. Because the one is before the five we subtract it making four. The same principle applies to the number nine, which is written as <code>IX</code>. There are six instances where subtraction is used:</p>
<ul>
<li><code>I</code> can be placed before <code>V</code>(5) and <code>X</code>(10) to make 4 and 9.</li>
<li><code>X</code> can be placed before <code>L</code>(50) and <code>C</code>(100) to make 40 to 90.</li>
<li><code>C</code> can be placed before <code>D</code>(500) and <code>M</code>(1000) to make 400 to 900.</li>
</ul>
<p>Given a roman numeral, convert it to an integer. Input is guaranteed to be within the range from 1 to 3999.</p>
<p><strong>Example 1 : </strong></p>
<blockquote>
<p><strong>Input : </strong> “III”<br><strong>Output : </strong> 3</p>
</blockquote>
<p><strong>Example 2 : </strong></p>
<blockquote>
<p><strong>Input : </strong> “IV”<br><strong>Output : </strong> 4</p>
</blockquote>
<p><strong>Example 3 : </strong> </p>
<blockquote>
<p><strong>Input : </strong> “IX”<br><strong>Output : </strong> 9</p>
</blockquote>
<p><strong>Example 4 : </strong></p>
<blockquote>
<p><strong>Input : </strong> “LVIII”<br><strong>Output : </strong> 58<br><strong>Examplanation : </strong> L = 50, V = 5, III = 3</p>
</blockquote>
<p><strong>Example 5 : </strong></p>
<blockquote>
<p><strong>Input : </strong> “MCMXCIV”<br><strong>Output : </strong> 1994<br><strong>Examplanation : </strong> M = 1000, CM = 900, XC = 90, IV = 4.</p>
</blockquote>
<h2 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h2><h3 id="暴力破解法"><a href="#暴力破解法" class="headerlink" title="暴力破解法"></a>暴力破解法</h3><p> 这个思路就是把每个罗马数字所对应的整数一一对应，最后再根据罗马数字的书写方式用循环套出结果。时间复杂度为O(n),空间复杂度为O(1)<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int fun(char s) &#123;</span><br><span class="line">        if (s &#x3D;&#x3D; &#39;I&#39;) return 1;</span><br><span class="line">        if (s &#x3D;&#x3D; &#39;V&#39;) return 5;</span><br><span class="line">        if (s &#x3D;&#x3D; &#39;X&#39;) return 10;</span><br><span class="line">        if (s &#x3D;&#x3D; &#39;L&#39;) return 50;</span><br><span class="line">        if (s &#x3D;&#x3D; &#39;C&#39;) return 100;</span><br><span class="line">        if (s &#x3D;&#x3D; &#39;D&#39;) return 500;</span><br><span class="line">        if (s &#x3D;&#x3D; &#39;M&#39;) return 1000;</span><br><span class="line">        return 0;</span><br><span class="line">    &#125;</span><br><span class="line">    int romanToInt(string s) &#123;</span><br><span class="line">        int res &#x3D; 0;</span><br><span class="line">        for (int i &#x3D; 0; i &lt; s.size();i++) &#123;</span><br><span class="line">            </span><br><span class="line">            if ((i+1 &lt; s.size()) &amp;&amp; (fun(s[i]) &lt; fun(s[i+1])))</span><br><span class="line">                res -&#x3D; fun(s[i]);</span><br><span class="line">            else </span><br><span class="line">                res +&#x3D; fun(s[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        return res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;      </span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-6</title>
    <url>/2020/10/04/Leetcode-6/</url>
    <content><![CDATA[<h2 id="Question"><a href="#Question" class="headerlink" title="Question"></a>Question</h2><p>Given a string s containing just the characters <code>&#39;(&#39;</code>,<code>&#39;)&#39;</code>,<code>&#39;&#123;&#39;</code>,<code>&#39;&#125;&#39;</code>,<code>&#39;[&#39;</code> and<code>&#39;]&#39;</code>, determine if the input string is valid.<br>An input string is valid if:</p>
<ol>
<li>Open brackets must be closed by the same type of brackets.</li>
<li>Open brackets must be closed in the correct order.</li>
</ol>
<p><strong>Example 1: </strong> </p>
<blockquote>
<p><strong>Input: </strong> s = “()”<br><strong>Output: </strong> true</p>
</blockquote>
<p><strong>Example 2: </strong></p>
<blockquote>
<p><strong>Input: </strong> s = “()[]{}”<br><strong>Output: </strong> true</p>
</blockquote>
<p><strong>Example 3: </strong></p>
<blockquote>
<p><strong>Input: </strong> s = “(]”<br><strong>Output: </strong> false</p>
</blockquote>
<p><strong>Example 4: </strong></p>
<blockquote>
<p><strong>Input: </strong> s = “([)]”<br><strong>Output: </strong> false</p>
</blockquote>
<p><strong>Example 5: </strong></p>
<blockquote>
<p><strong>Input: </strong> s = “{[]}”<br><strong>Output: </strong> true</p>
</blockquote>
<p><strong>Constraints:</strong></p>
<ul>
<li>1 &lt;= s.length() &lt;= $10^{4}$</li>
<li>s consists of parentheses only <code>&#39;()[]&#123;&#125;&#39;</code></li>
</ul>
<h2 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h2><h3 id="思路一"><a href="#思路一" class="headerlink" title="思路一"></a>思路一</h3><p>  <strong>堆栈法 : </strong> 首先把有效括号的左半边先入栈，再来看右半边括号，首先需要判断我们堆栈是否为空，或者是否是匹配的括号。如果是，那就直接输出false。如果不是就出栈，在最后判断一下这个数组是否为空，因为如果全部出栈那么这个stack就是空的。如果不是，那么就不是空，就要输出false。时间复杂度为O(n),具体代码如下：<br>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    bool Pair(char begin,char end) &#123;</span><br><span class="line">        if (begin &#x3D;&#x3D; &#39;(&#39; &amp;&amp; end &#x3D;&#x3D; &#39;)&#39;)</span><br><span class="line">            return true;</span><br><span class="line">        if (begin &#x3D;&#x3D; &#39;[&#39; &amp;&amp; end &#x3D;&#x3D; &#39;]&#39;) </span><br><span class="line">            return true;</span><br><span class="line">        if (begin &#x3D;&#x3D; &#39;&#123;&#39; &amp;&amp; end &#x3D;&#x3D; &#39;&#125;&#39;) </span><br><span class="line">            return true;</span><br><span class="line">        </span><br><span class="line">        return false;</span><br><span class="line">    &#125;</span><br><span class="line">    bool isValid(string s) &#123;</span><br><span class="line">        stack&lt;char&gt; res;</span><br><span class="line">        for (int i &#x3D; 0 ; i &lt; s.size() ; i++) &#123;</span><br><span class="line">            if (s[i] &#x3D;&#x3D; &#39;(&#39; || s[i] &#x3D;&#x3D; &#39;[&#39; || s[i] &#x3D;&#x3D; &#39;&#123;&#39;)</span><br><span class="line">                res.push(s[i]);</span><br><span class="line">            if (s[i] &#x3D;&#x3D; &#39;)&#39; || s[i] &#x3D;&#x3D; &#39;]&#39; || s[i] &#x3D;&#x3D; &#39;&#125;&#39;) &#123;</span><br><span class="line">                if (res.empty() || !Pair(res.top(),s[i]))</span><br><span class="line">                    return false;</span><br><span class="line">                else </span><br><span class="line">                    res.pop();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return res.empty();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>  <strong>简化版堆栈法 : </strong> 不需要用函数来判断括号是否匹配，直接在switch语句中判断好，时间复杂度为O(n),不过switch相对于if-else所需的空间更多，具体代码如下：<br>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">  class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    bool isValid(string s) &#123;</span><br><span class="line">       stack&lt;char&gt; res;</span><br><span class="line">        for (char&amp; c : s) &#123;</span><br><span class="line">            switch (c) &#123;</span><br><span class="line">                case &#39;(&#39;: </span><br><span class="line">                case &#39;[&#39;: </span><br><span class="line">                case &#39;&#123;&#39;: res.push(c);break;</span><br><span class="line">                case &#39;)&#39;: if (res.empty() || res.top() !&#x3D; &#39;(&#39;) return false; else res.pop();break;</span><br><span class="line">                case &#39;]&#39;: if (res.empty() || res.top() !&#x3D; &#39;[&#39;) return false; else res.pop();break;</span><br><span class="line">                case &#39;&#125;&#39;: if (res.empty() || res.top() !&#x3D; &#39;&#123;&#39;) return false; else res.pop();break;</span><br><span class="line">                default: ;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return res.empty();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<h3 id="思路二"><a href="#思路二" class="headerlink" title="思路二"></a>思路二</h3><p>  <strong>AscII值相减 : </strong> 时间复杂度为O(n),空间复杂度为O(1),具体代码如下:<br>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    bool isValid(string s) &#123;</span><br><span class="line">        int n&#x3D; 0;</span><br><span class="line">        for (char a : s) &#123;</span><br><span class="line">             if (n &amp;&amp; ((a - s[n-1]+1)&#x2F;2 &#x3D;&#x3D; 1))</span><br><span class="line">                n--;</span><br><span class="line">            else </span><br><span class="line">                s[n++] &#x3D; a;</span><br><span class="line">        &#125;</span><br><span class="line">        return !n;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-5</title>
    <url>/2020/10/03/Leetcode-5/</url>
    <content><![CDATA[<h2 id="Question"><a href="#Question" class="headerlink" title="Question"></a>Question</h2><p>Write a function to find the longest common prefix string amongst an array of strings.  </p>
<p>If there is no common prefix, return an empty string “”.</p>
<p> <strong>Example 1:</strong></p>
<blockquote>
<p><strong>Input : </strong> [“flower”,”flow”,”flight”]<br><strong>Output : </strong> “fl”</p>
</blockquote>
<p> <strong>Example 2: </strong></p>
<blockquote>
<p><strong>Input : </strong> [“dog”,”racecar”,”car”]<br><strong>Output : </strong> “”<br><strong>Explanation : </strong> There is no common prefix among the input strings.</p>
</blockquote>
<p>  <strong>Note : </strong><br>  All given inputs are in lowercase letters a-z.</p>
<h2 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h2><h3 id="思路一"><a href="#思路一" class="headerlink" title="思路一"></a>思路一</h3><p>  <strong>暴力破解法:</strong> 首先通过循环把这个字符串中最长的一个字符设置为第一个字符串，再使用循环将这第一个字符串去和所有的字符比较，不对就输出””，对了就有ans存储并输出.时间复杂度为O($n^{2})$,空间复杂度为O(n).整体代码如下<br>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    string longestCommonPrefix(vector&lt;string&gt;&amp; strs) &#123;</span><br><span class="line">        string ans &#x3D; &quot;&quot;;</span><br><span class="line">        int n &#x3D; strs.size();</span><br><span class="line">        if (n &#x3D;&#x3D; 0) return &quot;&quot;;</span><br><span class="line">        </span><br><span class="line">        int l &#x3D; strs[0].size();</span><br><span class="line">        for (string a : strs) &#123;</span><br><span class="line">            if (l &lt; strs.size()) &#123;</span><br><span class="line">                l &#x3D; strs.size();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        int index &#x3D; 0;</span><br><span class="line">        </span><br><span class="line">        for (char s : strs[0]) &#123;</span><br><span class="line">            for (int i &#x3D; 0 ;i &lt; n ;i++) &#123;</span><br><span class="line">                if (strs[i][index] !&#x3D; s || index &gt; l)</span><br><span class="line">                    return ans;</span><br><span class="line">               &#125;</span><br><span class="line">            ans +&#x3D; s;</span><br><span class="line">            index++;</span><br><span class="line">        &#125;</span><br><span class="line">        return ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<h3 id="思路二"><a href="#思路二" class="headerlink" title="思路二"></a>思路二</h3><p> <strong>排序法 : </strong> 首先把这个字符串用sort函数从小到达排序一遍，然后选择第一个和最后一个,比较他们是否存在公共字符就可以了(选择第一个和最后一个，是因为他们的区别是最大的，可以更加快速的得到结果),最后我们只需要比较有多少公共字符就行了. 时间复杂度为O(n)，空间复杂度为O(n).具体代码如下:<br> <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    string longestCommonPrefix(vector&lt;string&gt;&amp; strs) &#123;</span><br><span class="line">        string ans &#x3D; &quot;&quot;;</span><br><span class="line">        int n &#x3D; strs.size();</span><br><span class="line">        if (n &#x3D;&#x3D; 0) return ans;</span><br><span class="line">        </span><br><span class="line">        sort(strs.begin(),strs.end());</span><br><span class="line">        string a &#x3D; strs[0];</span><br><span class="line">        string b &#x3D; strs[n-1];</span><br><span class="line">        </span><br><span class="line">        for (int i &#x3D; 0 ; i &lt; a.size() ; i++) &#123;</span><br><span class="line">            if (a[i] &#x3D;&#x3D; b[i]) &#123;</span><br><span class="line">                ans +&#x3D; a[i];</span><br><span class="line">            &#125;</span><br><span class="line">            else</span><br><span class="line">                break;</span><br><span class="line">        &#125;</span><br><span class="line">        return ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-7</title>
    <url>/2020/10/04/Leetcode-7/</url>
    <content><![CDATA[<h2 id="Question"><a href="#Question" class="headerlink" title="Question"></a>Question</h2><p>Merge two sorted linked lists and return it as a new sorted list. The new list should be made by splicing together the nodes of the first two lists.</p>
<p><strong>Example 1 : </strong><br>  <img src="https://assets.leetcode.com/uploads/2020/10/03/merge_ex1.jpg" alt="avatar"></p>
<blockquote>
<p><strong>Input : </strong> l1 = [1,2,4],l2 = [1,3,4]<br><strong>Output : </strong> [1,1,2,3,4,4]</p>
</blockquote>
<p><strong>Example 2 : </strong></p>
<blockquote>
<p><strong>Input : </strong> l1 = [], l2 = []<br><strong>Output : </strong> []</p>
</blockquote>
<p><strong>Example 3 : </strong> </p>
<blockquote>
<p><strong>Input : </strong> l1 = [], l2 = [0]<br><strong>Output : </strong> [0]</p>
</blockquote>
<p><strong>Constraints : </strong></p>
<ul>
<li>The number of nodes in both lists is in the range <code>[0, 50]</code>.</li>
<li><code>-100 &lt;= Node.val &lt;= 100</code></li>
<li>Both <code>l1</code> and <code>l2</code> are sorted in <strong>non-decreasing</strong> order.</li>
</ul>
<h2 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h2><h3 id="思路一"><a href="#思路一" class="headerlink" title="思路一"></a>思路一</h3><p>  <strong>递归法 : </strong> 首先判断两个链表中的一个是否为空，如果有一个为空，那么就以这个链表为输出结果。为了保存其他两个链表，我就需要新建一个链表。因为要顺序保存，所有通过值大小的比较存入t，作t的头节点。之后t-&gt;next用递归的方式依次存入。时间复杂度为O(n),具体代码如下:<br>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;**</span><br><span class="line"> * Definition for singly-linked list.</span><br><span class="line"> * struct ListNode &#123;</span><br><span class="line"> *     int val;</span><br><span class="line"> *     ListNode *next;</span><br><span class="line"> *     ListNode() : val(0), next(nullptr) &#123;&#125;</span><br><span class="line"> *     ListNode(int x) : val(x), next(nullptr) &#123;&#125;</span><br><span class="line"> *     ListNode(int x, ListNode *next) : val(x), next(next) &#123;&#125;</span><br><span class="line"> * &#125;;</span><br><span class="line"> *&#x2F;</span><br><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) &#123;</span><br><span class="line">        if (!l1) return l2;</span><br><span class="line">        if (!l2) return l1;</span><br><span class="line">        </span><br><span class="line">        ListNode* t;</span><br><span class="line">        if (l1-&gt;val &lt; l2-&gt;val) &#123;</span><br><span class="line">            t &#x3D; l1;</span><br><span class="line">            l1 &#x3D; l1-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        else &#123;</span><br><span class="line">            t &#x3D; l2;</span><br><span class="line">            l2 &#x3D; l2-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        t-&gt;next &#x3D; mergeTwoLists(l1,l2);</span><br><span class="line">        return t;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<h3 id="思路二"><a href="#思路二" class="headerlink" title="思路二"></a>思路二</h3><p>  <strong>迭代法 : </strong> 首先设置一个前哨节点，为了之后输出方便。程序主体:通过值的比较，把每个链表的值都挨个存入cur链表中。while判断的问题，一定还有最有一个没有存入到链表中，所以我们还需要单独的把最后一位加入到链表中来。最后输出前哨节点的下一个也就是dummy-&gt;next。时间复杂度为O(n),具体代码如下 :<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;**</span><br><span class="line"> * Definition for singly-linked list.</span><br><span class="line"> * struct ListNode &#123;</span><br><span class="line"> *     int val;</span><br><span class="line"> *     ListNode *next;</span><br><span class="line"> *     ListNode() : val(0), next(nullptr) &#123;&#125;</span><br><span class="line"> *     ListNode(int x) : val(x), next(nullptr) &#123;&#125;</span><br><span class="line"> *     ListNode(int x, ListNode *next) : val(x), next(next) &#123;&#125;</span><br><span class="line"> * &#125;;</span><br><span class="line"> *&#x2F;</span><br><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) &#123;</span><br><span class="line">        ListNode* dummy &#x3D; new ListNode(-1);</span><br><span class="line">        ListNode* cur &#x3D; dummy;</span><br><span class="line">        </span><br><span class="line">        while (l1 &amp;&amp; l2) &#123;</span><br><span class="line">            if (l1-&gt;val &lt; l2-&gt;val) &#123;</span><br><span class="line">                cur-&gt;next &#x3D; l1;</span><br><span class="line">                l1 &#x3D; l1-&gt;next;</span><br><span class="line">            &#125;</span><br><span class="line">            else &#123;</span><br><span class="line">                cur-&gt;next &#x3D; l2;</span><br><span class="line">                l2 &#x3D; l2-&gt;next;</span><br><span class="line">            &#125;</span><br><span class="line">            cur &#x3D; cur-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        cur-&gt;next &#x3D; l1 &#x3D;&#x3D; NULL ? l2 : l1;</span><br><span class="line">        return dummy-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-8</title>
    <url>/2020/10/11/Leetcode-8/</url>
    <content><![CDATA[<h1 id="Question"><a href="#Question" class="headerlink" title="Question"></a>Question</h1><p>  Given a sorted array nums, remove the duplicates in-place such that each element appears only once and returns the new length.<br>  Do not allocate extra space for another array, you must do this by modifying the input array in-place with O(1) extra memory.</p>
<p>  <strong>Example 1: </strong></p>
<blockquote>
<p>Given nums = [1,1,2],<br>Your function should return length = 2, with the first two elements of nums being 1 and 2 respectively.<br>It doesn’t matter what you leave beyond the returned length.</p>
</blockquote>
<p>  <strong>Example 2: </strong></p>
<blockquote>
<p>Given nums = [0,0,1,1,1,2,2,3,3,4],<br>Your function should return length = 5, with the first five elements of nums being modified to 0, 1, 2, 3, and 4 respectively.<br>It doesn’t matter what values are set beyond the returned length.</p>
</blockquote>
<p>  <strong>Clarification: </strong><br>  Confused why the returned value is an integer but your answer is an array?<br>  Note that the input array is passed in by reference, which means a modification to the input array will be known to the caller as well.<br>  Internally you can think of this:<br>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; nums is passed in by reference. (i.e., without making a copy)</span><br><span class="line">int len &#x3D; removeDuplicates(nums);</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; any modification to nums in your function would be known by the caller.</span><br><span class="line">&#x2F;&#x2F; using the length returned by your function, it prints the first len elements.</span><br><span class="line">for (int i &#x3D; 0; i &lt; len; i++) &#123;</span><br><span class="line">    print(nums[i]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h1 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h1><p>  <strong>双指针法: </strong> 首先这是有序的数组，那么如果出现重复，就一定是重复数字的下一个(只会重复一次)。那么我就只需要比较当前位和下一位是否产生重复，如果产生了重复。那么我就用下一位来覆盖当前位，最后返回的数组长度。时间复杂度为O(n),空间复杂度为O(1),具体代码如下:<br>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int removeDuplicates(vector&lt;int&gt;&amp; nums) &#123;</span><br><span class="line">        if (nums.size() &#x3D;&#x3D; 0) return 0;</span><br><span class="line">        int i &#x3D;0 ,j &#x3D; 1;</span><br><span class="line">        while (j &lt; nums.size()) &#123;</span><br><span class="line">            if (nums[i] !&#x3D; nums[j])</span><br><span class="line">                nums[++i] &#x3D; nums[j];</span><br><span class="line">            j++;</span><br><span class="line">        &#125;</span><br><span class="line">        return i+1;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-9</title>
    <url>/2020/10/11/Leetcode-9/</url>
    <content><![CDATA[<h1 id="Question"><a href="#Question" class="headerlink" title="Question"></a>Question</h1><p> &emsp;&emsp;Given an array nums and a value val, remove all instances of that value in-place and return the new length.<br> &emsp;&emsp;Do not allocate extra space for another array, you must do this by <font color = red>modifying the input array in-place</font> with <code>O(1)</code> extra memory.</p>
<p><strong>Clarification:</strong><br> &emsp;&emsp;Confused why the returned value is an integer but your answer is an array?<br> &emsp;&emsp;Note that the input array is passed in by <strong>reference</strong>, which means a modification to the input array will be known to the caller as well.  </p>
<p>  <strong>Internally you can think of this:</strong><br> <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"> &#x2F;&#x2F; nums is passed in by reference. (i.e., without making a copy)</span><br><span class="line">int len &#x3D; removeElement(nums, val);</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; any modification to nums in your function would be known by the caller.</span><br><span class="line">&#x2F;&#x2F; using the length returned by your function, it prints the first len elements.</span><br><span class="line">for (int i &#x3D; 0; i &lt; len; i++) &#123;</span><br><span class="line">    print(nums[i]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p><strong>Example 1: </strong></p>
<blockquote>
<p><strong>Input:</strong> nums = [3,2,2,3], val = 3<br><strong>Output:</strong> 2, nums = [2,2]<br><strong>Explanation:</strong> Your function should return length = <strong>2</strong>, with the first two elements of nums being <strong>2</strong><br>It doesn’t matter what you leave beyond the returned length. For example if you return 2 with nums = [2,2,3,3] or nums = [2,3,0,0], your answer will be accepted.</p>
</blockquote>
<p><strong>Example 2: </strong></p>
<blockquote>
<p><strong>Input:</strong> nums = [0,1,2,2,3,0,4,2], val = 2<br><strong>Output:</strong>  5, nums = [0,1,4,0,3]<br><strong>Explanation:</strong> Your function should return length = <strong>5</strong> ,with the first five elements of nums containing <strong>0, 1, 3, 0</strong> and <strong>4</strong>.Note that the order of those five elements can be arbitrary. It doesn’t matter what values are set beyond the returned length.</p>
</blockquote>
<p><strong>Constrains:</strong></p>
<ul>
<li><code>0 &lt;= nums.length() &lt;= 100</code></li>
<li><code>0 &lt;= nums[i] &lt;= 50</code></li>
<li><code>0 &lt;= val &lt;= 100</code></li>
</ul>
<h1 id="Answer"><a href="#Answer" class="headerlink" title="Answer"></a>Answer</h1><h2 id="思路一"><a href="#思路一" class="headerlink" title="思路一"></a>思路一</h2><p>&emsp;&emsp;<strong>堆栈法:</strong> 首先将nums中不等于val的值全部入栈，再将nums清空。再将没有val值的res的值重新的一一赋给nums，同时将res中的值一一弹出，最后直接返回nums数组的size即可。时间复杂度为O(n),空间复杂度为O(1).具体代码如下:<br>     <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">     class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int removeElement(vector&lt;int&gt;&amp; nums, int val) &#123;</span><br><span class="line">        int n &#x3D; nums.size();</span><br><span class="line">        stack&lt;int&gt; res;</span><br><span class="line">        </span><br><span class="line">        for (int i &#x3D; 0 ; i &lt; n ;i++) &#123;</span><br><span class="line">            if (nums[i] !&#x3D; val) </span><br><span class="line">                res.push(nums[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        nums.clear();</span><br><span class="line">        </span><br><span class="line">        for (int i &#x3D; 0 ; i &lt; n ; i++) &#123;</span><br><span class="line">            if (!res.empty())</span><br><span class="line">                nums.push_back(res.top()),res.pop();</span><br><span class="line">        &#125;</span><br><span class="line">        return nums.size();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<h2 id="思路二"><a href="#思路二" class="headerlink" title="思路二"></a>思路二</h2><p>&emsp;&emsp;<strong>双指针法:</strong> 首先快慢指针置0，进入循环。如果nums数组对应的值不等于val,那么就把当前nums[fast]的值赋给nums[slow++]。如果相等那么fast就自增，最后结果返回slow的值。时间复杂度位O(n),空间复杂度O(1),具体代码如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int removeElement(vector&lt;int&gt;&amp; nums, int val) &#123;</span><br><span class="line">        int slow &#x3D; 0,size &#x3D; nums.size(),fast &#x3D; 0;</span><br><span class="line">        </span><br><span class="line">        while (fast &lt; size) &#123;</span><br><span class="line">            if (nums[fast] !&#x3D; val) &#123;</span><br><span class="line">                nums[slow++] &#x3D; nums[fast];</span><br><span class="line">            &#125;</span><br><span class="line">            fast++;</span><br><span class="line">        &#125;</span><br><span class="line">        return slow;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Operation_System_experiment_1</title>
    <url>/2020/10/06/Operation-System-experiment-1/</url>
    <content><![CDATA[<h2 id="实验内容"><a href="#实验内容" class="headerlink" title="实验内容"></a>实验内容</h2><h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h3><p>&emsp;&emsp;设置进程PCB结构和进程队列(就绪、执行、阻塞)</p>
<h3 id="循环执行菜单"><a href="#循环执行菜单" class="headerlink" title="循环执行菜单"></a>循环执行菜单</h3><ol>
<li><p>创建原语<br>&emsp;&emsp;输入进程的名字、优先级(用数字0-9表示，0级最高，9级最低).建议一个节点，插入就绪队列</p>
</li>
<li><p>撤销原语<br>&emsp;&emsp;将执行态进程删除</p>
</li>
<li><p>阻塞原语<br>&emsp;&emsp;将执行态的进程插入到阻塞队列</p>
</li>
<li><p>唤醒原理<br>&emsp;&emsp;输入唤醒进程的名字，插入就绪队列</p>
</li>
<li><p>处理器调度<br>&emsp;&emsp;选择一个优先级最高的就绪态进程，插入执行队列</p>
</li>
<li><p>结束</p>
</li>
</ol>
<h2 id="实验代码"><a href="#实验代码" class="headerlink" title="实验代码"></a>实验代码</h2><h3 id="算法介绍"><a href="#算法介绍" class="headerlink" title="算法介绍"></a>算法介绍</h3><p>  &emsp;&emsp;优先算法:进程就绪队列是按<strong>优先数大小从高到底排序，队首进程首先投入运行。每过一个时间片，运行进程所需的时间片减1，优先数减3</strong>，说明他已经运行了一个时间片，如果在一个时间内完成不了，应该降低其优先数。接着比较现行进程和就绪队列队首进程的优先数，如果仍是现行进程优先数高或者相同，就让现行进程继续运行，否则，调度就绪队列队首进程投入运行。原运行进程再按优先数大小插入就绪队列，且改变它们对应进程的状态，直至所有进程都运行完各自的时间片数。</p>
<h3 id="程序简介"><a href="#程序简介" class="headerlink" title="程序简介"></a>程序简介</h3><p>  &emsp;&emsp;每一个进程控制块(PCB)标志(进程标识号、优先数、占用CPU时间数、进程所需时间片数、进程状态)。每一个进程都会处于运行(run)、就绪(ready)、阻塞(blocked)和完成(finish)状态之一。程序分为初始化(输入数据)、算法模拟、输出三大模块，核心就是处理就绪队列(readyQueue)、运行队列(runQueue)、阻塞队列(blockQueue)、完成队列(finishQueue)四个队列的相互转化。具体代码如下:</p>
<pre><code>
#include &lt;iostream&gt;
#include &lt;cstdio&gt;
#include &lt;cstdlib&gt;
#include &lt;iomanip&gt;
#include &lt;ctime&gt;
#include &lt;cstring&gt;
#include &lt;algorithm&gt;
#include &lt;queue&gt;
#define IsRun 1 // 运行
#define isFinish 0 // 完成
#define isReady 2 // 就绪
#define isBlock -1 // 阻塞
using namespace std;
const int processNum = 110; // 进程数量110以内

int randomBlockTime[processNum]; // 随机等待时间
bool visBlock[processNum]; // 记录当前是否为阻塞状态

struct PCB // 进程控制块
&#123;
    int processId; // 进程标识号
    int priority; // 优先级
    int cpuTime; // 进程占用CPU时间
    int needTime; // 进程执行所需时间
    int blockTime; // 阻塞时间
    int processState; // 进程状态

    PCB() &#123;&#125;

    // 自定义构造函数
    PCB(int pi, int p, int ct, int nt, int ps): processId(pi), priority(p), cpuTime(ct), needTime(nt), processState(ps) &#123;&#125;

    // 自定义构造函数
    PCB(bool isRand, int id)
    &#123;
        if (isRand) // 随机
        &#123;
            this-&gt;cpuTime = 0;
            this-&gt;blockTime = 0;
            this-&gt;needTime = rand() % 10 + 1;
            this-&gt;priority = rand() % 30 + 1;
            this-&gt;processState = isReady;
            this-&gt;processId = id;
        &#125;
        else // 手动输入
        &#123;
            cout &lt;&lt; &quot;Process ID: &quot;;
            cin &gt;&gt; this-&gt;processId;
            cout &lt;&lt; &quot;Need Time: &quot;;
            cin &gt;&gt; this-&gt;needTime;
            this-&gt;cpuTime = 0;
            this-&gt;blockTime = 0;
            this-&gt;priority = rand() % 30 + 1;
            this-&gt;processState = isReady;
        &#125;
    &#125;

    // 重载&lt;，按优先数从大到小输出
    bool operator &lt; (const PCB &amp; r) const
    &#123;
        return priority &lt; r.priority;
    &#125;
&#125;pcb[processNum];

string statue_to_str(int statue) //  将进程状态转化为字符
&#123;
    string str;
    if (statue == 1) // 对应状态
        str = &quot;run   &quot;;
    else if (statue == 0)
        str = &quot;finish&quot;;
    else if (statue == 2)
        str = &quot;ready &quot;;
    else if (statue == -1)
        str = &quot;block &quot;;
    return str;
&#125;

priority_queue &lt;PCB&gt; runQueue, readyQueue, blockQueue, finishQueue; // 运行队列， 就绪队列， 阻塞队列， 完成队列

void init(int processCnt, bool isRandom) // 初始化进程, 默认为随机
&#123;
    srand((unsigned)time(NULL)); // 设置随机数种子

    memset(randomBlockTime, 0, sizeof(randomBlockTime));
    memset(visBlock, false, sizeof(visBlock)); // 默认为flase

    while (!runQueue.empty()) // 清空队列
        runQueue.pop();
    while (!readyQueue.empty())
        readyQueue.pop();
    while (!blockQueue.empty())
        blockQueue.pop();
    while (!finishQueue.empty())
        finishQueue.pop();

    for (int i = 1; i &lt;= processCnt; ++i) // 生成数据
        pcb[i] = PCB(isRandom, i);

    for (int i = 1; i &lt;= processCnt; ++i) // 初始程序加入就绪队列
        readyQueue.push(pcb[i]);

&#125;

void display() // 输出
&#123;
    priority_queue &lt;PCB&gt; _readyQueue = readyQueue;
    priority_queue &lt;PCB&gt; _runQueue = runQueue;
    priority_queue &lt;PCB&gt; _blockQueue = blockQueue;
    priority_queue &lt;PCB&gt; _finishQueue = finishQueue;

    cout &lt;&lt; &quot;ProcessId    |    &quot; &lt;&lt; &quot;Priority    |    &quot; &lt;&lt; &quot;ProcessState    |    &quot; &lt;&lt; &quot;CPUTime    |    &quot; &lt;&lt; &quot;NeedTime    |    &quot; &lt;&lt; &quot;BlockTime&quot; &lt;&lt;  endl;

    while (!_runQueue.empty()) // 运行队列
    &#123;
        PCB now = _runQueue.top();
        cout &lt;&lt; setw(5) &lt;&lt; now.processId  &lt;&lt; setw(16) &lt;&lt; now.priority &lt;&lt; setw(20) &lt;&lt; statue_to_str(now.processState) &lt;&lt; setw(18) &lt;&lt; now.cpuTime &lt;&lt; setw(17) &lt;&lt; now.needTime &lt;&lt; setw(17) &lt;&lt; now.blockTime &lt;&lt; endl;
        _runQueue.pop();
    &#125;

    while (!_blockQueue.empty()) // 阻塞队列
    &#123;
        PCB now = _blockQueue.top();
        cout &lt;&lt; setw(5) &lt;&lt; now.processId  &lt;&lt; setw(16) &lt;&lt; now.priority &lt;&lt; setw(20) &lt;&lt; statue_to_str(now.processState) &lt;&lt; setw(18) &lt;&lt; now.cpuTime &lt;&lt; setw(17) &lt;&lt; now.needTime &lt;&lt; setw(17) &lt;&lt; now.blockTime &lt;&lt; endl;
        _blockQueue.pop();
    &#125;

    while (!_readyQueue.empty()) // 就绪队列
    &#123;
        PCB now = _readyQueue.top();
        cout &lt;&lt; setw(5) &lt;&lt; now.processId  &lt;&lt; setw(16) &lt;&lt; now.priority &lt;&lt; setw(20) &lt;&lt; statue_to_str(now.processState) &lt;&lt; setw(18) &lt;&lt; now.cpuTime &lt;&lt; setw(17) &lt;&lt; now.needTime &lt;&lt; setw(17) &lt;&lt; now.blockTime &lt;&lt; endl;
        _readyQueue.pop();
    &#125;

    while (!_finishQueue.empty()) // 完成队列
    &#123;
        PCB now = _finishQueue.top();
        cout &lt;&lt; setw(5) &lt;&lt; now.processId  &lt;&lt; setw(16) &lt;&lt; now.priority &lt;&lt; setw(20) &lt;&lt; statue_to_str(now.processState) &lt;&lt; setw(18) &lt;&lt; now.cpuTime &lt;&lt; setw(17) &lt;&lt; now.needTime &lt;&lt; setw(17) &lt;&lt; now.blockTime &lt;&lt; endl;
        _finishQueue.pop();
    &#125;

    cout &lt;&lt; &quot;---------------------------------------------------------------------------------------------------&quot; &lt;&lt; endl;
&#125;

void Priority(int processCnt, bool isRandom) // 优先数法
&#123;
    init(processCnt, isRandom);

    cout &lt;&lt; &quot;-------------------------------------------Initial State-------------------------------------------&quot; &lt;&lt; endl;
    display(); // 初始等待状态

    cout &lt;&lt; endl;

    int totalTime = 0; // 程序运行总时间

    cout &lt;&lt; &quot;-------------------------------------------Running State-------------------------------------------&quot; &lt;&lt; endl;
    priority_queue &lt;PCB&gt; _tmpblockQueue; // 使用_tmpblockQueue临时储存就绪队列进程信息
    while (!readyQueue.empty() || !blockQueue.empty()) // 程序执行
    &#123;
        totalTime++;

        while (!_tmpblockQueue.empty())
            _tmpblockQueue.pop();

        while (!blockQueue.empty()) // 依次判断阻塞队列中的所有进程
        &#123;
            PCB block = blockQueue.top();
            blockQueue.pop();
            if (block.blockTime &gt; randomBlockTime[block.processId]) // 达到阻塞等待时间，退出
            &#123;
                block.processState = isReady;
                block.blockTime = 0;
                visBlock[block.processId] = false; // 退出阻塞状态
                readyQueue.push(block);
            &#125;
            else // 阻塞时间更新
            &#123;
                block.blockTime++;
                _tmpblockQueue.push(block);
            &#125;
        &#125;

        while (!_tmpblockQueue.empty()) // “恢复信息”
        &#123;
            blockQueue.push(_tmpblockQueue.top());
            _tmpblockQueue.pop();
        &#125;

        if (readyQueue.empty()) // 当前就绪队列已经为空，则单独处理阻塞队列
        &#123;
            display();
            continue;
        &#125;

        PCB now = readyQueue.top();
        readyQueue.pop();

        if (rand() % 2 == 0 &amp;&amp; !visBlock[now.processId]) // 当前程序未处于阻塞状态，随机进入
        &#123;
            now.processState = isBlock;
            visBlock[now.processId] = true; // 标记处于阻塞状态
            now.blockTime++; // 阻塞时间
            randomBlockTime[now.processId] = rand() % 2 + 1;
            blockQueue.push(now); // 进入阻塞队列
            display();
            continue;
        &#125;

        now.processState = IsRun;
        now.priority -= 3; // 优先数减3
        now.needTime -= 1; // 需要时间减1
        now.cpuTime += 1; // 运行时间加1
        runQueue.push(now);

        display();

        if (now.needTime &gt; 0) // 执行所需时间大于0，进入就绪队列
        &#123;
            now.processState = isReady;
            readyQueue.push(now);
        &#125;
        else //  程序执行完成
        &#123;
            now.processState = isFinish;
            finishQueue.push(now);
        &#125;

        runQueue.pop();
    &#125;

    cout &lt;&lt; endl;

    cout &lt;&lt; &quot;-------------------------------------------Finished State------------------------------------------&quot; &lt;&lt; endl;
    display(); // 完成状态

    cout &lt;&lt; &quot;The total running time of all programs is: &quot; &lt;&lt; totalTime &lt;&lt;  &quot;.&quot; &lt;&lt; endl;
&#125;

int main()
&#123;
    char ch;
    cout &lt;&lt; &quot;Please choose whether to randomly generate data(y for yes, n for no): &quot;; // 是否创建随机进程
    cin &gt;&gt; ch;
    int processCnt;
    bool isRandom;
    if (ch == &#39;y&#39;) // 随机
    &#123;
        cout &lt;&lt; &quot;Please enter the number of random processes(1~100): &quot;;
        cin &gt;&gt; processCnt;
        isRandom = true;
        //freopen(&quot;out.txt&quot;,&quot;w&quot;,stdout); // 输出到文件
        Priority(processCnt, isRandom);
    &#125;
    else if (ch == &#39;n&#39;) // 非随机
    &#123;
        cout &lt;&lt; &quot;Please enter the number of threads you need to create(1~100): &quot;;
        cin &gt;&gt; processCnt;
        isRandom = false;
        cout &lt;&lt; &quot;Please enter the process ID and the corresponding running time.&quot; &lt;&lt; endl;
        //freopen(&quot;out.txt&quot;,&quot;w&quot;,stdout); // 输出到文件
        Priority(processCnt, isRandom);
    &#125;
    else
        cout &lt;&lt; &quot;Input errors, please input again.&quot; &lt;&lt; endl;
    return 0;
&#125;
</code></pre>]]></content>
      <categories>
        <category>lab</category>
      </categories>
      <tags>
        <tag>OS</tag>
      </tags>
  </entry>
  <entry>
    <title>Operation_System_experiment_2</title>
    <url>/2020/10/07/Operation-System-experiment-2/</url>
    <content><![CDATA[<h2 id="实验内容"><a href="#实验内容" class="headerlink" title="实验内容"></a>实验内容</h2><h3 id="实验目标"><a href="#实验目标" class="headerlink" title="实验目标"></a>实验目标</h3><p> &emsp;&emsp;进程在执行过程中提出资源请求，使用银行家算法决定是否进行分配</p>
<h3 id="实验要求"><a href="#实验要求" class="headerlink" title="实验要求"></a>实验要求</h3><ol>
<li>设置资源总数</li>
<li>新建进程(进程名称、最大需求数)</li>
<li>提出资源请求 (进程名称、需要资源数)</li>
<li>资源分配情况显示</li>
<li>结束</li>
</ol>
<h2 id="实验代码"><a href="#实验代码" class="headerlink" title="实验代码"></a>实验代码</h2><h3 id="银行家算法介绍"><a href="#银行家算法介绍" class="headerlink" title="银行家算法介绍"></a>银行家算法介绍</h3><p>  &emsp;&emsp;银行家算法 : 实质就是<strong>保证系统动态分配资源后不进入不安全状态，以避免可能产生的死锁。</strong> 即没当进程提出进程请求且系统资源能够满足请求时,系统将判断此次资源请求后系统是否安全。如果判断是安全，则分给该进程资源，否则不分配资源，申请资源的进程将被阻塞。<br>  &emsp;&emsp;银行家算法执行有个前提，即要求进程预先提出自己的最大资源请求，并假设系统拥有固定的资源总量。</p>
<h3 id="银行家算法的数据结构"><a href="#银行家算法的数据结构" class="headerlink" title="银行家算法的数据结构"></a>银行家算法的数据结构</h3><pre><code>1. 可用资源向量available,它记录系统中各类资源的当前可利用数目
2. 最大需求矩阵 max ,它记录每个进程对各类资源的最大需求量。
3. 分配矩阵 allocation ,它记录每个进程对各类资源当前的占有量。
4. 需求矩阵   need , 它记录每个进程对各类资源需要的数目(即等于最大需求矩阵与分配矩阵的差)。
5. 请求向量 request ,它记录某个进程当前对各类资源的申请量，是银行家算法的入口参数。
</code></pre><h3 id="算法描述"><a href="#算法描述" class="headerlink" title="算法描述"></a>算法描述</h3><p>  &emsp;&emsp;设进程P<sub>i</sub> 向系统提出request<sub>i</sub>资源请求 :</p>
<ol>
<li>若 request<sub>i</sub> &gt; need<sub>i</sub>,则进程P<sub>i</sub>出错</li>
<li>若 request<sub>i</sub> &lt; available,则进程P<sub>i</sub>阻塞</li>
<li>系统试着把资源分配给进程P<sub>i</sub>,并对相应数据结构作如下修改 :<br>available - request<sub>i</sub> ; allocation<sub>i</sub> + request<sub>i</sub> ; need<sub>i</sub> - request<sub>i</sub>;</li>
<li>系统执行安全性检测子算法，以判断试分配后系统状态是否安全</li>
<li>若第4步返回真值，即安全，则完成本次分配，返回</li>
<li>否则，撤销此次(即第三步中的试分配) ,进程P<sub>i</sub>阻塞</li>
</ol>
<h3 id="算法的组成"><a href="#算法的组成" class="headerlink" title="算法的组成"></a>算法的组成</h3><p>  银行家算法主要是由 <code>Safety algorithm</code> 和 <code>Resource request algorithm</code> 组成</p>
<h4 id="Safety-algorithm"><a href="#Safety-algorithm" class="headerlink" title="Safety algorithm"></a>Safety algorithm</h4><p>  判断系统是否处于安全状态的算法可以描述如下 : </p>
<ol>
<li>Let Work And Finish be vectors of lengh ‘m’ and ‘n’ respectively<br>Initialize: Work = Avaliable<br>Finish[i] = false;for = 1,2,3,4…n</li>
<li>Find an i such that both<br>a. Finish[i] = false<br>b. Need<sub>i</sub> &lt;= Work<br>if no such i exists goto step(4)</li>
<li>Work = Work + Allocation[i]<br>Finish[i] = true<br>goto step(2)</li>
<li>if Finish[i] = true for all i<br> then the system is in a safe state</li>
</ol>
<h5 id="Safety-algorithm-Example"><a href="#Safety-algorithm-Example" class="headerlink" title="Safety algorithm Example"></a>Safety algorithm Example</h5><h4 id="Resource-request-algorithm"><a href="#Resource-request-algorithm" class="headerlink" title="Resource-request algorithm"></a>Resource-request algorithm</h4><p>  令Request<sub>i</sub>为进程P<sub>i</sub>的请求数组。Request[j] = k 表示进程P<sub>i</sub>想要k个资源类型R<sub>j</sub>的实例。当进程P<sub>i</sub>提出资源请求时，将采取一下措施。</p>
<ol>
<li>if Request<sub>i</sub> &lt;= Need<sub>i</sub><br>Goto step (2) ; otherwise, raise an error condition, since the process has exceeded its maximum claim.</li>
<li>if Request<sub>i</sub> &lt;= Avaliable<br>Goto step (3); otherwise, Pi must wait, since the resources are not available</li>
<li>Have the system pretend to have allocated the requested resources to process Pi by modifying the state as follows<br>Avaliable = Avaliable - Request<sub>i</sub><br>Allocation<sub>i</sub> = Allocation<sub>i</sub> + Request<sub>i</sub><br>Need<sub>i</sub> = Need<sub>i</sub> - Request<sub>i</sub></li>
</ol>
<h3 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#include &lt;iostream&gt;</span><br><span class="line">using namespace std;</span><br><span class="line"></span><br><span class="line">int p;  &#x2F;&#x2F;Process 进程数</span><br><span class="line">int r;  &#x2F;&#x2F;Rescource 资源类别</span><br><span class="line">int Max[10][10];   &#x2F;&#x2F;最大需求矩阵</span><br><span class="line">int allocation[10][10];    &#x2F;&#x2F;分配矩阵</span><br><span class="line">int need[10][10];    &#x2F;&#x2F;需求矩阵</span><br><span class="line">int Available[10];   &#x2F;&#x2F;可用资源量</span><br><span class="line">int request[10];   &#x2F;&#x2F;请求量</span><br><span class="line"></span><br><span class="line">void input() &#123;</span><br><span class="line">  int i,j ;</span><br><span class="line">  cout &lt;&lt; &quot;请输入最大矩阵Max : \n&quot;;</span><br><span class="line">  for (i &#x3D; 0; i &lt; p ; i++) &#123;</span><br><span class="line">    for (j &#x3D; 0; j &lt; r ;j++) &#123;</span><br><span class="line">      cin &gt;&gt; Max[i][j];</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  cout &lt;&lt; &quot;请输入分配矩阵 allocation : \n&quot;;</span><br><span class="line">  for (i &#x3D; 0; i &lt; p ; i++) &#123;</span><br><span class="line">    for (j &#x3D; 0; j &lt; r ; j++) &#123;</span><br><span class="line">      cin &gt;&gt; allocation[i][j];</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  cout &lt;&lt; &quot;需求矩阵need : \n&quot;;</span><br><span class="line">  for (i &#x3D; 0; i &lt; p ;i++) &#123;</span><br><span class="line">    for (j &#x3D; 0; j &lt; r; j++) &#123;</span><br><span class="line">      need[i][j] &#x3D; Max[i][j] - allocation[i][j];</span><br><span class="line">      cout &lt;&lt; need[i][j] &lt;&lt; &quot;\n&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  cout &lt;&lt; &quot;请输入可用资源量Available : \n&quot;;</span><br><span class="line">  for (i &#x3D; 0;i &lt; r; i++) &#123;</span><br><span class="line">    cin &gt;&gt; Available[i];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int Compare(int m[],int n[]) &#123;</span><br><span class="line">  for (int i &#x3D; 0; i &lt; r;i++) &#123;</span><br><span class="line">    if (m[i] &lt; n[i]) &#123;</span><br><span class="line">      return 0;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  return 1;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int SafeTest() &#123;</span><br><span class="line">  int flag &#x3D; 0;</span><br><span class="line">  int finish[p];</span><br><span class="line">  int Work[r];</span><br><span class="line">  for (int i &#x3D; 0;i &lt; p;i++) &#123;</span><br><span class="line">    finish[i] &#x3D; 0;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  for (int i &#x3D; 0 ;i &lt; r;i++) &#123;</span><br><span class="line">    Work[i] &#x3D; Available[i];</span><br><span class="line">  &#125;</span><br><span class="line">  cout &lt;&lt; &quot;分配序列 : \n&quot;;</span><br><span class="line">  cout &lt;&lt; &quot;     allocation      need          available&quot; &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">  for (int i &#x3D; 0 ; i &lt; p;i++) &#123;</span><br><span class="line">    for (int j &#x3D; 0 ; j &lt; p ;j++) &#123;</span><br><span class="line">      if (finish[j] &#x3D;&#x3D; 1) &#123;</span><br><span class="line">        continue;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      else &#123;</span><br><span class="line"></span><br><span class="line">        if (Compare(Work,need[j])) &#123;    &#x2F;&#x2F;work &gt;&#x3D; need </span><br><span class="line"></span><br><span class="line">          finish[j] &#x3D; 1;</span><br><span class="line">          cout &lt;&lt; &quot;\n&quot; &lt;&lt; &quot;进程&quot; &lt;&lt; j+1 &lt;&lt; &quot;\t&quot;;</span><br><span class="line">          flag &#x3D; 1;</span><br><span class="line"></span><br><span class="line">          for (int k &#x3D; 0;k &lt; r; k++) &#123;</span><br><span class="line">            printf(&quot;  %2d &quot;, allocation[j][k]);</span><br><span class="line">          &#125;</span><br><span class="line">          cout &lt;&lt; &quot;      &quot;;</span><br><span class="line"></span><br><span class="line">          for (int k &#x3D; 0 ; k &lt; r;k++) &#123;</span><br><span class="line">            printf(&quot;  %2d &quot;,need[j][k]);</span><br><span class="line">          &#125;</span><br><span class="line">          cout &lt;&lt; &quot;      &quot;;</span><br><span class="line"></span><br><span class="line">          for (int k &#x3D; 0; k &lt; r;k++) &#123;</span><br><span class="line">            printf(&quot;  %2d &quot;,Work[k]+allocation[j][k]);</span><br><span class="line">          &#125;</span><br><span class="line"></span><br><span class="line">          for (int k &#x3D; 0;k &lt; r;k++) &#123;</span><br><span class="line">            Work[k] &#x3D; Work[k] + allocation[j][k];</span><br><span class="line">          &#125;</span><br><span class="line"></span><br><span class="line">          break;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      if (flag &#x3D;&#x3D; 1) &#123;</span><br><span class="line">        break;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  cout &lt;&lt; &quot;\n&quot;;</span><br><span class="line">  for (int i &#x3D; 0 ; i &lt; p ;i++) &#123;</span><br><span class="line">    if (finish[i] &#x3D;&#x3D; 0)</span><br><span class="line">      return 0;</span><br><span class="line">  &#125;</span><br><span class="line">  return 1;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void rtest(int n) &#123;</span><br><span class="line">  if (Compare(Available,request) &amp;&amp; Compare(need[n-1],request)) &#123;</span><br><span class="line">    for (int j &#x3D; 0; j &lt; r;j++) &#123;</span><br><span class="line">      allocation[n-1][j] +&#x3D; request[j];</span><br><span class="line">      need[n-1][j] -&#x3D; request[j];</span><br><span class="line">      Available[j] -&#x3D; request[j];</span><br><span class="line">    &#125;</span><br><span class="line">    if (SafeTest())&#123;</span><br><span class="line">      cout &lt;&lt; &quot;允许&quot; &lt;&lt; n &lt;&lt; &quot;进程申请资源！ \n&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">      else &#123;</span><br><span class="line">        cout &lt;&lt; &quot;不允许&quot;&lt;&lt;n&lt;&lt;&quot;进程申请资源! \n&quot;;</span><br><span class="line">        for (int j &#x3D; 0;j &lt; r;j++) &#123;</span><br><span class="line">          allocation[n-1][j] +&#x3D; request[j];</span><br><span class="line">          need[n-1][j] -&#x3D; request[j];</span><br><span class="line">          Available[j] -&#x3D; request[j];</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  else &#123;</span><br><span class="line">    cout &lt;&lt; &quot;申请资源越界！ \n&quot;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    int i,n;       &#x2F;&#x2F;n-第n个资源申请</span><br><span class="line">    cout&lt;&lt;&quot;请输入进程数：&quot;;</span><br><span class="line">    cin&gt;&gt;p;</span><br><span class="line">    cout&lt;&lt;&quot;请输入资源种类数：&quot;;</span><br><span class="line">    cin&gt;&gt;r;</span><br><span class="line">    &#x2F;&#x2F;默认状态4、3</span><br><span class="line">    input();&#x2F;&#x2F;输入函数</span><br><span class="line">    if(SafeTest()&#x3D;&#x3D;1)</span><br><span class="line">    &#123;</span><br><span class="line">        cout&lt;&lt;&quot;存在安全序列，初始状态安全。\n&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">    else</span><br><span class="line">    &#123;</span><br><span class="line">        cout&lt;&lt;&quot;不存在安全序列，初始状态不安全。\n&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;&quot;请输入发出请求向量request的进程编号：&quot;;</span><br><span class="line">    cin&gt;&gt;n;</span><br><span class="line">    cout&lt;&lt;&quot;请输入请求向量request\n&quot;;</span><br><span class="line">    for(i&#x3D;0; i&lt;r; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        cin&gt;&gt;request[i];</span><br><span class="line">    &#125;</span><br><span class="line">    rtest(n);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>lab</category>
      </categories>
      <tags>
        <tag>OS</tag>
      </tags>
  </entry>
  <entry>
    <title>Two Pointers Technique</title>
    <url>/2020/01/14/Two-Pointers/</url>
    <content><![CDATA[<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>  &emsp;&emsp;双指针，顾名思义就是利用两个指针去遍历数组。一般来说遍历数组都是使用单指针(index)去遍历的，<br>  两个指针一般是在有序数组中使用，一个放首，一个放尾，同时向中间遍历，直到两个指针相交，完成遍历，时间复杂度<br>  也是O(n).</p>
<h2 id="用法"><a href="#用法" class="headerlink" title="用法"></a>用法</h2><p>  一般会有两个指针 <code>font</code>,<code>tail</code>。分别指向开始和结束的位置。</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">front &#x3D; 0;</span><br><span class="line">tail &#x3D; A.length()-1</span><br></pre></td></tr></table></figure>
<p>  一般循环结束条件采用的是判断两个指针是否相遇</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">while (front &lt; tail)</span><br><span class="line">  &#123;</span><br><span class="line">    ....</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>  对于in place 交换问题，循环结束条件一般是其中一个指针遍历完成的</p>
<h2 id="使用范围"><a href="#使用范围" class="headerlink" title="使用范围"></a>使用范围</h2><p>  一般双指针在有序数组中使用的特别多。(部分情况下，未排序数组也有应用)一般用来解决下列问题</p>
<ul>
<li><p>两数求和<br>一般这种问题都是问，寻找两个数的和为一个特定值，这时候，如果数组有序，我们采用两个指针，分别从<br>前和后往中间遍历,front移动和增大，tail移动和减小，通过特定的判断，可以求出特定的和<br>时间复杂度为O(n)，如果要使用双重循环则时间复杂度为O($n^{2}$)</p>
</li>
<li><p>in place 交换<br>数组的in place原地交换一般都得用双指针，不然数组中添加或这删除一个元素，需要移动大量的元素。这时候一般是一个指针遍历，一个指针用来找可以交换的元素。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>theory</tag>
      </tags>
  </entry>
  <entry>
    <title>腾讯光子一面</title>
    <url>/2021/03/17/interview_first/</url>
    <content><![CDATA[<h1 id="面试问题"><a href="#面试问题" class="headerlink" title="面试问题"></a>面试问题</h1><h2 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h2><ul>
<li>时间复杂度与空间复杂</li>
<li>不同的排序的最好与最坏的时间复杂度</li>
<li>快排<ol>
<li>如何实现的</li>
<li>何时会导致快排的最坏时间复杂度</li>
<li>快排是否稳定，为什么不稳定</li>
</ol>
</li>
<li>了解堆排序吗</li>
</ul>
<h2 id="算法"><a href="#算法" class="headerlink" title="算法"></a>算法</h2><ul>
<li>反转链表(Leetcode ＃206)</li>
</ul>
<h2 id="C-基础"><a href="#C-基础" class="headerlink" title="C++ 基础"></a>C++ 基础</h2><ul>
<li>C++ 关键字static，它的作用</li>
<li>C++ 关键字sizeof作用<ul>
<li>strlen与它的区别</li>
<li>sizeof(“hello”)与strlen(“hello”)区别</li>
<li>double<code><em></code> (<code></em></code>a) [3][6]; sizeof(*a)与sizeof(a)的大小</li>
</ul>
</li>
<li>C++ 多态<ul>
<li>如何实现</li>
<li>什么是虚函数，什么是虚表和虚拟指针</li>
<li>内存中的布局是什么样子的</li>
</ul>
</li>
<li>C++ 重载与重写<ul>
<li>什么是重载，什么是重写</li>
<li>它们的区别</li>
<li>重载是否与类有关，重载是如何实现的</li>
</ul>
</li>
<li>C++ new/delete &amp;&amp; malloc/free<ul>
<li>它们的区别</li>
</ul>
</li>
<li>C++ 的内存分区<ul>
<li>说一下每个区的作用</li>
</ul>
</li>
<li>C++ 堆栈的区别</li>
<li>C++ 编译执行的过程</li>
<li>C++ 深拷贝与浅拷贝的区别</li>
<li>C++ 11新特性有了解过哪些?</li>
<li>C++ 动态转换有了解过？<ul>
<li>常见的几种动态转换说一下</li>
<li>它们的作用</li>
</ul>
</li>
<li>C++ define 与 typedef<ul>
<li>define的功能与作用</li>
<li>define 与 typedef 区别</li>
</ul>
</li>
<li>C++ 内存泄漏的原因</li>
<li>C++ move <ul>
<li>move的使用与功能</li>
</ul>
</li>
</ul>
<h2 id="C-STL"><a href="#C-STL" class="headerlink" title="C++ STL"></a>C++ STL</h2><ul>
<li>你有用过哪些容器，可以讲讲</li>
<li>vector容器的底层实现<ul>
<li>map底层实现红黑树</li>
</ul>
</li>
<li>有使用过迭代器吗</li>
</ul>
<h2 id="操作系统"><a href="#操作系统" class="headerlink" title="操作系统"></a>操作系统</h2><ul>
<li>进程间的通信方式</li>
<li>进程与线程的区别</li>
</ul>
<h2 id="计算机网络"><a href="#计算机网络" class="headerlink" title="计算机网络"></a>计算机网络</h2><ul>
<li>讲一下TCP/IP的三次握手与四次挥手<ul>
<li>三次握手<ul>
<li>TCP/IP 是否可以为两次，为什么</li>
<li>TCP/IP 最后一个确认包server端一直接收不到，client应如何操作</li>
</ul>
</li>
<li>四次挥手<ul>
<li>什么时候会进入Time_wait 等待状态，时间为多久</li>
<li>server 是否也会进入Time_wait 等待状态，什么状况会导致server进行这个状态</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="结束反问"><a href="#结束反问" class="headerlink" title="结束反问"></a>结束反问</h2><ul>
<li>面试官评价<ul>
<li>指针使用仍有欠缺</li>
<li>对于一些底层原理应深入了解</li>
<li>操作系统还需要深入</li>
</ul>
</li>
<li>面试官荐书<ul>
<li>书箱大致属于一些经典书目</li>
<li>可以多阅读一些优秀的Blog，参考这些优秀博主的如何思考以及他们对于底层原理的解析</li>
</ul>
</li>
</ul>
<h1 id="面试问题答案总结"><a href="#面试问题答案总结" class="headerlink" title="面试问题答案总结"></a>面试问题答案总结</h1><h2 id="排序-1"><a href="#排序-1" class="headerlink" title="排序"></a>排序</h2><ol>
<li><strong>排序算法的时间复杂度与空间复杂度</strong></li>
</ol>
<table>
<thead>
<tr>
<th align="center">排序算法</th>
<th align="center">平均时间复杂度</th>
<th align="center">最坏时间复杂度</th>
<th align="center">最好时间复杂度</th>
<th align="center">空间复杂度</th>
<th align="center">稳定性</th>
</tr>
</thead>
<tbody><tr>
<td align="center">冒泡排序</td>
<td align="center">$O(n^{2})$</td>
<td align="center">$O(n^{2})$</td>
<td align="center">$O(n)$</td>
<td align="center">$O(1)$</td>
<td align="center">稳定</td>
</tr>
<tr>
<td align="center"><font color=red>直接选择排序</font></td>
<td align="center">$O(n^{2})$</td>
<td align="center">$O(n^{2})$</td>
<td align="center">$O(n)$</td>
<td align="center">$O(1)$</td>
<td align="center"><font color=red>不稳定</font></td>
</tr>
<tr>
<td align="center">直接插入排序</td>
<td align="center">$O(n^{2})$</td>
<td align="center">$O(n^{2})$</td>
<td align="center">$O(n)$</td>
<td align="center">$O(1)$</td>
<td align="center">稳定</td>
</tr>
<tr>
<td align="center"><font color=red>快速排序</font></td>
<td align="center">$O(nlogn)$</td>
<td align="center">$O(n^{2})$</td>
<td align="center">$O(nlogn)$</td>
<td align="center">$O(nlogn)$</td>
<td align="center"><font color=red>不稳定</font></td>
</tr>
<tr>
<td align="center"><font color=red>堆排序</font></td>
<td align="center">$O(nlogn)$</td>
<td align="center">$O(nlogn)$</td>
<td align="center">$O(nlogn)$</td>
<td align="center">$O(1)$</td>
<td align="center"><font color=red>不稳定</font></td>
</tr>
<tr>
<td align="center"><font color=red>希尔排序</font></td>
<td align="center">$O(nlogn)$</td>
<td align="center">$O(ns)$</td>
<td align="center">$O(n)$</td>
<td align="center">$O(1)$</td>
<td align="center"><font color=red>不稳定</font></td>
</tr>
<tr>
<td align="center">归并排序</td>
<td align="center">$O(nlogn)$</td>
<td align="center">$O(nlogn)$</td>
<td align="center">$O(nlogn)$</td>
<td align="center">$O(n)$</td>
<td align="center">稳定</td>
</tr>
<tr>
<td align="center">计数排序</td>
<td align="center">$O(n+k)$</td>
<td align="center">$O(n+k)$</td>
<td align="center">$O(n+k)$</td>
<td align="center">$O(n+k)$</td>
<td align="center">稳定</td>
</tr>
<tr>
<td align="center">基数排序</td>
<td align="center">$O(N * M)$</td>
<td align="center">$O(N * M)$</td>
<td align="center">$O(N * M)$</td>
<td align="center">$O(M)$</td>
<td align="center">稳定</td>
</tr>
</tbody></table>
<ol start="2">
<li><strong>快速排序</strong></li>
</ol>
<ul>
<li> 基本思想    *  通过一趟排序将待排记录分割成独立的两个部分，其中一部分关键字均比另一部分记录关键字小，接着对这两部分记录继续进行排序，以达到整个序列有序的目的。</li>
<li>最优与最坏情况分析<ul>
<li>最优的情况：Parition每次都划分得很均匀。在获得枢轴后将数组一分为二，各自仅需$(n/2)$的时间，这是最好的情况，时间复杂度为$O(nlogn)$</li>
<li>最坏的情况：待排序的数组是正序或逆序，每次划分只得到一个比上一次少一个记录的子序列，这里另一个记录是为空的。如果用二叉树表示就是一个斜树,这里就需要n-1次递归调用，所以其最终时间复杂度为$O(n^{2})$</li>
</ul>
</li>
<li>快排的不稳定的原因<ul>
<li>关键字的比较是跳跃进行的，因此快排是不稳定的</li>
</ul>
</li>
</ul>
<ol start="3">
<li><strong>堆排序</strong></li>
</ol>
<ul>
<li>基本思想：将待排序的序列构造成一个大顶堆。此时，整个序列的最大值就是堆顶的根节点。将其移走(实际就是将其与堆数组末尾元素交换，此时末尾元素为最大值),然后将剩余的n-1个序列重新构造成一个椎，这样就会得到n个元素中的次小值。之后重复之前的操作，就可以得到一个有序序列了</li>
<li>稳定性：由于记录的比较与交换是跳跃的，所以堆排序是一种不稳定的排序方式</li>
<li>适用场景：不适合待排序</li>
</ul>
<h2 id="算法-1"><a href="#算法-1" class="headerlink" title="算法"></a>算法</h2><ol>
<li><strong>反转链表</strong><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    ListNode* reverseList(ListNode* head) &#123;</span><br><span class="line">        ListNode* prev &#x3D; nullptr;</span><br><span class="line">        ListNode* cur &#x3D; head;</span><br><span class="line">        </span><br><span class="line">        while (cur !&#x3D; nullptr) &#123;</span><br><span class="line">            ListNode* next &#x3D; cur-&gt;next;</span><br><span class="line">            cur-&gt;next &#x3D; prev;</span><br><span class="line">            prev &#x3D; cur;</span><br><span class="line">            cur &#x3D; next;</span><br><span class="line">        &#125;</span><br><span class="line">        return prev;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
</ol>
<h2 id="C-基础-1"><a href="#C-基础-1" class="headerlink" title="C++ 基础"></a>C++ 基础</h2><ol>
<li><strong>C++ 用法与作用</strong></li>
</ol>
<ul>
<li>隐藏: 在编译多个文件时，所有未加static前缀的全局变量与函数都具有全局可见性</li>
<li>保持变量内容的持久</li>
<li>static变量默认初始化为0</li>
<li>类成员声明为static<ul>
<li>static 变量只会被分配一次内存，因此它具有对值的记忆性</li>
<li>static 全局变量可以被模块内所有函数访问，不可以被模块外的其它函数访问</li>
<li>类中static成员变量属于整个类，对类的对象仅有一份拷贝</li>
<li>类的static成员函数为这个类所拥有，这个函数不接收this指针，所以只可以访问类的static成员变量</li>
<li>static类对象必须在类外进行初始化</li>
<li>static成员函数不可以被virtul修饰</li>
</ul>
</li>
</ul>
<ol start="2">
<li><strong>C++ sizeof用法与作用</strong></li>
</ol>
<ul>
<li>定义： sizeof是一个操作符，作用是返回一个对象或类型所占的内存字节数</li>
<li>数组： sizeof(数组)的值等于数组所占用内存字节数</li>
<li>联合类型：操作数为联合类型时，sizeof的结果为其最大字节成员的字节数</li>
<li>内存对齐问题：由于结构体会自动对齐，所以sizeof的结果就是最大类型的所占用的字节数</li>
<li>类：空类大小为1，一个空类作为基类，它的大小为0。如果类的成员函数中有虚函数，就是固定占用4字节</li>
<li>strlen与它的区别<ul>
<li>sizeof 是运算符，strlen是库函数</li>
<li>sizeof的参数可以为任何数据类型或者数据，strlen参数只能是字符指针且结尾为’\0’的字符串</li>
<li>sizeof值在编译时确定，所以不能得到动态分配存储空间的大小</li>
</ul>
</li>
<li>sizeof(“hello”) 与 strlen(“hello”) <ul>
<li>sizeof(“hello”) 结果为6</li>
<li>strlen(“hello”) 结果为5，因为不包含’\0’</li>
</ul>
</li>
<li>sizeof( * a)指向的是整个数组空间，所以大小为144。sizeof(a)指的是单个变量所占用的字节数，又因为指向double类型，所以为8字节</li>
</ul>
<ol start="3">
<li><strong>C++ 多态</strong></li>
</ol>
<ul>
<li>如何实现多态<ul>
<li>在基类的函数前加上<code>virtual</code>关键字，在派生类中重写该函数，运行时将会根据所指对象的实际类型来调用相应的函数。</li>
</ul>
</li>
<li>多态实现过程<ul>
<li>编译器发现类中在虚函数，会自动为其生成一份虚表，该表是一个一维数组，虚表里存储了虚函数入口地址</li>
<li>编译器会在对象的前四个字节中保存一个虚表指针，即vptr</li>
<li>调用构造函数，在构造函数中创建虚表并对其进行初始化</li>
<li>派生类对基类的虚函数没有重写，则派生类的vptr指向基类的虚表。有重写的话，派生类的vptr会指向自身的虚表。当派生类中有自己的虚表时，会将其添加在后面。</li>
</ul>
</li>
<li>内存布局<ul>
<li>单一继承的内存布局<ul>
<li>每个类最多只会有一个vptr指针，并放在第一个拥有virtual function 的类之后(<font color=red>父类必须保证对象的完整性</font>)</li>
<li>virtual function 工作方式：可以通过vptr找到vtable，对应的vtable记录了所指对象的真正类型。vtable中第一个slot存储了type_info。对应的函数的真正的地址可以从vtable中的对应slot中找到。</li>
</ul>
</li>
<li>多重继承下的内存布局<ul>
<li>根据继承父类的个数保存对应的vptr</li>
<li>根据所保存的虚表指针个数，产生相应个数的虚表</li>
<li>构造时会行构造会优先构造第一个基类</li>
<li>多重继承中的菱形继承是存在二义性的风险的，即使可以通过明确调用路径，但是二义性的潜在性还未消除。</li>
</ul>
</li>
<li>虚拟继承<ul>
<li>解决了菱形继承中，子类拥有多个间接父类实例的情况</li>
<li>虚拟继承而来的子类会生成一个隐藏的虚基类指针，虚基类指针总是在vptr之后</li>
<li>子类会覆盖基类的虚函数 </li>
</ul>
</li>
</ul>
</li>
</ul>
<ol start="4">
<li><strong>C++ 重载与重写</strong></li>
</ol>
<ul>
<li>重载是指在同一范围定义中的同名成员函数才存在重载关系,其特点为：<ul>
<li>参数名相同</li>
<li>参数类型与数目有所不同</li>
<li>不能出现参数个数与类型均相同</li>
</ul>
</li>
<li>重写指的是在派生类中的基类中的同名函数，<strong>重写就是重写函数体，要求基类函数必须是虚函数</strong>且<ul>
<li>与基类的虚函数有相同的参数个数</li>
<li>与基类的虚函数有相同的参数类型</li>
<li>与基类的虚函数有相同的返回类型</li>
</ul>
</li>
<li>区别<ul>
<li>重写是父类与子类之间的垂直关系，重载是不同函数间的水来关系</li>
<li>重写要求参数列表相同，重载要求参数列表不同，返回值不要求</li>
<li>重写关系中，调用方法根据对象类型决定，重载根据调用时实参表与形参表的对应关系来选择函数体</li>
</ul>
</li>
<li>重载的实现原理<ul>
<li>利用命名倾轧技术(name mangling)，来改名函数名，区分参数不同的同名函数</li>
</ul>
</li>
</ul>
<ol start="5">
<li><strong>C++ new/delete 与 malloc/free 区别</strong></li>
</ol>
<ul>
<li>new/delete是C++ 关键字需要编译器支持；malloc/free 是库函数，需要头文件支持。</li>
<li>使用new申请的内存，编译器会自动计算分配大小；malloc申请的内存，需要手动计算大小</li>
<li>new 内存分配成功，返回的是对象的类型指针，无须强制类型转换；malloc分配内存成功后返回是void，需要强制类型转,所以malloc不安全。</li>
<li>new 内存分配失败会抛出bac_alloc异常，malloc分配失败会返回NULL</li>
<li>new/delete 会去调用constructor与deconstructor，而malloc/free 仅是对于资源的申请与释放</li>
</ul>
<ol start="6">
<li><strong>C++ 内存分区</strong></li>
</ol>
<ul>
<li>一共分为六个区域，分别是：栈、自由存储区、堆、全局/静态存储区、常量存储区与代码区</li>
<li>简单说明各个分区的作用<ul>
<li>栈：在函数中，函数内部局部变量的存储单元都可以在栈上创建，结束后自动释放。由于栈内置于处理器中，效率高，但是分配的内存有限</li>
<li>堆：就是那些由new分配的内存块，需要我们程序去控制。一个delete对应一个new，如果没有释放，程序结束后会由系统自动回收</li>
<li>自由存储区：全局变量与静态变量被分配到一块内存中，它们会被自动初始化</li>
<li>常量存储区：仅存放常量，不允许修改</li>
<li>代码区：存放函数体的二进制代码</li>
</ul>
</li>
</ul>
<ol start="7">
<li><strong>C++ 堆栈的区别</strong></li>
</ol>
<ul>
<li>申请方式不同<ul>
<li>栈是由系统自动分配的</li>
<li>堆是由自己申请与释放的</li>
</ul>
</li>
<li>申请的大小限制<ul>
<li>栈顶与栈底是预设好的，栈是向栈底扩展，大小固定</li>
<li>堆向高地址扩展，是不连续的内存区域</li>
</ul>
</li>
<li>申请效率不同<ul>
<li>栈由系统分配，速度快，不会在碎片</li>
<li>堆是由程序员分配，速度会比较慢，且会有内存碎片</li>
</ul>
</li>
<li>各自大小<ul>
<li>栈默认4M</li>
<li>堆区一般为1G-4G</li>
</ul>
</li>
</ul>
<ol start="8">
<li><strong>C++ 程序编译过程</strong></li>
</ol>
<ul>
<li>具体为两步：编译、链接</li>
<li>编译可以细分为三个阶段<ul>
<li>预编译处理</li>
<li>编译优化</li>
<li>转换成汇编</li>
</ul>
</li>
<li>链接<ul>
<li>需要链接原因：某个文件调用了另一个文件的函数或常量,或者是调用了某库函数</li>
<li>主要工作就是将有关目标文件连接起来</li>
</ul>
</li>
</ul>
<ol start="9">
<li><strong>深拷贝与浅拷贝的区别</strong></li>
</ol>
<ul>
<li>浅拷贝<ul>
<li>浅拷贝只是拷贝一个指针，并没有新开辟一个地址，拷贝的指针与原来的指针指向同一块地址，如果原来的指针所指向的资源被释放了，那么再释放浅拷贝指针的资源就会出现错误。</li>
</ul>
</li>
<li>深拷贝<ul>
<li>深拷贝不仅影响值，还为其开辟一个新的空间来存放这个值。即使原先的对象被析构且内存被释放掉了，这也不会对深拷贝的值有所影响。在自己实现拷贝赋值时，如果有指针变量的话需要自己实现深拷贝的。</li>
</ul>
</li>
</ul>
<ol start="10">
<li><strong>C++ 新特性有了解过哪些</strong></li>
</ol>
<ul>
<li>nullptr替代了NULL</li>
<li>引入了auto与decltype这两个关键字，实现了类型推导</li>
<li>基于范围的for循环 <code> for(auto& i : res) &#123;&#125; </code></li>
<li>类与结构体中初始化列表 </li>
<li>Lambda表达式(匿名函数)</li>
<li>std::forward_list(单向链表)</li>
<li>右值引用与move语义</li>
<li>…..</li>
</ul>
<ol start="11">
<li><strong>了解C++四种强制转换</strong></li>
</ol>
<ul>
<li>reinterpret_cast<ul>
<li>reinterpret_cast(expression)</li>
<li>type_id必须是一个指针、引用、算术类型、函数指针或者成员指针。它可以用于类型之间的强制转换</li>
</ul>
</li>
<li>const_cast <ul>
<li>const_cast<type_id>(expression)</li>
<li>该运算符用来修改类型const与volatile属性。除了const与volatile修饰之外,type_id与expression的类型一致。用法如下：<ul>
<li>常量指针被转化为非常量的指针，并且仍然指向原来的对象</li>
<li>常量引用被转化为非常量的引用，并且仍然指向原来的对象</li>
<li>const_cast一般用于修改底层指针，如const char *p形式</li>
</ul>
</li>
</ul>
</li>
<li>static_cast<ul>
<li>static_cast<type_id>(expression)</li>
<li>该运算符把expression转换为type_id类型，但没有运行时类型检查来保证转换的安全性，主要有如下几种用法<ul>
<li>用于类层次结构中基类与派生类之间指针或引用的转换<ul>
<li>进行上行转换(把派生类指针或引用转换为基类表示)是安全的</li>
<li>进行下行转换(把基类的指针或引用转换为派生类表示)时，由于没有动态类型检查，所以是不安全的。</li>
</ul>
</li>
</ul>
</li>
<li>用于基本数据类型之间的转换,如int转换为char，int转换为enum。这种类型的转换的安全性需由开发者来保证</li>
<li>把空指针转换为目标类型的空指针</li>
<li>把任何类型的表达式转换为void类型</li>
<li><font color=red> static_cast 不能转换expression的const、volatile、_unaligned属性</font></li>
</ul>
</li>
<li>dynamic_cast <ul>
<li>dynamic_cast(expression)</li>
<li>在类型检查，基类向派生类转换是安全的，但是派生类向基类转换就有一些不安全</li>
<li>该运算符把expression转换为type_id类型。type_id必须是类指针、类的引用或void *</li>
<li>如果type_id 是指针类型，那么expression也必须是一个指针，如果type_id是一个引用，那么expression也必须是一个引用</li>
<li>这个转换是可以在执行期间决定真正的类型，因此expression必须是多态。</li>
<li>在类之间进行转换，dynamic_cast与static_cast的效果一样</li>
<li>在进行上下转换时，dynamic_cast具有类型检查功能,比static_cast功能</li>
</ul>
</li>
</ul>
<ol start="12">
<li><strong>C++ define 与typedef</strong></li>
</ol>
<ul>
<li>宏主要用于定义常量及书写复杂的内容；typedef 主要用于定义类型别名</li>
<li>宏替换发生在编译阶段，属于文本插入替换；typedef是编译的一部分</li>
<li>宏不检查类型；typedef会检查数据类型</li>
<li>宏不是语句，不用在最后添加分号；typedef 是语句，要加分号标识</li>
<li>注意对指针的操作，typedef char * p_char与#define p_char char * 区别巨大</li>
</ul>
<ol start="12">
<li><strong>C++ 内存泄漏原因</strong></li>
</ol>
<ul>
<li>定义<ul>
<li>内存泄漏是指<strong>堆内存的泄漏</strong>。堆内存是指程序从堆中分配的大小任意的内存块。如果通过new/malloc申请意资源后，没有使用delete/free来释放，那么这个内存块将不可被再次使用，这就是内存泄漏。</li>
</ul>
</li>
<li>泄漏的几种情况<ul>
<li>基类的析构函数没有申明为虚函数</li>
<li>对象数组没有使用delete[]来释放</li>
<li>new/delete,malloc/free 必须成对出现</li>
</ul>
</li>
</ul>
<ol start="13">
<li><strong>C++ move语义</strong></li>
</ol>
<ul>
<li>作用<ul>
<li>独享指针所有权的转移</li>
<li>左值到右值属性的转移</li>
</ul>
</li>
</ul>
<h2 id="C-STL-1"><a href="#C-STL-1" class="headerlink" title="C++ STL"></a>C++ STL</h2><ol>
<li>使用过哪些容器</li>
</ol>
<ul>
<li>vector</li>
<li>map</li>
<li>set</li>
<li>…</li>
</ul>
<ol start="2">
<li>vector容器的底层实现  </li>
</ol>
<ul>
<li>vector 是一个类似于array的序列容器。但是array维护的是静态空间，而vector则使用灵活的动态空间配置,维护一块连续的线性空间。在空间不足时，可以自动扩充空间容纳新元素。扩充时也需经历的有：重新配置空间、移动数据、释放原空间行操作</li>
<li>vector 扩充规则<ul>
<li>以原大小的两倍配置另外一块较大空间(或者 旧长度+新增元素个数)(Win+Vs下扩展1.5倍，Linux+GCC扩展是两倍)</li>
</ul>
</li>
</ul>
<ol start="3">
<li>迭代器使用</li>
</ol>
<ul>
<li>迭代器是一种抽象的设计理念，通过迭代器可以在不了解容器的情况下遍历容器。除此之外，它还是作为容器与STL算法的重要粘合剂。</li>
<li>迭代器的作用就是提供一个遍历容器内部所有元素的接口，因此容器内部必须保存一个与容器相关联的指针</li>
<li>常用的五种迭代器： value type、different type、pointer、reference、iterator catagoly;</li>
</ul>
<h2 id="操作系统-1"><a href="#操作系统-1" class="headerlink" title="操作系统"></a>操作系统</h2><ol>
<li>进程间的通信方式(Linux)</li>
</ol>
<ul>
<li>管道<ul>
<li>无名管道(内存文件): 管道是一种半双工的通信方式，数据只能单向的流通，而且只能在具有亲缘关系的进程之间使用。(进程的亲缘关系：父子进程关系)</li>
<li>有名管道(FIFO文件，借助文件系统): 有名管道是半双工的通信方式，但是允许在没有亲缘关系的进程之间使用，管道是先进先出的通信方式</li>
</ul>
</li>
<li>共享内存：映射一段能被其它进程所访问的内存，这段共享内存是由一个进程创建，可由多个进程访问。共享内存是最快的IPC方式，它是针对其它进程间通信方式运行效率低而设计的。它往往与信号量配合使用来实现进程间的同步与通信</li>
<li>消息队列：消息队列是有消息的链表，存放在内核中并由消息队列标识符标识。消息队列克服了信号传递信息少、管道只能承载无格式字节流以及缓冲区大小受限等缺点。</li>
<li>套接字： 适用于不同的机器间进程通信，在本地也可作为两个进程通信的方式</li>
<li>信号：用于通知接收进程某个事件已经发生，比如按下<code>Ctrl+c</code>就是信号。</li>
<li>信号量：这是一个计数器，可以用来控制多个进程对共享资源的访问。它常作为一种锁机制，实现进程、线程的对临界区的同步及互斥访问</li>
</ul>
<ol start="2">
<li>进程与线程</li>
</ol>
<ul>
<li>定义<ul>
<li>进程是资源分配与拥有的基本单位；线程是程序执行的基本单位</li>
</ul>
</li>
<li>切换过程<ul>
<li>进程是：用户态-&gt;内核态-&gt;用户态;线程是与其一致</li>
</ul>
</li>
<li>拥有的资源<ul>
<li>进程拥有CPU资源、内存资源、文件资源与句柄；线程拥有程序计数器、寄存器、栈与状态字。</li>
</ul>
</li>
<li>并发性<ul>
<li>不同的进程之间acrq实现并发，各自占有CPU实现并行;一个进程内部的多个线程并执行。</li>
</ul>
</li>
<li>通信方面<ul>
<li>进程间通信需要借助操作系统；线程间可以直接读写进程数据段。</li>
</ul>
</li>
</ul>
<h2 id="计算机网络-1"><a href="#计算机网络-1" class="headerlink" title="计算机网络"></a>计算机网络</h2><ol>
<li>TCP/IP的三次握手<br>刚开始客户端处于Closed的状态，服务端处于Listen状态，进行三次握手</li>
</ol>
<ul>
<li>第一次握手：客户端给服务端发一个SYN报文，并指明客户端的初始化序列号ISN(c)。此时客户端处于<code>SYN_SEND</code>状态<ul>
<li>首部的同步位SYN=1,初始化seq=x,SYN=1的报文段不能携带数据，但要消耗一个序列号</li>
</ul>
</li>
<li>第二次握手：服务器收到客户端SYN报文之后，会以自己的SYN报文作为应答，并且也是指定了自己的初始化ISN(s)。同时会把客户端的ISN+1作为ACK的值，表示自己已经收到了客户端的SYN，此时服务器牌SYN_RCVD的状态<ul>
<li>在确认报文段中SYN=1,ACK=1,确认号ack=x+1,初始序号seq = y.</li>
</ul>
</li>
<li>第三次握手：客户端收到SYN报文之后，会发送一个ACK报文，当然，也是一样把报务器的ISN＋1作为ACK的值，表示已经收到了服务端的SYN报文，此时客户端牌ESTABLISHED状态。服务器收到ACK报文之后，也处于ESTABLISHED状态，此时，双方已经建立起了连接<ul>
<li>确认报文段ACK＝1，确认号ack=y+1,序号seq=x+1(初始为seq=x,第二个报文段所以要＋1),ACK报文段可以携带数据，不携带数据则不消耗序号。</li>
</ul>
</li>
</ul>
<p>&emsp;&emsp;发送第一个SYN的一端执行主动打开(avtive open),接收这个SYN并发回下一个SYN的另一端执行被动打开(passive open)。在socket编程中，客户端执行connect()时，将触发三次握手。</p>
<ol start="2">
<li>TCP/IP是否可以为两次，为什么</li>
</ol>
<ul>
<li>弄清这个问题，我们需要先弄明白三次握手的目的是什么，能否使用再次握手达到目的。<ul>
<li>第一次握手：客户端发送网络包，服务端到了。这样服务端就能得出结论：客户端的发送能力、服务端的接收能力正常。</li>
<li>第二次握手：服务端发包，客户端到了。这样客户端就能得出结论：服务端的接收、发送能力，客户端的接收、发送能力是正常的。不过此时服务器并不能确认客户端的接收能力是否正常。</li>
<li>第三次握手：客户端发包，服务端收到了。这样服务端就能得出结论：客户端的接收、发送能力正常，服务器自己的发送、接收能力也正常。</li>
</ul>
</li>
<li>因此，需要三次握手才能确认双方的接收与发送能力是否正常。</li>
<li>如果使用两次握手，则会出现以下这种情况<ul>
<li>客户端发出连接请求，但因连接请求报文丢失而未收到确认，于是客户端再重传一次连接请求。</li>
<li>后来收到确认，建立连接。数据传输完毕后，就释放连接，客户端共发出两个连接请求报文段，其中第一个丢失，第二个到达了服务端。</li>
<li>但是第一个丢失的报文段只是在某些网络结点长时间滞留了，延误到连接释放以后的某个时间才到达服务端。</li>
<li>此时服务端误认为客户端又发出一次新的连接请求，于是就向客户端发出确认报文段，同意建立，不采用三次握手，只要服务端发出确认，就建立新的连接了。</li>
<li>此时客户端忽略服务端发来的确认，也不发送数据，则服务端一致等待客户端发送数据，浪费资源。</li>
</ul>
</li>
</ul>
<ol start="3">
<li>TCP/IP最后一个确认包server端一直接收不到，client应如何操作</li>
</ol>
<ul>
<li>SYN-ACK重传次数的问题：服务器发送完SYN-ACK包，如果未收到客户确认包，服务器进行首次重传，等待一段时间仍未收到客户确认包，进行二次重传。如果重传次数超过系统规定的最大重传次数，系统将该连接信息从半连接队列中删除。(<font color=red>每次重传等待的时间不一定趋同，一般会是指数增长，例如间隔时间为1s,2s,4s,8s…</font>)</li>
</ul>
<ol start="4">
<li>TCP/IP 四次挥手<br>刚开始双方都处于ESTABLISHED状态,假如是客户端先发起关闭请求。四次挥手的过程如下:</li>
</ol>
<ul>
<li>第一次挥手：客户端发送一个FIN报文，报文中会指定一个序列号。此时客户端牌FIN_WATI1状态。即发出<strong>连接释放报文段</strong>(FIN=1,seq=u),并停止再发送数据，主动关闭TCP连接，进入FIN_WATI1状态(终止等待1)状态，等待服务端的确认。</li>
<li>第二次挥手：服务器收到FIN之后，会发送ACK报文，且把客户端的序列号值+1作为ACK报文的序列号值，表明已经收到客户端的报文了，此时服务端处于<code>CLOSE_WAIT</code>(关闭等待)状态，此时的TCP处于半关闭状态，客户端到服务端的连接释放。客户端收到服务端的确认后，进行FIN_WATI2(终止等待2)状态，等待服务端发出的连接释放报文段。</li>
<li>第三次挥手：如果服务端也想断开连接了，和客户端的第一次挥手一样，发给FIN报文，且指定一个序列号。此时服务端处于<code>LAST_ACK</code>状态。即服务端没有要向客户端发出的数据，服务端发出<strong>连接释放报文</strong>(FIN=1,ACK=1,seq=w,ack=u+1),服务端进入LAST_ACK(最后确认)状态，等待客户端的确认。</li>
<li>第四次挥手：客户端收到FIN之后，一样发送一个ACK报文作为应答，且把服务器端的序列号值+1作为自己ACK报文的序列号值，此时客户端处于<code>TIME_WAIT</code>， 。需要过一阵子以确保服务端收到自己的 ACK 报文之后才会进入 CLOSED 状态，服务端收到 ACK 报文之后，就处于关闭连接了，处于 CLOSED 状态。 即客户端收到服务端的连接释放报文段后，对此发出确认报文段（ACK=1，seq=u+1，ack=w+1），客户端进入TIME_WAIT（时间等待）状态。此时TCP未释放掉，需要经过时间等待计时器设置的时间2MSL后，客户端才进入CLOSED状态。<br>&emsp;&emsp;收到一个FIN只意味着在这一方向上没有数据流动。客户端执行主动关闭并进入TIME_WAIT是正常的，服务端通常执行被动关闭，不会进入TIME_WAIT状态。在socket编程中，任何一方执行close()操作即可产生挥手操作。</li>
</ul>
<ol start="5">
<li>何时会进入TIME_WAIT等待状态，多长时间，为什么是那么长时间</li>
</ol>
<ul>
<li>当收到一个FIN只意味着这一方向上没有数据流动。客户端执行主动关闭并进入TIME_WAIT。</li>
<li>2msl，理论上，四个报文者发送完毕，就可以直接进入CLOSED状态了，但是可能网络是不可靠的，在可能最后一个ACK丢失。所以TIME_WAIT状态就是用来生性可能丢失的ACK报文。</li>
</ul>
<ol start="6">
<li>server是否也会进入TIME_WAIT状态，什么情况会导致server进行这个状态</li>
</ol>
<ul>
<li>会的，出现在高并发短连接的TCP服务器上</li>
</ul>
]]></content>
      <categories>
        <category>experience</category>
      </categories>
      <tags>
        <tag>interview</tag>
      </tags>
  </entry>
  <entry>
    <title>Inside the C++ Object Model reading note</title>
    <url>/2021/01/11/%E6%B7%B1%E5%BA%A6%E6%8E%A2%E7%B4%A2C-%E5%AF%B9%E8%B1%A1%E6%A8%A1%E5%9E%8B/</url>
    <content><![CDATA[<p><font color="blue">蓝色:概念、讨论主题</font><br><u>下划线：值得注意的内容</u></p>
<p><font color="fuchsia">紫红色：比较重要的地方</font></p>
<p><font color="red">红色：必须理解的地方</font></p>
<h2 id="第一章-关于对象"><a href="#第一章-关于对象" class="headerlink" title="第一章 关于对象"></a><center>第一章 关于对象</center></h2><p>介绍了C++ 如何在背后实现一个对象，内存中的布局以及空间上的关系</p>
<ol>
<li>C++ 中以类对数据进行封装后，布局成本是否会增加？<ul>
<li>一般来说，并不会增加成本。每个data member直接内含在每一个class object中，就像C struct的情况一样。而member funcitons虽然含在class声明中，却不出现在object中。<font color="purple">所以类的封装并不会增加布局成本</font></li>
</ul>
</li>
<li><font color="red">C++在布局以及存取时间上主要的额外负担是由virtual引起的</font>，包括：<ul>
<li><strong>virtual funcitons机制</strong>:每一个class有一个virtual table内含class之中有作用的virtual funcitons地址，每个object有一个vptr指向virtual table所在</li>
<li><strong>virtual base class</strong>: 用以实现 “多次出现在继承体系中的base class有一个单一而被共享的实例”</li>
</ul>
</li>
<li><u>C++ 布局成本的主要来源就是virtual机制</u></li>
<li><font color="red">C++对象模型(The C++ Object Model)</font><br><img src="https://i.loli.net/2021/01/19/gfEsZuIHRxYBdhD.png" alt="C___object_model.png"><ul>
<li>non-static data members被配置于每一个class object之内</li>
<li>static data members则被存放在所有class object之外</li>
<li>static/non-static funciton members也被放在所有class object之外</li>
</ul>
</li>
<li><font color="blue">virtual funciton 机制由以下两个步骤来支持</font>：<ul>
<li>每一个class产生出一系列<font color="blue">virtual funciton的指针,放在一个被称为virtual table(vtbl,vtable</font>的表格中;</li>
<li>每一个class object被添加一个<font color="blue">vptr指向相应的vtable</font>,vptr的设置由编译器全权负责，程序员无须关心</li>
</ul>
</li>
<li><font color="blue">RTTI</font> : 一般来说，每一个class相关联的type_info object通常也保存在vtable的第一个slot中</li>
<li>需要清楚明白的一个概念: <u>一个vtbl对应一个class,一个vptr对应一个class object</u></li>
<li><font color="blue">引入继承后对象模型成本</font>:<ul>
<li>普通继承 : 父对象被直接包含在子对象中,这样对父对象的存取也是直接的,没有额外成本;</li>
<li>虚拟继承 : 父对象会由一个指针指出来，这样对于父对象的存取就多了一层间接性,必须由一个指针来访问，这样就添加了一次间接的额外成本</li>
</ul>
</li>
<li><font color="blue">C++优先判断一个语句为声明</font> : 当语言无法区分一个语句是声明还是表达式时，就需要用一个超越语言范围的规则——C++优先判断为声明</li>
<li><font color="blue">struct和class关键字的意义</font> :<ul>
<li>它们之间并无本质区别,<u>更多的是概念和编程思想上的区别</u>。</li>
<li>struct 用来表现那些只有数据的集合体POD(Plain OI’ Data)、而class则希望表达的是ADT(abstract data type)的思想;</li>
<li>两个关键字本质是无区别的,所以class并没有必须引入,但是引入它的确非常令人满意，因为这个语言所引入的不止是这个关键字,还有它所支持的封装和继承的哲学;</li>
<li>struct方便C程序员迁移到C++</li>
</ul>
</li>
<li><font color="fuchsia">C++只保证处于同一个access section 的数据,一定以声明的次序出现在内存布局当中</font>。</li>
<li><font color="blue">与C兼容的内存布局</font> : <u>组合,而非继承,才是把C的C++结合在一起的<strong>唯一可行</strong>的方法</u><ul>
<li>只有使用组合时,才能保证C拥有相同的内存布局,使用继承的内存布局是不受C++ standard 所保证的</li>
</ul>
</li>
<li><font color="blue">C++ 支持三种形式的编译风格(programming paradigm)</font> : <ul>
<li><font color="blue">面向过程的风格</font> : 就像C一样, 一条语句接一条语句的执行或者函数跳转;</li>
<li><font color="blue"><strong>基于对象</strong>的风格(object-based)</font> (或称ADT) : <u>仅仅使用了class封装</u>,很多人都是在用基于对象的风格却误以为自己在使用面向对象的风格;</li>
<li><font color="blue"><strong>面向对象</strong>的风格(object-oriented)</font> : <u>使用了class的封装和多态</u>的编程思维(多态才是真正的面向对象的特征)。</li>
<li>纯粹以一种paradigm写程序,有助于整体行为的良好稳固。</li>
</ul>
</li>
<li>C++ 支持多态的方法 : <ul>
<li>经由一组隐式的转化操作,例如把derived class 指针转化为一个指向其public base type 的指针: <code> shape *ps = new circle() </code></li>
<li>经由 virtual function 机制 : ps-&gt;test();</li>
<li>经由 dynamic_cast 和 typeid 运算符 : if (circle <em>pc = dynamic_cast&lt;circle </em>&gt; (ps)) ….</li>
</ul>
</li>
<li>一个reference通常是以一个指针实现的，所以point和reference并没有本质的区别。</li>
<li><font color="blue">一个对象的内存布局大小(通常由3部分组成)</font>　:　<ul>
<li>其nonstatic data member 的总和大小;</li>
<li>任何由于位对齐所需要的填补上云的空间;</li>
<li>加上为了支持virtual 机制而引起的额外负担。</li>
<li>这也印证了前面的一个结论 : C++中的额外成本都是由于virtual 机制所引起的。</li>
</ul>
</li>
<li>指针的类型 : <ul>
<li>对于内存来说,不同类型的指针并没有什么不同。它们都是占用一个word的大小,包含一个数字,这个数字代表内存中的整体上地址;</li>
<li>感觉上,指针的类型是编译器的概念,对于硬件来,并没有什么指针类型的概念;</li>
<li>转型操作也只是一种编译器的指令,它改变的内是编译器对被内存的解释方式而已！</li>
</ul>
</li>
<li><font color="blue"> 多态只能由”指针”或”引用”来实现,根本原因在于 </font> : <ul>
<li><u>指针的引用(通常以指针来实现)的大小是固定的(一个word),而对象的大小却是可变的</u>。其类的指针和引用可以指向(或引用)子类,但是基类的对象也只能是基类，没有变化不可能引发多态。</li>
<li>一个point或reference绝不会引发任何”与类型有关的内存委托操作”,<u>在指针类型转换时会受到的改变的只有它们所指向内存的解释方式而已</u>。(例如指针绝不会引发slice,因为它们大小相同)</li>
</ul>
</li>
<li><u>在初始化、assignment等操作,编译器会保证对象的vptrs得到正确的设置。这是编译器的职责</u>。一般都是通过在各种操作中插入编译器的代码来实现的。</li>
</ol>
<h2 id="第二章-构造函数语意学"><a href="#第二章-构造函数语意学" class="headerlink" title=" 第二章 构造函数语意学 "></a><center> 第二章 构造函数语意学 </center></h2><p>详细的讨论了constructor如何工作,讨论构造一个对象的过程以及构造一个对象给程序带来的影响。</p>
<ol>
<li>C++中对于默认构造函数的解释为: <font color="red">默认的构造函数会在需要的时候被编译器产生出来</font>。<ul>
<li><font color="fuchsia">这里非常重要的一点是: 谁需要? 是<strong>程序的需要</strong>还是<strong>编译器的需要</strong> </font>？<ul>
<li><u>如果是程序的需要,那是程序员的责任;只有在是编译器的需要时,默认构造函数才会被编译器产生出来,而且被产生出来的默认构造函数只会执行编译器所需要的行动，而且这个产生操作只有在默认构造函数真正被调用时才会</u></li>
<li>例如成员变量初始化为0操作，这个操作是程序和需要,而不是编译器的需要。</li>
</ul>
</li>
</ul>
</li>
<li><font color="blue"> 区分trivial 和 notrivial </font> : <ul>
<li>只有编译器需要的时候,合成操作才是notrivial的, 这样的函数才会被真正的合成出来;</li>
<li><u>如果编译器不需要，而程序员又没有提供,这时的默认构造函数就是trivial的。虽然它在概念上存在，但是编译器实际上根本不会去合成出来</u>,因为他不做任何有意义的事情,所以当然可以忽略它不去合成。trivial的函数只存在于概念上，实际上不存在这个函数。</li>
</ul>
</li>
<li><font color="red"> 总结变量的初始化 : 只有全局变量和静态变量才会保证初始化</font>,其中静态变量可以视为全局变量的一种,因它静态变量也是保存在优变量的存储空间上的。<ul>
<li>Golbal objects 的内存保证会在程序激活的时候被清0;Local objects 配置于程序的堆栈中, Heap objects 配置于自由空间中,都不一定会被清0,它们的内容将是内存上次被使用后的痕迹</li>
</ul>
</li>
<li>类声明头文件可以被许多源文件所包含,如何避免合成默认构造函数、拷贝构造函数、析构函数、赋值拷贝操作符(4大成员函数)时不引起函数的重定义?<ul>
<li>解决方法是以inline的方式完成,如果函数太复杂不适合inline,就会合成一个excplicit non-inline static实体(static 函数独立于编译单元)</li>
</ul>
</li>
<li>如果class A内含一个或以上的member objects，那么A的constructor必须调用每一个member class的默认构造函数。<ul>
<li>具体方法是 : <u>编译器会扩张constructor,在其中安插代码使得在user code被调用之前先调用member objects 的默认构造函数(当然如果需要调用基类的默认构造函数,则放在基类的默认构造函数调用之后:基类构造函数-&gt;成员构造函数-&gt;user code)</u>。</li>
<li>C++ 要求以”member objects 在 class 中的声明次序”来调用各个constructors。这就是<font color="fuchsia">声明的次序决定了初始化次序</font>(构造函数初始化列表一直要求以声明顺序来初始化)的根本原因!</li>
</ul>
</li>
<li>带有virtual funcitons 的类的默认构造函数毫无疑问是nontrivial的,需要编译器安插额外的成员vptr并在构造函数中正确设置vptr,这是编译器的重要职责之一。<ul>
<li>带有virtual base class 的类的默认构造函数同样也是毫无疑问的nonstatic的,编译器需要正确设置相关的信息以僵持这些virtual base class 的信息能够在执行时准妥当,这些设置取决于实现虚基类的手法。</li>
</ul>
</li>
<li>编译器有4种情况会使得编译器真正为class生成nontrivial的默认构造函数,这个<u>nontrivial的默认构造函数只满足编译器的需要(调用member objects 或base class 的默认构造函数、初始化virtual funciton 或 virtual base class 机制)</u>。其它情况时,类在概念上拥有默认构造函数,但是实际根本不会被产生出来(前面的区分)</li>
<li><font color="red">C++新手常见的两个误区</font><ul>
<li><u>ERROR : 如果class 没有定义default constructor 就会合成一个</u>;<ul>
<li>首先定义了其它constructor就不会合成默认构造函数,其次即使没有定义任何函数也不一定会合成default constructor,可能仅仅是概念上有, 但实际上不合成出来。</li>
</ul>
</li>
<li><u>ERROR : 编译器合成出来的默认构造函数会明确设定一个data member 的默认值;</u><ul>
<li>明显不会,区分了Golbal objects,stack objects,Heap objects 就非常明白了只有在Golbal 上的objects会被清0,其它的情况不会保证被清0。</li>
</ul>
</li>
</ul>
</li>
<li>Copy constructor 和默认构造函数来说,只有在必须的时候才会被产生出来,对于大部分的class 来说,拷贝构造函数仅仅需要按位拷贝就可以。<u>满足<font color="blue">bitwise copy semantics</font>的拷贝构造函数是trivial的，就不会真正被合成出来(与默认构造函数,只有nontrivial的拷贝构造函数才会被真正合成出来)</u>。<ul>
<li>对于大多数类按位拷贝就够了,什么时候一个class 不展现bitwise copy semantics呢? 有以下四种情况<ul>
<li>class内含一个member object而后者声明了(或者是由于nontrivial而被合成出来的)一个copy constructor时;</li>
<li>当class 继承自一个base class 而后者存在一个copy constructor时(不论显示定义或是合成而得的)</li>
<li>当class 声明了一个或多个virtual funcitons时; (vf 影响了位语意,进而影响了效率)</li>
<li>当class 派生自一个继承串链,其中一个或多个virtual base classes时。</li>
</ul>
</li>
</ul>
</li>
<li><font color="blue">NVR优化</font> : 编译器会把返回值作为一个参数传到函数内,比如:<ul>
<li><code>X foo() &#123;…&#125;</code> 会被更改(也可以手动做这个优化)为 : <code>void foo(X &amp; result) &#123;…&#125;</code>;</li>
<li>从使用者角度，用<code>return X(…)</code>代替 <code>X x;return x;</code>能够辅助这个优化操作。</li>
</ul>
</li>
<li><font color="blue">不要随意提供copy constructor</font>,<u>对于满足bitwise copy constructor的类来说,编译器自动生成的拷贝构造函数自动地使用了位拷贝(这是效率最高的)</u>,如果你自己随意提供copy constructor就会压抑掉编译器的这个行为,这还会影响效率。</li>
<li><font color="blue">成员初始化列表</font> : 在成员初始化列表背后发现的事情是什么?<ul>
<li>编译器会一一操作初始化列表,把其中<font color="red">初始化操作以member声明的次序</font>在constructor内安插初始化,并且在任何excplicit user code 之前。</li>
<li><u>“以member声明的次序来决定初始化次序”和”初始化列表中的排列次序”之间的<font color="red">外观错乱</font></u>,可能会导致一些不明显的Bug</li>
<li>不过GCC已经强制要求使用声明次序来进行初始化以避免这个陷阱。</li>
</ul>
</li>
<li>理解了初始化列表中的实际执行顺序中”以member声明的次序”来决定的,就可以理解一些很微妙的错误。比如:<ul>
<li><code>A() : i(99),j(66),value(foo()) &#123;…&#125;</code></li>
<li>int i, int j;</li>
<li>这会不会产生错误取决于成员函数foo()是依赖i还是j;</li>
<li>如果foo依赖于i,由于i声明在value之前,所以不会产生错误；</li>
<li>如果foo依赖于j,由于j声明在value之后,就产生了使用未初始化成员的错误。</li>
</ul>
</li>
</ol>
<h2 id="第三章-Data语意学"><a href="#第三章-Data语意学" class="headerlink" title="第三章 Data语意学 "></a><center>第三章 Data语意学 </center></h2><p>C++ 对象模型的细节,讨论data member的处理。</p>
<ol>
<li><u>空类也有1Byte的大小,因为这样才能使得这个class 的2个objects在内存中有独一无二的地址</u>。</li>
<li><font color="blue">一个对象内存布局大小(通常由3外部分组成)</font><ul>
<li>其nonstatic data member的总和大小;</li>
<li>任何由于位对齐所需要的填补上去的大小;</li>
<li>加上为了支持virtual机制而引起的额外负担;</li>
</ul>
</li>
<li>对member functions本身的分析会直到整个class 的声明都出现才开始。所以class的member funcitons可以引用声明在后面的成员,C语言就做不到。</li>
<li>和第3条对比，需要十分注意的一点是 : <font color="fuchsia">class 中的typedef并不具备这个性质</font>。<ul>
<li><u>因此，类中的typedef影响会受到函数与typedef的先后顺序的影响</u>。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">typedef int length;</span><br><span class="line">class Point3d &#123;</span><br><span class="line">  public:</span><br><span class="line">    void f1(length l) &#123;cout &lt;&lt; l &lt;&lt; endl; &#125;</span><br><span class="line">    typedef string length;</span><br><span class="line">    void f2(length l) &#123; cout &lt;&lt; l &lt;&lt; endl; &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
<li>这样f1绑定的length类型是int;而f2绑定的length类型才是string。</li>
<li><font color="fuchsia">所以，对于typedef需要防御性的风格: 始终把nested type声明(即typedef)放在class起始处！</font></li>
</ul>
</li>
<li>传统上,vptr被安放在所有被明确声明的member的最后,不过也有些编译器把vptr放在最前面(MSVC++ 就是把vptr放在最前面，而GCC是把vptr放在最后面).</li>
<li>在C++中，直观上来说,由一个对象存取一个member会比由一个指针存取一个member更快捷。但是对于经由一个对象来存取和一个指针来存取一个静态的member来说，是完全一样的，都会被编译器所扩展。</li>
<li>经由一个函数调用的结果来存取静态成员，C++标准要求必须对这个函数进行求值，虽然这个求值的结果并无用处,例如:<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">foobar().static_data &#x3D; 10;</span><br></pre></td></tr></table></figure>
<ul>
<li>foo()返回一个类型为X的对象，含有一个static_data，foobar()其实可以不用求值而直接访问这个静态成员，但是C++标准保证了foobar()会被求值，可能的代码扩展为:<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(void) foobar();</span><br><span class="line">X::static_data &#x3D; 10;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li>对一个nonstatic data member 进行存取操作,编译器会进行如下扩展:<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">origin._y &#x3D; 0.0;</span><br><span class="line"></span><br><span class="line">那么地址&amp;origin._y将等于:</span><br><span class="line"></span><br><span class="line">＆origin + (&amp;X::_y - 1);</span><br></pre></td></tr></table></figure>
<ul>
<li>注意其中的-1的操作，指向data member 的指针，其offset 值总是被加上1，这桩可以2使编译器系统区分”一个指针data member 的指针，用以指向class的第一个member”和”一个指向data member的指针,但是没有指向任何member”两种情况(<i>成员指针也需要有个表示NULL的方式，0相当于用来表示NULL了，其它的就都要加上1了</i>)。</li>
</ul>
</li>
<li>以下这两种写法有什么区别？<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">X x; x.x &#x3D; 0.0;</span><br><span class="line"></span><br><span class="line">*px &#x3D; &amp;x; px-&gt;x &#x3D; 0.0;</span><br></pre></td></tr></table></figure>
<ul>
<li>答案是:<u>X是一个派生类，而其继承结构有一个虚基类，且x成员又是蜡烛基类中的成员，这两种写法在编译器眼中就有巨大区别了</u>因为我们不能明确的说pt必然指向哪一个class type,故我们也就不知道编译时期member 真正的offset位置，存取操作只能延时到执行期。对于x来说类型固定，所以member的offset位置也在编译时期就确定了。</li>
</ul>
</li>
<li>派生类的成员和基类的排序并未在C++ standard中强制指定；理论止编译器可以自由安排，<u>但是对于大部分编译器实现来说，都是把基类成员放在前面，但是virtual base class 除外。(一般而言，任何一条规则一旦碰到virtual base class 就没辙了)</u>。</li>
<li><font color="red">C++ standard中保证： 出现在派生类中的base class subobject有其完整原样性！</font><ul>
<li>子类会被放在父类的对齐空白字节之后，因为父类的完整性必须得以保证，父类的对齐空白字节也是父类的一部分，也是不可分割的。</li>
</ul>
</li>
<li><font color="blue">支持多态所带来的4个负担</font>：<ul>
<li>导入virtual table 用来存放每一个virtual funcitons的地址。这个Table 的元素数目一般而言是被声明的virtual funcitons数目再加上一个或两个slots(用心支持RTTI)；</li>
<li>在每一个class object 中安插一个vptr指向相应vtable;</li>
<li>在constructor 中安插代码以正确设置vptr，让它指定class 所对应的virtual table;</li>
<li>在deconstructor 中安插代码以正确设置vptr，使它能够抹消”指向class 之相关virtual table”的vptr；</li>
</ul>
</li>
<li><font color="blue"><strong>单一继承</strong>并<strong>含有虚拟函数</strong>时的内存布局</font>(考虑把vptr放在尾部的情况)<br><img src="https://i.loli.net/2021/01/19/J9y5AXFnGpf1gTW.png" alt="blog1.png"><ul>
<li><u>单一继承时vptr被放在第一个子类的末尾，产生这样的布局的原因在于”<strong>基类的完整性必须在子类中得以保存</strong>“</u>。</li>
<li>对于第一个<strong>vptr</strong>Point2d的vptr可以这样理解，由Point2d而引发的vptr，在Point2d的对象中，这个<strong>vptr</strong>Point2d所指向的是与Point2d所指向的是与Point2d所对应的point2d_vtable,而在Point3d的对象中，这个<strong>vptr</strong>Point2d所指向的却是与Point3d所对应的point3d_vtable.</li>
</ul>
</li>
<li><font color="blue">多重集成时的布局</font>(多重继承时的主要问题在于派生类与非第一基类之间的转换)：<br><img src="https://i.loli.net/2021/01/19/fH6MdhvlIUk2KoO.png" alt="Blog_2.png"><ul>
<li>在多重继承的派生体系中，将派生类的地址转换为第一基类是成本与单继承是相同的，只需要修改地址的解释方式而已；而对于转换为第一基类的情况，则需要对地址进行一定的offset操作才可以。</li>
<li><u>C++ standard 并未明确base classes 的特定排列次序，但是目前的编译器都是按照声明的次序来安放他们的。</u>(有一个优化:如果第一基类没有vtable而后继基类有，则可能把它们调换位置).</li>
<li>多重继承中，可能会有多个vptr指针，视其继承体系而定:<font color="fuchsia">派生类中的ptr的数目等于所有基类的vptr数目的总和</font>。</li>
</ul>
</li>
<li><font color="blue">虚拟继承:<u>虚拟继承就是把一个类切割成两个部分：一个不变局域和一个共享局部。</u></font><ul>
<li>这个共享局部必须通过编译器安插的一些指针指向virtual base class object 来间接的存取，这样才能够实现共享。对于这个安插指针来实现共享的技术，有两种主流的做法：</li>
<li>一种做法就是直接使用一个指针指向虚基类:<br><img src="https://i.loli.net/2021/01/19/Zp1iUaex6qnI3XL.png" alt="Blog_3.png"></li>
<li>另一种做法就是在vtable 中放置virtual base class的offset:<br><img src="https://i.loli.net/2021/01/19/GPLfRBbtrNT36YW.png" alt="Blog_4.png"><ul>
<li><u>这种使用偏移地址的方式好处在于:vptr是已经存在的成本，而vtable是class的所有objects所共享的成本。对于每一个class object没有引入任何的额外成本，仅仅在vtable多存储了一个slot布局，而前一种方式却对每一个object都引入了两个指针的巨大成本。</u></li>
<li>这两种方式教师把虚基类放在内存模型中的最后面，然后借由一层间接性(指针或offset)来访问。</li>
</ul>
</li>
</ul>
</li>
<li>一般而言：<u>virtual base class 最有效的一种运用形式就是：一个抽象virtual base class ，没有任何data members</u>。</li>
<li>普通封装不会带来任何执行期的成本，编译器可以轻松优化掉普通封装带来的任何成本。<ul>
<li>但是一旦涉及到虚拟继承，效率就会大幅降低，在有n层的虚拟继承体系中，普通的访问就要经过n次间接，普通访问的成本就变为了n倍。</li>
<li>再次表示，C++中的额外成本基本都是由于virtual 机制引起的。</li>
</ul>
</li>
<li>指向Data Members 的指针内部实际保存的是这个data member 相对于对象起始地址的偏移地址(offset)(但需要另外加1以区分空指针，前面有讲过了)</li>
<li>使用指向Data Members的指针时也不会损失效率，成本与直接存取相同。就像第17条所说的，普通访问没有额外成本，但是遇到虚拟继承效率就大幅降低。</li>
</ol>
<h2 id="第四章-Function语意学"><a href="#第四章-Function语意学" class="headerlink" title="第四章 Function语意学 "></a><center>第四章 Function语意学 </center></h2><p>C++对象模型的细节，讨论了member funcitons,尤其是virtual funciton。</p>
<ol>
<li>C++的设计准则之一就是：<u>nonstatic member funciton 至少必须一般的nonmember funciton 有相同的效率</u>。<ul>
<li>实际上，nonstatic member funciton 会被编译器进行如下的转换，变成一个普通函数<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; non-const nonstatic member</span><br><span class="line">Type1 X::foo(Type2 arg1) &#123;....&#125; </span><br></pre></td></tr></table></figure></li>
<li>会被转换为如下普通函数(可能的内部转换结果):<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F;C++伪码</span><br><span class="line">void foo(X* const this,Type1&amp; _result，Type2 arg1) &#123;....&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li>如何确定函数是否为non-static?<ul>
<li>它是否可以直接存取nonstatic数据。</li>
<li>看这个函数是否被声明为const。</li>
</ul>
</li>
<li>实际上，<strong>普通函数，普通成员函数，静态成员函数到最后才会变成与C语言类似的普通函数</strong>，只是编译器在这些不同类型的函数身上做了不同的扩展，并放在不同的scope里面而已。</li>
<li><font color="blue">虚拟成员函数:ptr-&gt;normalize();</font><ul>
<li>会被内部转化为: ( * ptr-&gt;vptr[1]) (ptr);</li>
<li>事实上vptr的名称也会被”mangled”(名称切割重组)，因为对于一个复杂的派生体系，可能会有多个vptr。前面总结过了，一个派生类中的vptr数目等于其基类的总和。</li>
</ul>
</li>
<li>static member funciton 主要特性：<ul>
<li>不能直接存取其class 中的nonstatic members.</li>
<li>不能被声明为const、volatile或virtual.</li>
<li>不需要经由class object 才被调用，虽然大部分时候都是这样使用的。</li>
</ul>
</li>
<li>静态成员函数其实就是带有类scope的普通函数，它也没有this指针，所以它的地址类型并还是一个指向成员的指针而仅仅是一个普通的函数指针而已。 <ul>
<li>静态成员函数是作为一个callback的理想对象，要类的scope内，又是普通的函数指针。</li>
</ul>
</li>
<li>识别一个classs是否支持多态，唯一的适当的方法就是看它是否有任何的virtual funciton。只有class声明有任意一个virtual funciton，那么它就需要额外的执行期信息vtable.</li>
<li>对于单一继承的情况，每个类最多只会有一个vptr指针，并放在第一个拥有virtual function 的类的后面(父类必须保证对象的完整性)。<ul>
<li>在单一继承的内存布局下，virtual function是如何工作的呢？因为在调用一个成员函数ptr-&gt;z()时；<ul>
<li>虽然不能确定ptr直接指向的类型，但是可以经由ptr找到它的vtable，而vtable记录了所指对象的真正类型(一般对象的type_info 放在vtable的第一个slot中)</li>
<li>虽然不能确定应该调用的z函数的真正地址，但是可以知道vtable中被放在哪一个slot，于是就直接去vtalbe中相应的slot中取出真正的函数地址加以调用。<br><img src="https://i.loli.net/2021/01/20/YIbzDSUBqmXu7hO.png" alt="Blog_5.png"></li>
</ul>
</li>
</ul>
</li>
<li><font color="blue">多重继承下的内存布局</font>:<ul>
<li><u>要多重继承中比单一继承更复杂的地方在于对大是大非第一基类的指针和引用进行操作时，必须进行一些执行期的调整this指针的操作。</u></li>
<li>比如对于简单的delete 操作：delete base2；由于base2可能没有指向对象的起始地址，这样简单的删除操作都会引发巨大灾难，所以需要对base2做执行期的调整才能正确的delete对象。</li>
<li>在多重继承下，一个derived class 可能同时含有多个vptrs指针，这取决于它的所有基类的情况(基类完整性定义)。也可能有对应的多个vtables(如cfront)，但也可能无论如何只有一个vtable(把所有的vtables合成一个，并使得所有的vptrs都指向这一个合成的vtable+offset，如Sum的编译器)，这些都取决于编译器的策略。</li>
<li>有三种情况，非第一基类会影响对虚函数的支持：<ul>
<li>通过一个指向非”第一base class”的指针，调用derived class virtual function。<u>最后为了能够正确执行，ptr必须调整指向derived对象起始处。</u><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Base2 *ptr&#x3D;new Derived;</span><br><span class="line">&#x2F;&#x2F;调用Derived::~Derived()</span><br><span class="line">&#x2F;&#x2F;ptr必须向后调整sizeof(Base1)个byte</span><br><span class="line">&#x2F;&#x2F;因为非第一base class的地址指定操作，需要加上或减去介于中间的base class subobject(s)的大小</span><br><span class="line">delete ptr;</span><br></pre></td></tr></table></figure></li>
<li>这种情况是第一种情况的变形，通过一个”指向derived class“的指针，调用第二个base class中的一个继承而来的virtual function。<u>最终derived class 指针必须再次调整，经指向第二个base subobjec最终derived class 指针必须再次调整，经指向第二个base subobject。</u><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Derived *pder&#x3D;new Derived;</span><br><span class="line">&#x2F;&#x2F;调用Base2::mumble()</span><br><span class="line">&#x2F;&#x2F;pder必须向前调整sizeof(Base1)个byte</span><br><span class="line">pder-&gt;mumble();</span><br></pre></td></tr></table></figure></li>
<li>第三种情况发生在一个语言扩充性质下，允许一个虚函数的返回值类型有所变化，可能是base type,也可能是publicly derived type。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Base2 *pb1&#x3D;new Derived;</span><br><span class="line">&#x2F;&#x2F;调用Derived * Derived::clone()</span><br><span class="line">&#x2F;&#x2F;返回值必须调整。以指向Base2 对象</span><br><span class="line">Base2 *pb2&#x3D;pb1-&gt;clone;</span><br></pre></td></tr></table></figure>
<img src="https://i.loli.net/2021/01/21/TYQ6oBgksmZjfGE.png" alt="Blog_6.png"></li>
</ul>
</li>
</ul>
</li>
<li>虚拟继承下的内存布局：<br><img src="https://i.loli.net/2021/01/21/IRtcs7fzX3yPnhp.png" alt="Blog_7.png"><ul>
<li><font color="fuchsia">Lippman建议：不要在一个virtual base class中声明nonstatic data members。如果一定要这么做，那么你会距离复杂的深渊愈来愈近，终不可拔。</font></li>
</ul>
</li>
<li>这里的<u>函数性能测试表明，inline函数的性能如此之高，比其它类型的函数高的不是一个等级。</u>因为inline函数不只能够节省一般函数调用所带来的额外负担，也给编译器提供了程序优化的额外机会。</li>
<li>取一个nonstatic member function 的地址，如果该函数是nontrivial,则得到的结果是它在内存中的真正地址。然而这个值是不完全的，它需要被绑定于某个class object的地址上，才能够通过它调用该函数。</li>
<li><font color="blue">指向virtual member funcitons的指针</font>：<ul>
<li><u>对于一个virtual member function取其地址，所能获得的只是一个vtable的索引值</u></li>
</ul>
</li>
<li>inline函数扩展时的实际参数取代形式参数的过程，会聪明地引入临时变量来避免重复求值。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">假设函数：</span><br><span class="line">  inline int min(int i,int j) </span><br><span class="line">  &#123;</span><br><span class="line">    return i &lt; j ? i : j;</span><br><span class="line">  &#125;</span><br><span class="line">三种调用方式：</span><br><span class="line">  int minval;</span><br><span class="line">  int val1 &#x3D; 1024;</span><br><span class="line">  int val2 &#x3D; 2048;</span><br><span class="line"></span><br><span class="line">  minval &#x3D; min(val1,val2); &#x2F;&#x2F;方式一</span><br><span class="line">  minval &#x3D; min(1024,2048); &#x2F;&#x2F;方式二</span><br><span class="line">  minval &#x3D; min(foo(),bar()+1) &#x2F;&#x2F;方式三</span><br></pre></td></tr></table></figure>
<ul>
<li>方式一，由于是参数，所以可以直接代换</li>
<li>方式二，由于是常数，所以可能直接拥抱常量</li>
<li>方式三，会引发参数副作用，所以需要引入一个临时变量，用以避免重复求值。</li>
</ul>
</li>
<li>inline 中再调用inline函数，可能使得表面上一个看起来很平凡的inline却因连锁的复杂性而没有办法扩展开来。<ul>
<li>对于既要安全又要效率的程序，inline函数提供了一个强而有力的工具，然后与non-inline函数比起来，它们需要更加小心处理。</li>
</ul>
</li>
</ol>
<h2 id="第五章-构造、解构、拷贝语意学"><a href="#第五章-构造、解构、拷贝语意学" class="headerlink" title="第五章 构造、解构、拷贝语意学 "></a><center>第五章 构造、解构、拷贝语意学 </center></h2><p>C++对象模型的细节，讨论了class 的整个模型，一个对象的完整生命周期</p>
<ol>
<li><font color="blue">纯虚函数志可以被调用</font>，方式如下：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class A</span><br><span class="line">&#123;</span><br><span class="line">  public:</span><br><span class="line">    virtual ~A() &#123;&#125;</span><br><span class="line">    virtual void f() &#x3D; 0;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F;纯虚函数必须定义在类声明之外</span><br><span class="line">void A::f() &#123;std::cout &lt;&lt; &quot;pure virtual&quot; &lt;&lt; std::endl;&#125; </span><br><span class="line"></span><br><span class="line">class D : public A</span><br><span class="line">&#123;</span><br><span class="line">  public:</span><br><span class="line">    &#x2F;&#x2F;纯虚函数必须经由派生类显式的要求调用</span><br><span class="line">    virtual void f() &#123;A::f(); &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">  D d;</span><br><span class="line">  d.f();</span><br><span class="line">  return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>输出结果为”pure virtual”,这里需要注意几点：</li>
<li>纯虚函数不能在类的声明中提供实现，只能在类声明的外部来提供默认的实现；</li>
<li>基类的纯函数的默认实现必须由派生类显式的要求调用；</li>
<li>派生类不会自动继承这个纯虚函数的定义，如果派生类D未定义f()，那么A依然是一个抽象类型；</li>
<li>这种pure virtual function 实现的方案比较好的应用场景为：基类提供了一个默认的实现，但是不希望自动的继承给派生类作用，除非派生类明确的要求。</li>
<li>仍需要注意这个纯函数为析构函数的情况。C++语言保证继承体系中的每一个class object 的 deconstructor 都会被调用。所以编译器一定会扩展派生类的析构函数去显式地调用基类的原构函数。</li>
<li><u>另外一个重要的应用场景：有些情况下会把析构函数声明为纯虚。这时，必须为纯虚析构函数提供一个默认的实现。</u>否则，派生类的析构函数由于编译器的扩展而显式的调用基类的原构函数时会找不到定义。同时编译器也无法为已经声明为纯虚的析构函数生成一个默认的实现。</li>
</ul>
</li>
<li>虚函数中的const 哲学： 一个虚函数该不该被定义为const呢？<ul>
<li>一个虚函数在基类中不需要修改data member 并不意味着派生类改写它时一定不会修改data member.</li>
<li><font color="fuchsia">所以除非有十足的把握，一般就不声明为const</font>。</li>
</ul>
</li>
<li>Lippman 认为把所有的函数都声明为virtual function,然后再靠编译器的优化操作把大是大非必须的virtual invocation 去除，并还是好的设计观念。不过，Java和.NET很可能都是这么干的。</li>
<li>对象能从三个地方产生出来：Global内存、Local内存、Heap内存。</li>
<li><font color="blue">观念上，编译器会为每一个类产生4个函数</font><ul>
<li>trivial default constructor,trivial deconstructor,trivial copy constructor,trivial copy assignment operator</li>
<li>但是这仅仅是观念上的，trivial的函数不会被真正的产生出来。</li>
</ul>
</li>
<li>C和C++的又一个不同点，就是<font color="blue">C语言的临时性定义</font><ul>
<li>就像书上的Point global;这样的定义：</li>
<li>在C中会被视为一个”临时性定义”，可以在程序中出现多次，这些实例最后会被链接器折叠起来，最终留下一个实体</li>
<li>在C++中会被视为一个”完全定义”,所以只能出现一次，要想实现C一样的临时性语意，C++中必须把它声明为extern，即:exern Point global;</li>
</ul>
</li>
<li>写法差异:<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A *pa1 &#x3D; new A;</span><br><span class="line">A *pa2 &#x3D; new A();</span><br></pre></td></tr></table></figure>
<ul>
<li>这两种写法是存在一定差异</li>
<li>对于内置类型：加括号会初始化，不加括号不初始化</li>
<li>对于自定义类型：都会调用默认构造函数，所以加不加括号没什么区别。</li>
</ul>
</li>
<li>对于<font color="blue">可以视为POD的class</font>(没有声明构造函数、没有virtual等等)，就可以使用<font color="blue">POD结构特有的initialization list进行初始化</font>。<ul>
<li>Point p={2,3} ，在C++11中的initialization list 被大量使用。</li>
</ul>
</li>
<li>引入<font color="blue">virtual function 会给对象的构造、拷贝和析构等过程带来的负担</font>如下：<ul>
<li>constructor 必须被安插一些代码以便将vptr正确的初始化，这些代码需要被安插在任何base class constructor的调用之后，但必须在任何user code 的代码之前；</li>
<li>合成copy constructor 和copy assignment operator ,因为它们不再是trivial的了，它们必须安插代码以正确的设置vptr；</li>
</ul>
</li>
<li>C++ standard 要求尽量延迟nontrivial members 的实际合成操作，直到真正遇到其使用场合为止。</li>
<li><font color="blue">constructor 会被编译器安插大量的代码，一般而言编译器所做的扩充操作大约如下： </font><ul>
<li>初始化成员：使用member initialization list 或者调用默认构造函数；</li>
<li>在那之前，如果class object 有vptr，它们必须被正确的设置；</li>
<li>在那之前，所有的上一层的base class constructors 必须被调用，以base classes声明的顺序。使用member initialization list 晓雾调用默认构造函数，同时如果base class是多重继承下的大是大非第1基类，还需要调整this指针；</li>
<li>在那之前，所有的virtual base class constructor 必须被调用，从左到右，从深到浅。并同时设置好virtual base class 所需要使用的各种机制；</li>
<li>处理顺序为：<font color="fuchsia">virtual base classes -&gt; base class -&gt; vptr -&gt; member </font>。</li>
</ul>
</li>
<li>赋值运算符中切记要记得进行自我检查。</li>
<li><font color="red">虚拟继承时，共享基类必须由最底层的class 负责初始化操作</font> ： <ul>
<li>这是虚拟继承时非常重要的一点，共享基类的初始化操作必须由最底层的类来负责，中间层次的类调用这个共享类初始化的操作会被编译器所压抑掉。</li>
<li>考虑对于如下继承体系的类：编译器如何压抑非底层对共享基类的初始化操作呢？ 是通过对Point3d和Vertex的构造安插一个安插一个额外的参数( _most_derived )来解决的。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">Point3d* Point3d::Point3d (Point3d *this, bool _most_derived,float x,</span><br><span class="line">                                                      float y,float z)</span><br><span class="line">&#123;</span><br><span class="line">  if (_most_derived !&#x3D; false)</span><br><span class="line">    this-&gt;Point::Point(x,y);</span><br><span class="line">  this-&gt;_vptr_Point3d &#x3D; _vtbl_Point3d;</span><br><span class="line">  this-&gt;_vptr_Point3d_Point &#x3D; _vtbl_Point3d_Point;</span><br><span class="line">  this-&gt;_z &#x3D; rhs._z;</span><br><span class="line">  return this;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Vertex3d* Vertex3d::Vertex3d(Vertex3d *this,bool _most_derived, float x,</span><br><span class="line">                                                        float y,float z)</span><br><span class="line">&#123;</span><br><span class="line">  if (_most_derived !&#x3D; false)</span><br><span class="line">    this-&gt;Point::Point(x,y);</span><br><span class="line">  this-&gt;Point3d::Point3d(false,x,y,z);</span><br><span class="line">  this-&gt;Vertex::Vertex(false,x,y);</span><br><span class="line">  return this;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>当Point3d是作为最底层来构造时，_most_derived参数针被设置为true，于是Point的构造函数就会被调用；当Point3d的构造函数是被Vertex3d间接调用时，_most_derived参数会被设置为flase，于是调用Point构造函数的操作就被压抑掉了。</li>
<li>这种由最底层类来负责初始脂共享基类的手法貌似有一点不优雅，但是这却是共享基类唯一可能正确的确定初始化的地方。</li>
</ul>
</li>
<li>在构造函数中调用virtual function 是没有多态性的，因为在构造函数中，对象还不完整，派生类的部分还没有开始构造，当然不能调用它们的成员函数，否则在它们的成员函数中可能会访问还不存在的成员变量。<ul>
<li>由于在构造函数中没有多态性，所以侧重了一种在构造函数中清0，再提供一个init()进香真正的初始化的保护性手法。</li>
</ul>
</li>
<li>要保证在构造函数中没有多态性，虚拟机制就必须知道一个调用操作是否来源于构造函数之中，变量如何实现的呢？<ul>
<li>编译器在构造函数中安插代码时会保证:<u>行调用所有基类的构造函数，再设置vptr,然后再调用member initializaiton操作。</u>变量构造函数中没有多态性的根本原因。</li>
<li>在任何User code 和 member initializaiton被调用之前，vptr被正确的设置为了当前类的类型，于是在调用virtual function时从vtable 中取出来的函数地址就是正确的当前类的成员方法地址。</li>
</ul>
</li>
<li>一个热爱容易犯的错误：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">struct A : public Base &#123;</span><br><span class="line">  A() : Base(foo()) , valueA(10) &#123;&#125;</span><br><span class="line">  int foo() &#123;return valueA;&#125;</span><br><span class="line">  int valueA;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>使用派生类的成员方法去初始化基类，注意在这个时候派生类还没有开始构造，调用它的成员方法的行为当然是未定义的！</li>
</ul>
</li>
<li>一个class 的默认copy assignment operator ,以下情况不会表现出bitwise copy 语意：<ul>
<li>当含有一个或以上的成员有copy assignment operator 时；</li>
<li>当基类有copy assignment operator时；</li>
<li>当class 中有virtual function 时(需要正确设置vptr);</li>
<li>当class 的继承体系中有virtual base class 时。</li>
</ul>
</li>
<li><font color="blue">C++语言中的虚拟继承时copy assignment operator弱点</font>：<ul>
<li>C++ 标准没有规定在虚继承时copy assignment operator 中是否会多次调用共享基类的copy assignment operator 。这样就有可能造成共享基类被赋值多次，造成一些错误，所以程序员应该在使用了virtual base class 时小心检验copy assignment operator 里的代码(经确保这样的多次赋值没有问题或者查看编译器是否已经提供了解决方案)。</li>
<li>因此，飞翔可能不要允许一个virtual base class 的拷贝操作，甚至根本就不要在任何virtual base class 中声明数据。</li>
</ul>
</li>
<li><font color="blue">C++隐式生成的4大成员函数，在不是真正需要的情况下都不要自己去声明。</font><ul>
<li>因为如果是trivial的，这些函数不会被真正的合成出来(只存在于概念上)，当然也就没有调用的成本了，去提供一个trivial的成员反而是不符合效率的。</li>
</ul>
</li>
<li><font color="blue">析构函数的执行顺序</font><ul>
<li>如果object 内带有vptr，那么首先重设相关的vtable；</li>
<li>deconstructor 函数本身现在会被执行，<u>也就是说vptr会在程序员的代码执行之前被重设；</u></li>
<li>以声明顺序的相反顺序调用members的析构函数；</li>
<li>如果有任何直接的(上一层)nontrivial base classed 拥有deconstructor，那么会以其声明顺序的相反顺序被调用；</li>
<li>如果有任何virtual base classes 拥有 deconstructor ，而当前讨论的这个class是最尾端，那么它们会以原来的构造顺序的相反顺序被调用。</li>
</ul>
</li>
<li><u>由于析构函数中的重设vptr会在任何代码之前被执行</u>这样就保证了<font color="fuchsia">要析构函数中也不具有多态性</font>，从而不会调用子类的函数。因为此时子类已经不完整了，子类的成员已经不存在了，而子类的函数有可能需要使用这些成员。</li>
<li><font color="red">构造函数和析构函数中都不具有多态性：这并不是语言的弱点，布是正确的语意所要求的</font>(因为那个时候的对象不完整)。</li>
</ol>
<h2 id="第六章-执行期语意学"><a href="#第六章-执行期语意学" class="headerlink" title="第六章 执行期语意学 "></a><center>第六章 执行期语意学 </center></h2><p>讨论执行期的对象模型的行为，包括临时对象的生命周期和new、delete运算符的行为。</p>
<ol>
<li>C++中过多的隐式变换有时候不太容易从程序代码中看出来表达式的复杂度。</li>
<li><font color="fuchsia">C++保证：全局变量会在第一次用到之前构造好，在main()结束之前原构掉。</font></li>
<li>C++程序中所有的Global object 都旋转在程序的data segment 中并清０，但是它的constructor要程序激活时才会被调用。</li>
<li><u>Lippman建议不要使用那些需要使用静态初始化的global object (Google C++编程规范也是如此建议的) 。</u></li>
<li>现在的C++ standard 已经强制要求局部静态在第一次使用时才被构造出来。<ul>
<li>这也是Effiective C++中Singleton 手法所利用的。而且在程序结束时会被以构造的相反次序被摧毁。</li>
</ul>
</li>
<li><font color="blue">对象的数组是通过编译器安插一个函数调用的代码来实现的</font>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">void* vec_new(void *array,  &#x2F;&#x2F;address of start of array</span><br><span class="line">        size_t elem_size,   &#x2F;&#x2F;size of each class object</span><br><span class="line">        int elem_count,     &#x2F;&#x2F;number of elements in array</span><br><span class="line">        void (*constructor) (void*),  &#x2F;&#x2F;构造函数的指针</span><br><span class="line">        void (*destructor) (void* , char)   &#x2F;&#x2F;析构函数的指针</span><br><span class="line">        )</span><br><span class="line">&#123;&#125;</span><br><span class="line"></span><br><span class="line">void * vec_delete (void *array    &#x2F;&#x2F; address of start of array</span><br><span class="line">        size_t elem_size,         &#x2F;&#x2F;size of each class object</span><br><span class="line">        int elem_count,           &#x2F;&#x2F;number of elements in array</span><br><span class="line">        void (*destructor) (void* , char)</span><br><span class="line">        )</span><br><span class="line">&#123;&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>由于把数组的声明转换为vec_new的函数调用，产生的问题是构造函数是通过指针调用的，因此无法使用任何参数，默认参数也不行。</li>
<li>对于那些声明了默认参数从而实际上拥有无参构造函数的类，编译器会产生整体上绝对无参的构造函数，再从这个构造函数里调用这个默认参数的构造函数。(这样，编译器实际上违反了语言的函数，拥有了２个没有参数的构造函数，但是这样的特例只能由编译器自己来违反)</li>
<li>这里之所以传入了析构函数的指针，是为了在构造函数抛出异常时，把已经构造好的对象给原构掉，这是vec_new义不容辞的任务。</li>
</ul>
</li>
<li><font color="blue">new 的两步曲</font><ul>
<li>分配内存；</li>
<li>调用构造函数；</li>
</ul>
</li>
<li><font color="blue">delete 的两步曲</font><ul>
<li>调用析构函数</li>
<li>释放内存</li>
</ul>
</li>
<li>一般的library 对new　运算符的实现：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">extern void* operator new (size_t size) &#123;</span><br><span class="line">  if (size &#x3D;&#x3D; 0)</span><br><span class="line">    size &#x3D; 1;</span><br><span class="line">  void *last_alloc;</span><br><span class="line">    while (!(last_alloc &#x3D; malloc(size))) &#123;</span><br><span class="line">      if (_new_handler)</span><br><span class="line">        (*_new_handler) ();</span><br><span class="line">      else </span><br><span class="line">        return 0;</span><br><span class="line">    &#125;</span><br><span class="line">    return last_alloc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>有2个精巧之处。第一：new 操作符至少会返回１个字节的内存；第二：_new_handler会给予内存分配不足时以补救的机会。</li>
</ul>
</li>
<li>虽然C++ standard并没有规定，但是实际上的new运算符都是以C malloc()完成；同样delete 运算符也都是以C free()完成的。</li>
<li><font color="blue">trivial的vec_new()</font>:<ul>
<li>如果要分配的数组的类型并没有默认构造函数，那么这个vec_new()的调用就是trivial的,完全可以仅仅分配内存就可以了，new操作符(注意区分new操作符和new运算符)足以胜任这个任务。只有在定义了默认构造函数时，vec_new才需要被调用起来。</li>
</ul>
</li>
<li>delete 和delete[]<ul>
<li><u>寻找数组维度给delete运算符带来了效率上的影响，所以出现了这个妥协。只有在”[]”出现时，编译器才会寻找数组的维度，否则它就假设只有一个object需要被删除</u>。</li>
<li>delete 数组时，只有第１个元素会被删除；</li>
<li>delete[] 单个对象时，１个元素都不会被删除，没有任何析梦函数被调用。</li>
</ul>
</li>
<li><u>数组的大小会被编译器记录在某个地方</u>，所以编译器能够直接查询出来某个数组的大小。</li>
<li><font color="red">数组和多态行为和天生不兼容性</font>：<ul>
<li>永远不要把数组和多态扯到一起，他们天生是不兼容。<u>当你对一个指向派生类的基类指针进行delete[] pabse ; 操作时，它是不会有正确的语意的</u>。</li>
<li>这是由于delete[]实际上会使用vec_delete()类似的函数调用代替，而在vec_delete()的参数中已经传递了元素的大小，在vec_delete()中的迭代删除时，会在删除一个指针之后将指针向后移动item_size个位置，如果DerivedClass 的size比BaseClass要大的话(通常都是如此)，指针就已经指向一个未知区域国(如果Derived与Base大小相同，那碰巧不会发生错误，delete[]可以正确的执行)。</li>
</ul>
</li>
<li><font color="blue">placement operator new 应该与 placement operator delete 搭配使用</font>，也可以在placement operator new出来的对象上显式的调用它的析构函数使得原来的内存又可以被再次使用。<ul>
<li>一般而言，placement operator new并不支持多态，因为Derived Class往往比Base class 要大，已经存在的类型为内存并不一定能够容纳Derived类型的对象。</li>
</ul>
</li>
<li><font color="blue">一段比较晦涩隐暗的代码</font>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Base &#123;</span><br><span class="line">  public:</span><br><span class="line">    virtual ~Base() &#123;&#125;</span><br><span class="line">    virtual void f() &#123;cout &lt;&lt; &quot;f in Base&quot; &lt;&lt; endl;&#125;</span><br><span class="line">    int value;</span><br><span class="line">&#125;;</span><br><span class="line">class Derived : public Base &#123;</span><br><span class="line">  public:</span><br><span class="line">    virtual void f() &#123;cout &lt;&lt; &quot;f in Derived&quot; &lt;&lt; endl;&#125;</span><br><span class="line">&#125;;</span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">  Base b;</span><br><span class="line">  b.f();    &#x2F;&#x2F;这个调用很明显</span><br><span class="line">  b.~Base();</span><br><span class="line">  new (&amp;b) Derived;</span><br><span class="line">  b.f();    &#x2F;&#x2F;这个调用就不太明显</span><br><span class="line">  return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>大部分人认为这里应该输出”f in Derived”，但实际上GCC输出的是”f in Base “。如果理解了前面的编译器如何扩展函数调用，就会明白”f in Base”才是正确的。因为b是一个对象而还是指针或者引用不具有多态性，所以编译器会：<strong>把b.f()直接扩展为Base::f(&amp;b);</strong></li>
<li>因此，可以想象，如果把b换成是Base * 类型，则由于指针会引发多态，所以才调用Derived的f()函数：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">  Base b &#x3D; new Base();</span><br><span class="line">  b-&gt;f();</span><br><span class="line">  b-&gt;~Base();</span><br><span class="line">  new (b) Derived;</span><br><span class="line">  b-&gt;f();</span><br><span class="line">  return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>这次，GCC输出了” in Derived”.</li>
</ul>
</li>
<li><font color="fuchsia">C++ standard允许编译器对临时性对象的产生胡完全的自由度。</font></li>
<li><font color="blue">临时对象的摧毁时机</font>：<ul>
<li><u>摧毁临时对象应该在产生它的完整的表达式的最后一个步骤</u>。切记是完整的表达式，比如一连串的逗号或一堆的括号，只有在完整的表达式最后才能保证这个临时对象在后面不会再被引用到。</li>
</ul>
</li>
<li>如果一个临时性对象被绑定于一个reference,对象将残留，直到被初始化之reference 的生命结束，或者直到临时对象的生命范畴(scope)结束——视哪一种情况先到达而定。</li>
<li>总结：临时性对象的确在一些场合、一定程度上影响了C++的效率。但是这些影响完全可以通过良好的编码和编译器的积极优化而解决掉临时性对象带来的问题(至少在很大的程度上)，所以对临时性对象的影响不能大意但也不必太放在心上。</li>
</ol>
<h2 id="第七章-站在对象模型的尖端"><a href="#第七章-站在对象模型的尖端" class="headerlink" title="第七章 站在对象模型的尖端 "></a><center>第七章 站在对象模型的尖端 </center></h2><p>讨论了C++的三个著名扩展:template,exception handing,RTTI。</p>
<ol>
<li>编译器在看到一个模板的声明时会做出什么反映呢？<ul>
<li>实际上编译器没有任何反映！编译器的反映只有在真正具现化时才会发生。</li>
<li>明白了这个，就明白了为什么在模板内部有明显的语法错误，编译器也不会报错，除非你要具现化出这个模板的——实体时编译器才会发出抱怨。</li>
<li>在这点上，似乎GCC做的比MSCV++要好的多。GCC好像会做完的解析，但是除了类型的检验；而MSVC++似乎就是放任不管，只有在具现化的时候才去检查。</li>
<li>在学习了C++ template 就明白了，编译器实际上会做<font color="blue">二阶段查找</font>而且这种延迟到实例化时的具体行为是：<u>延迟定义，而不是声明</u>。</li>
</ul>
</li>
<li><u>声明一个模板类型的指针是不会引起模板的具现化操作的，因为仅仅声明指针不需要知道class的实际内存布局</u>。</li>
<li>只有在某个member function 真正被使用时，它才会被真正的具现化出来，这样的延迟具现化至少有２个好处：<ul>
<li>空间和时间上的效率；</li>
<li>如果使用的类型并不完全支持所有的函数，但是只需要不去用那些不支持函数，这样的部分具现化就能得以通过编译。</li>
</ul>
</li>
<li>int 和 long 的一致性：int 和 long 在大多数的机器上都是相同的，但是如果编译器看到如下声明：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Point&lt;int&gt; p1;</span><br><span class="line">Point&lt;long&gt; p2;</span><br></pre></td></tr></table></figure>
<ul>
<li>目前的所有编译器都会具现化２个实体。</li>
<li>可以想象，编译器用一些mangling的手法把具现出来的２个实体分别叫做：_Point_int,_Point_Long之类的东西。</li>
</ul>
</li>
<li>涉及Template时的错误检查太弱了，template中那些与语法无关的错误，程序员可能认为十分明显，编译器却放它通过了，只有特定的实体具现化时，编译器才发出抱怨，这是目前实现技术上的一个大问题(二阶段查找的必然结果)。</li>
<li><font color="blue">Template中的名称决议方式:scope of the template definition(定义模板的地方)和scope of the tempalate instantiation (具现出模板实体的地方)</font>。示例如下：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F;scope of the template definition</span><br><span class="line">extern double foo (double);</span><br><span class="line">template &lt;class type&gt;</span><br><span class="line"></span><br><span class="line">class ScopeRules</span><br><span class="line">&#123;</span><br><span class="line">  public:</span><br><span class="line">    void invariant() &#123;    &#x2F;&#x2F;情况１</span><br><span class="line">      _member &#x3D; foo(_val);</span><br><span class="line">    &#125;</span><br><span class="line">    type type_dependent() &#123;   &#x2F;&#x2F;情况２</span><br><span class="line">      return foo (_member);</span><br><span class="line">    &#125;</span><br><span class="line">  private:</span><br><span class="line">    int _val;</span><br><span class="line">    type _member;</span><br><span class="line">&#125;;</span><br><span class="line">&#x2F;&#x2F;scope of the template instantiation</span><br><span class="line">extern int foo(int);</span><br><span class="line">ScopeRules&lt;int&gt; sr0;</span><br></pre></td></tr></table></figure>
<ul>
<li>Template中，对于一个<font color="fuchsia">nonmember name</font>的决议结果是根据这个name的使用是否”用以具现出该template的参数类型”有关而决定的。</li>
<li>当时觉得这样的规则很诡异，然后在学习了C++ Template 之后就很清晰的明白了。这是因为前面一个是非依赖名称；而后面的使用是依赖名称，所以会在不同的时机进行查找(<font color= "blue">二阶段查找</font>)。<ol>
<li>情况１：如果其使用互不相关，那就以scope of template declaration 来决议name；</li>
<li>情况２：如果其使用互有关系，那就以scope of tmeplate declaration 来决议name；</li>
<li>这个看似很诡异的规则，实际上是非常必要的！这给予了一个调用者可以进行自定义的机会。模板的使用者往往可以在使用时，根据具体的调用类型来提供一个更好的函数给模板(就像示例中，提供一个完全符合int类型的函数，可以视为一个更好的函数)。</li>
<li>与参数无关的调用，就是站在模板设计者的角度来看，所以然就使用scope of Template declaration;而与参数相关的调用，就是站在模板使用者的角度来看，当然也就使用scope of the template instantiation.</li>
<li>还需语意一点的就是：<u>这里依据是否与类型相关而决定使用哪一个scope，然后其中搜寻适当的name</u>.示例中的代码，在调用sr0.type_dependent();时，由于使用了scope of the Template instantiation，使得２个foo()函数同时成为备选函数，但是由于foo(int)更加的符合，所以最后才决议使用foo(int)这个版本。如果sr0是ScopeRules<double>类型的话，最后调用的依然是foo(double)那个版本。</li>
</ol>
</li>
</ul>
</li>
<li>编译器维持了２个scope contexts:<ol>
<li>scope of Template declaration : 用以专注一般的template class;</li>
<li>socpe of template instantiation : 用以专注于特定的实体；</li>
</ol>
<ul>
<li>这种关联性不能简单的作用一个宏扩展来重现，是一种很新奇的关联。</li>
</ul>
</li>
<li><font color="blue">一种具现化的策略</font>：先不具现任何的member function ，链接器会登记缺少哪些函数的定义，然后再重新调用编译器把编译器把登记重写编译出来，最后在把这些缺乏的定义重写编译出来，最后在把这些缺乏的定义和以前的链接结果链接起来堪最后的可执行文件或者库。</li>
<li><font color="blue">如果vtalbe被具现出来，那么每一个virtual function 也都必须被具现。</font><ul>
<li>这就是为什么C++ standard 中有如下的描述：”如果一个虚函数被具现出来，其具现点紧跟在其class 的具现点之后”。(也就是说，virtual function是一口气被具现出来的)</li>
</ul>
</li>
<li>一般而言，exception handing 机制需要与编译器所产生的数据结构以及执行期的一个excplicit library紧密合作而实现。</li>
<li><u>编译器为了支持异常的机制，又需要把程序员的代码进行大量的扩展才能保证异常机制的正确执行。</u>但是处理这些问题是编译器的责任，不过程序员应该明白这里把做的事情以及有可能付出的代价。</li>
<li><font color="red">以值类型抛出，以引用类型进行捕获</font>：<ul>
<li><u>被抛出的异常类型，一定会被先复制一份，真正被抛弃的实际上是这份复制器</u>；</li>
<li>即使是以值类型来进行捕获异常也可以捕获该值类型和其派生类的异常，但是在catch语句中会引发切割。</li>
</ul>
</li>
<li>对于每一个被丢出的exception,编译器必须产生一个类型描述器，对exception类型进行编码。如果那是一个derived type,则编码内容还必须包括所有base class 类型信息。</li>
<li>当一个exception被丢出时，<u>exception object 会被产生出来并通常旋转在相同形式的exception数据堆栈中。从throw 端传染给catch子句的是exception object 的地址、类型描述器(或是一个函数指针，该函数会返回该exception type 有关的类型描述器对象)，以及有可能还有的exception object 的析构函数的地址(如果有的话)</u></li>
<li>只有在一个catch子句评估完毕并且知道它不会丢出exception之后，真正的exception object 才会被摧毁。</li>
<li>支持异常机制的代价：<font color="fuchsia">与其它语言特征相比较，C++编译器支持ＥＨ机制所付出的代价最大。</font><ul>
<li><u>C++对异常机制所付出的代价大概为：空间10%、时间5%</u>。不算小，但也不是不可以接受吧。有一个问题：如果编译器开启了异常支持，但是在某一段未使用异常的代码中，也会为编译器开启了异常支持而付出代价吗？</li>
</ul>
</li>
<li>在C++中，一个具备多态性质的class，就是指賖virtual functions 的类(直接声明或者继承而来的)。</li>
<li>由于具备多态性质的class都已经含有一个vptr指向vtable了，C++把类型信息放在vtable 的第１个slot中(一个type_info的指针指向一个表示当前类型的type_info对象)，从而几乎没有付出代价的支持RTTI(1byte per class,not 1byte per class object)(中文书有误)<ul>
<li><u>由于RTTI所需要的信息放在vtable中，自然的：</u><font color="fuchsia">只有含有vptr的类才支持RTTI</font>.</li>
</ul>
</li>
<li>有了RTTI机制的支持，就可以实施保证安全的动态转型操作dynamic_cast&lt;&gt;();</li>
<li><font color="blue">在dynamic_cast中使用指针和引用的区别在于当转型失败时：</font><ul>
<li>指针版本会返回0,使用者需要进行检查；</li>
<li>引用的版本会抛出一个bad_exception(因为没有空引用啊)；</li>
<li>这两个机制各有用处吧，视需而用。</li>
</ul>
</li>
<li>type_info类型copy构造函数和operator=操作符都被声明为私有，禁止了赋值和拷贝操作。而且只提供了一个受保护的带有一个const char * 参数的构造函数，因为不能直接得到<u>type_info 对象，只能通过typeid()运算符来得到这类对象</u>。</li>
<li>RTTI只适用于多态类型(RTTI信息在于vtable的原因)，事实上type_info object 也适用于非多态类型。typeid()作用于多态类型时的差异在于，这时候type_info object 是静态取得的(编译器直接给扩展了)，而非像多态类型一样在执行期通过vtable 动态取得。可以通过这个例子来理解：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">struct A&#123;&#125;</span><br><span class="line">struct B : public A&#123;&#125;;</span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">  A *pa &#x3D; new B;</span><br><span class="line">  cout &lt;&lt; typeid(pa).name() &lt;&lt; endl;</span><br><span class="line">  cout &lt;&lt; typeid(*pa).name() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line">将输出：</span><br><span class="line">  struct A *</span><br><span class="line">  struct A</span><br></pre></td></tr></table></figure>
<ul>
<li>这没有检测出pa所指的真正类型，原因就在于<u>typeid运算符用在非多态类型上时，会被编译器在编译器静态的扩展了</u>。也许是类似的扩展：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cout &lt;&lt; typeid(pa).name() &lt;&lt; endl;   &#x3D;&gt; typeid(A*).name()</span><br><span class="line">cout &lt;&lt; typeid(*pa).name() &lt;&lt; endl;  &#x3D;&gt; typeid(A).name()</span><br></pre></td></tr></table></figure></li>
<li>如果给struct A添加一个虚拟函数，从而<u>使得类型Ａ和Ｂ都变成多态类型，于是typeid运算符就会在运行期间动态的去获取它们的真正类型了</u><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">struct A</span><br><span class="line">&#123;</span><br><span class="line">  virtual ~A() &#123;&#125;   &#x2F;&#x2F;A包含了一个虚函数，从而把A变成了多态类型</span><br><span class="line">&#125;;</span><br><span class="line">struct B : public A&#123;&#125;;    &#x2F;&#x2F;B从A继承一个虚函数，所以也是多态类型</span><br><span class="line">int main() </span><br><span class="line">&#123;</span><br><span class="line">  A *pa &#x3D; new B;</span><br><span class="line">  cout &lt;&lt; typeid(pa).name() &lt;&lt; endl;</span><br><span class="line">  cout &lt;&lt; typeid(*pa).name() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line">将输出：</span><br><span class="line">  struct A*</span><br><span class="line">  struct A</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li>效率和弹性始终是矛盾体！</li>
</ol>
]]></content>
      <categories>
        <category>Read Notes</category>
      </categories>
      <tags>
        <tag>theory</tag>
      </tags>
  </entry>
  <entry>
    <title>NRV优化</title>
    <url>/2021/03/19/NRV%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<h1 id="导言"><a href="#导言" class="headerlink" title="导言"></a>导言</h1><p>&emsp;&emsp;之前在阅读候捷老师的《深入探索C++对象模型》一书时，对于NRV优化操作这个编译器优化并还是特别的清楚。所以今天准备通过实际测试来深入了解一下这个NRV的编译器优化。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#include &lt;iostream&gt;</span><br><span class="line">class Edge&#123;</span><br><span class="line"> public:</span><br><span class="line">  Edge() &#123;</span><br><span class="line">    std::cout &lt;&lt; &quot;Edge construct!&quot; &lt;&lt; std::endl;</span><br><span class="line">    Point &#x3D; 0;</span><br><span class="line">  &#125;</span><br><span class="line">  ~Edge() &#123; std::cout &lt;&lt; &quot;Edge destruct！&quot; &lt;&lt; std::endl; &#125;</span><br><span class="line">  Edge(const Edge&amp;_result) &#123;</span><br><span class="line">    std::cout &lt;&lt; &quot;Copy Construct!&quot; &lt;&lt; std::endl;</span><br><span class="line">    Point &#x3D; _result.Point;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"> private:</span><br><span class="line">  int Point;</span><br><span class="line">&#125;;</span><br><span class="line">Edge get() &#123;</span><br><span class="line">  Edge point;</span><br><span class="line">  return point;</span><br><span class="line">&#125;</span><br><span class="line">int main() &#123; Edge point &#x3D; get(); &#125;</span><br></pre></td></tr></table></figure><br>&emsp;&emsp;接下来我们将通过这个例子来深入了解NRV优化</p>
<h1 id="不用NVR优化"><a href="#不用NVR优化" class="headerlink" title="不用NVR优化"></a>不用NVR优化</h1><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; g++ -o demo test.cpp -fno-elide-constructors</span><br><span class="line">&#x2F;&#x2F; -fno-elide-constructors 表示输出未优化的结果</span><br><span class="line">Edge construct!</span><br><span class="line">Copy Construct!</span><br><span class="line">Edge destruct！</span><br><span class="line">Copy Construct!</span><br><span class="line">Edge destruct！</span><br><span class="line">Edge destruct！</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;在上述代码中，在成员函数get()函数中定义了一个Edge对象，然后将其作为返回值返回。如果按照调用copy construct,编译器会对代码进行一个扩张操作</p>
<ul>
<li>首先加上一个额外参数，类型是类对象的一个引用，这个参数用来放置由copy construct 而得到的返回值。</li>
<li>在return命令之前安插一个copy construct调用操作，以便将欲传回的对象的内容当作上述新增参数的初值。</li>
</ul>
<p>具体过程可以通过下面的伪代码来了解<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Edge get(Edge &amp;_result) &#123;</span><br><span class="line">  &#x2F;&#x2F; 增加一个额外参数作为引用传入函数</span><br><span class="line">  Edge point;</span><br><span class="line">  &#x2F;&#x2F;这里会调用Edge的空构造函数</span><br><span class="line">  point.Edge::Edge();</span><br><span class="line">  &#x2F;&#x2F;调用拷贝构造函数将Edge将point的值赋给返回值</span><br><span class="line">  _result.Edge(point);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>&emsp;&emsp;在上述代码中，编译器的扩张操作，构造出了point。但是这个没用空构造的point根本就没有起到什么作用。对于编译器来说，这就会导致资源的浪费，拖慢运行速度。所以NRV优化操作就显的很有必要了。</p>
<h1 id="使用NRV优化"><a href="#使用NRV优化" class="headerlink" title="使用NRV优化"></a>使用NRV优化</h1><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; g++ -o demo test.cpp</span><br><span class="line">&#x2F;&#x2F; g++ -o 会对程序做部分优化 </span><br><span class="line">Edge construct!</span><br><span class="line">Edge destruct！</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;编译器为了避免这个无意义的临时变量，编译器选择使用传入的参数(_ result)。对其直接进行构造，这样就避免了无意义的临时变量。具体操作参考如下伪代码<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Edge get(Edge &amp;_result) &#123; &#x2F;&#x2F;额外参数作为引用参数介入函数</span><br><span class="line">  &#x2F;&#x2F;使用NRV优化后，不再生成point临时变量，而是对_result直接进行构造</span><br><span class="line">  _result.Edge::edge();</span><br><span class="line">  return; &#x2F;&#x2F; 返回已有的返回值</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h1 id="结束语"><a href="#结束语" class="headerlink" title="结束语"></a>结束语</h1><p>&emsp;&emsp;只有在显式定义copy construct 的时候，编译器才会激活NRV优化操作。对于普通的程序，是看不出NRV优化的好处，只有在庞大数据量时，NRV优化操作的优点才会显现出来。</p>
]]></content>
      <categories>
        <category>Read Notes</category>
      </categories>
      <tags>
        <tag>theory</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-24</title>
    <url>/2021/03/19/Leetcode-24/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;给定两个大小分别为<code>m</code>和<code>n</code>的正序(从小到大)数组<code>nums1</code>和<code>nums2</code>。要求找出这两个正序数组的<strong>中位数</strong>。</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p>&emsp;&emsp;输入是两个数组，合并后的数组顺序是由小到大。</p>
<p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> nums1 = [1,3], nums2 = [2]<br>
  <strong>Output:</strong> 2.00000 
</p><br>&emsp;&emsp;对于偶数个数字的数组，中位数是(n/2)。对于奇数个数字的数组，中位数为(n+(n+1))/2的值。</p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;简单解法,就是将num2的值使用<code>emplace_back()</code>函数将nums2的接在nums1后面，之后再使用sort()。进行一个从小到大的排序，最后根据奇数偶数，返回不同的中位数。但是这个做法的缺点就是，使用sort后的时间复杂度就变为了$n * log^{2}(n)$,具体代码如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    double findMedianSortedArrays(vector&lt;int&gt;&amp; nums1, vector&lt;int&gt;&amp; nums2) &#123;</span><br><span class="line">       int n &#x3D; nums1.size(),m &#x3D; nums2.size();</span><br><span class="line"></span><br><span class="line">       for (int i &#x3D; 0; i &lt; m ; i++) &#123;</span><br><span class="line">           nums1.emplace_back(nums2[i]);</span><br><span class="line">       &#125; </span><br><span class="line"></span><br><span class="line">       sort(nums1.begin(),nums1.end());</span><br><span class="line"></span><br><span class="line">       int a &#x3D; nums1.size();</span><br><span class="line">       int k &#x3D; a &#x2F; 2;</span><br><span class="line"></span><br><span class="line">       if (a % 2 &#x3D;&#x3D; 1) &#123;</span><br><span class="line">           return nums1[k];</span><br><span class="line">       &#125;</span><br><span class="line">       else</span><br><span class="line">           return (double)(nums1[k-1]+nums1[k])&#x2F;2;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-25</title>
    <url>/2021/03/20/Leetcode-25/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;给定一个字符串<code>s</code>,找到<code>s</code>中最长的回文子串。</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> s = "babad" <br>
  <strong>Output:</strong> "bab" 
</p><br>&emsp;&emsp;对于这样一个字符串，不止存在一个最长回文子串。可以是bab或aba。</p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;对于这个题目，如果使用翻转的话。对于<code>aacxycaa</code>,反转之后是<code>aacyxcaa</code>,这时最长公共子串为<code>aac</code>,但是正确的回文子串为<code>aa</code>，所以这种方法不可以使用。<br>&emsp;&emsp;这个题目，应该思考如何使用双指针。对于寻找回文串的问题的关键之处在于：从中间开始向两边扩散来判断。首先我们需要先找到这个最长字符串，但是对于字符串来说，它们的个数可能是奇数或是偶数。对于偶数来说，寻找s[i]为中心的回文串；对于奇数，寻找s[i]和s[i+1]为中心的回文串。在寻找的过程中，应该注意边界限制。具体代码如下<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    string longestPalindrome(string s) &#123;</span><br><span class="line">        string res;</span><br><span class="line">        for (int i &#x3D; 0; i &lt;s.size(); i++) &#123;</span><br><span class="line">            &#x2F;&#x2F;以s[i]为中心的最长回文子串</span><br><span class="line">            string s1 &#x3D; Palindrome(s,i,i);</span><br><span class="line">            &#x2F;&#x2F;以s[i+1]为中心的最长回文子串</span><br><span class="line">            string s2 &#x3D; Palindrome(s,i,i+1);</span><br><span class="line"></span><br><span class="line">            &#x2F;&#x2F; res &#x3D; longest(res,s1,s2);</span><br><span class="line">            res &#x3D; res.size() &gt; s1.size() ? res : s1;</span><br><span class="line">            res &#x3D; res.size() &gt; s2.size() ? res : s2;</span><br><span class="line">        &#125;</span><br><span class="line">        return res;</span><br><span class="line">    &#125;</span><br><span class="line">    string Palindrome(string&amp; s,int l, int r) &#123;</span><br><span class="line">        &#x2F;&#x2F; 防止索引越界</span><br><span class="line">        while (l &gt;&#x3D; 0 &amp;&amp; r &lt; s.size() &amp;&amp; s[l] &#x3D;&#x3D; s[r]) &#123;</span><br><span class="line">            &#x2F;&#x2F; 向两边展开</span><br><span class="line">            l--,r++;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; 返回以 s[l] 和 s[r]为中心的最长字符串</span><br><span class="line">        return s.substr(l+1, r-l-1);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-26</title>
    <url>/2021/03/22/Leetcode-26/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;给你<code>n</code>个非负整数<code>a<sub>1</sub>,a<sub>2</sub>,…,a<sub>n</sub></code>。每个数代表坐标中的一个点<code>(i,a<sub>i</sub>)</code>。在坐标内画<code>n</code>条垂直线，垂直线<code>i</code>的两个端点分别为<code>(i,a<sub>i</sub>)</code>和<code>(i,0)</code>。找出其中两条线，使得它们与<code>x</code>轴共同构成的容器可以容纳最多的水。(容器不可倾斜)</p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> [1,8,6,2,5,4,8,3,7] <br>
  <strong>Output:</strong> 49
</p><br>&emsp;&emsp;容器容纳水的多少取决于高低与两个柱子之间的间距。容器高低是由这其中最低的柱子而决定的，所以这个容器最大值为$7 * 7 = 49$</p>
<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;这种可以通过移到柱子来求取容器最大值，最好的方法莫过于使用双指针。通过左右指针来进行操作，进行柱子的移动。首先，先从最外围进行处理。由于最低的柱子决定了容器的高低，所以$[长度 * 最低柱子高度]$。通过max对比，用res来保存结果，最后就可以输出最大值了。具体代码：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    int maxArea(vector&lt;int&gt;&amp; height) &#123;</span><br><span class="line">        int res &#x3D; 0;</span><br><span class="line">        int left &#x3D; 0;</span><br><span class="line">        int right &#x3D; height.size() - 1;</span><br><span class="line">        while (left &lt; right) &#123;</span><br><span class="line">            int area &#x3D; (right - left) * min(height[left],height[right]);</span><br><span class="line">            res &#x3D; max(area,res);</span><br><span class="line">            if (height[left] &lt; height[right])</span><br><span class="line">                left++;</span><br><span class="line">            else right--;</span><br><span class="line">        &#125;</span><br><span class="line">        return res;</span><br><span class="line">    &#125;  </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Effective-C++ Reading Notes</title>
    <url>/2021/03/23/Effective-C-Read-Note/</url>
    <content><![CDATA[<h1 id="第一章-让自己习惯C"><a href="#第一章-让自己习惯C" class="headerlink" title="第一章 让自己习惯C++"></a><center>第一章 让自己习惯C++</center></h1><ol>
<li>视C++为一个语言联邦</li>
</ol>
<ul>
<li>C++ 语言的四个层次<ul>
<li>C：没有C++的面向对象、没有模板、没有异常、没有重载等。</li>
<li>Object-Oriented C++：这部分也就是C with Classes。classes、封装、继承、多态、虚函数。这都属于面向对象的特性。</li>
<li>Template C++: 这部分是C++的泛型部分。这部分带来的是template metaprogramming,也就是所谓的模板元编程。</li>
<li>STL：STL是template程序库。它对容器、迭代器、算法及函数对象的规约，并且是以template及程序库的方式构建出来。</li>
</ul>
</li>
</ul>
<ol start="2">
<li>尽量以const、enum、inline替换define<ol>
<li>多用编译器，少用预处理器(Preder the compiler to the preprocessor)<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#define A 1.653</span><br></pre></td></tr></table></figure></li>
</ol>
<ul>
<li>字符串’A’将不会被编译器看到，而编译器看到的是’1.653’。这会调试过程中，编译器错误信息只显示’1.653’而非’A’</li>
<li>解决方法：定义全局变量<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const double A &#x3D; 1.653</span><br></pre></td></tr></table></figure></li>
<li>使用全局变量的好处是，不会像define一样只是将宏名称进行替换,这会导致内存中有多份备份。</li>
</ul>
<ol start="2">
<li>对于定义或声明全局变量时，常数指针与类的常数需要特别考虑</li>
</ol>
<ul>
<li>对于指针<ul>
<li>对于指针要把指针与它指向的数据都定义为const，例如：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cosnt char* const myWorld &#x3D; &quot;Hello&quot;;</span><br></pre></td></tr></table></figure></li>
<li>在C++中可以更方便地使用std::string这样基于char*类型的推广，例如：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const std::string myWorld(&quot;Hello&quot;);</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li>对于类的常数<ul>
<li>声明为类的私有静态成员，这样既保证变量只能被这个类的对象接触到，且不会生成多个拷贝<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Plyaer &#123;</span><br><span class="line">  private: </span><br><span class="line">    static const int numPlayer &#x3D; 5;</span><br><span class="line">......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<ol start="3">
<li>枚举技巧</li>
</ol>
<ul>
<li>当我们想在类中声明某个变量，但编译器不允许声明时赋值初始化，例如:<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int noPlayer;</span><br><span class="line">int socres[noPlayer];</span><br></pre></td></tr></table></figure></li>
<li>这种情况下，编译器会报错，我们可以使用enum<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">enum &#123;noPlayer &#x3D; 5&#125;;</span><br><span class="line">int scores[noPlayer];</span><br></pre></td></tr></table></figure></li>
<li>这样就可以编译通过，因为这里的enum可以被当成int类型使用</li>
<li>enum类型在内存中是没有实体的，无法取得enum类型的地址，因此这个方法更相当于取一个本地的#define数值</li>
</ul>
<ol start="4">
<li>对于#define的宏函数，尽量使用inline修饰的函数来代替#define</li>
</ol>
<ul>
<li>inline 关键字用来建议编译器把某频繁调用的函数当做内联函数<ul>
<li>优点：在每次调用函数时，直接把函数代码放在函数调用语句的地址，减少堆栈浪费。</li>
</ul>
</li>
<li>若将其定义为宏也可以，就是这种做法不太好<br>```<br>#define call(a,b) f((a) &gt; (b) ? (a) : (b))<br>…<br>int a = 5, b = 0;<br>call(++a, b)    //a 增加了一次<br>call(++a, b+10) //a 增加了两次</li>
</ul>
</li>
</ol>
]]></content>
      <categories>
        <category>Read Notes</category>
      </categories>
      <tags>
        <tag>theory</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-27</title>
    <url>/2021/03/24/Leetcode-27/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;给定一个包含n个整数的数组<code>nums</code>,判断<code>nums</code>中是否存在三个元素a,b,c,使得a+b+c=0?请你找出所有和为0且不重复的三元组。<font color=red>要求不可以有重复的三元组</font></p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> [-1,0,1,2,-1,-4] <br>
  <strong>Output:</strong> [[-1,-1,2],[-1,0,1]]
</p>
&emsp;&emsp;在这个样例中，可以组成零的三元组有[[-1,-1,2],[-1,0,1],[-1,0,1]],但是题目要求去重，所以最后只可以保留[[-1,-1,2],[-1,0,1]];

<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>&emsp;&emsp;三个值的计算可能会比较麻烦，但是固定一个值，仅去计算两数之和就会简单很多。我们可以先对数组进行一个排序，方便待会儿处理左右指针的移动。至于固定一个值，我们可以使用for循环,使用完一个推向下一个。<br>&emsp;&emsp;由于存在数字重复,所以需要对数字进行去重。在固定好第一个值后，我们就要开始操作双指针了。i是值固定，因此我们的left必须为(i+1),right不受影响也就是数组的最后一个值(len-1)。因为$a+b+c=0$,所以$a+b=-c$，接下来就是通过值的比较来寻找有效三元组。<br><br>&emsp;&emsp;当找到一个有效三元组时，left与right需同时变化。这里仍需考虑去重问题如:<code>[-1,-1,-1,3,3,3];left = 1,right =4;</code>，为了避免这种情况,我们需要对数字进行去重。最后重复前面操作就可以得出结果了，具体代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    vector&lt;vector&lt;int&gt;&gt; threeSum(vector&lt;int&gt;&amp; nums) &#123;</span><br><span class="line">        &#x2F;&#x2F; base case</span><br><span class="line">        int len &#x3D; nums.size();</span><br><span class="line">        if (len &lt; 3) return&#123;&#125;;</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; save result</span><br><span class="line">        vector&lt;vector&lt;int&gt; &gt; res;</span><br><span class="line">        std::sort(nums.begin(),nums.end());</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F;固定一个值</span><br><span class="line">        for (int i &#x3D; 0;i &lt; len; i++) &#123;</span><br><span class="line">            &#x2F;&#x2F;第一个值大于零，则直接返回</span><br><span class="line">            if (nums[i] &gt; 0) return res;</span><br><span class="line"></span><br><span class="line">            &#x2F;&#x2F; 去重</span><br><span class="line">            if (i &gt; 0 &amp;&amp; nums[i] &#x3D;&#x3D; nums[i-1]) continue;</span><br><span class="line"></span><br><span class="line">            &#x2F;&#x2F;双指针操作</span><br><span class="line">            &#x2F;&#x2F; left起始点在i之后，right 不受影响</span><br><span class="line">            int left &#x3D; i + 1, right &#x3D; len -1;</span><br><span class="line">            while (left &lt; right) &#123;  </span><br><span class="line">                int sum &#x3D; nums[left] + nums[right];</span><br><span class="line">                if (sum &gt; -nums[i]) right--;</span><br><span class="line">                else if (sum &lt; -nums[i]) left++;</span><br><span class="line">                else &#123;</span><br><span class="line">                    &#x2F;&#x2F;找到一个有效三元组</span><br><span class="line">                    res.push_back(&#123;nums[i],nums[left],nums[right]&#125;);</span><br><span class="line">                    left++;</span><br><span class="line">                    right--;</span><br><span class="line">                    </span><br><span class="line">                    &#x2F;&#x2F;去重</span><br><span class="line">                    while (left &lt; right &amp;&amp; nums[left] &#x3D;&#x3D; nums[left-1]) left++;</span><br><span class="line">                    while (right &lt; right &amp;&amp; nums[right] &#x3D;&#x3D; nums[right+1]) right--;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-28</title>
    <url>/2021/03/25/Leetcode-28/</url>
    <content><![CDATA[<h1 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h1><p>&emsp;&emsp;给定一个仅包含数字<code>2-9</code>的字符串，返回所有它能表示的字母组合。答案可以<strong>任何顺序</strong>返回。给定数字到字母的映射如下(与电话按键相同),<font color=red>1不对应任何字母</font>。<br><img src="https://assets.leetcode-cn.com/aliyun-lc-upload/original_images/17_telephone_keypad.png" alt="avatar"></p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> digits = "23" <br>
  <strong>Output:</strong> ["ad","ae","af","bd","be","bf","cd","ce","cf"]
</p>
&emsp;&emsp;"2"对应的字符串是abc,"3"对应的字符串是def。a对应def，b对应def，c对应def。从而产生最后结果["ad","ae","af","bd","be","bf","cd","ce","cf"]。

<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><ol>
<li>首先对于这道题目，我们需要解决的问题有三：</li>
</ol>
<ul>
<li>数字与字母的映射如何处理</li>
<li>无法直接使用暴力破解，需要使用其它方法代替</li>
<li>除了2-9的异常按键如何处理</li>
</ul>
<ol start="2">
<li>首先处理字母与数字的映射问题，我们可以使用map或者使用数组进行映射。这里我使用的是一维数组进行了映射。</li>
<li>由于使用暴力的多个for无法解决，因此转而采用回溯方法来进行处理。使用回溯三部曲。<ol>
<li>确定回溯函数</li>
</ol>
<ul>
<li>字符串s收集叶子节点，字符串数组保存结果。还需要一个记录遍历digits的index。</li>
</ul>
<ol start="2">
<li>确定终止条件</li>
</ol>
<ul>
<li>当(index == digits.size())时，即遍历结束后，结束回溯。</li>
</ul>
<ol start="3">
<li>确定单层遍历逻辑</li>
</ol>
<ul>
<li>将digit对应的字符转换成int类型，根据数字找到它在phone中的映射</li>
<li>用for来处理横向遍历，用递归进行纵向遍历，用pop_back()进行回溯。</li>
</ul>
</li>
</ol>
<p>具体代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Solution &#123;</span><br><span class="line">private:</span><br><span class="line">    const string phone[10] &#123;</span><br><span class="line">        &quot;&quot;,</span><br><span class="line">        &quot;&quot;,</span><br><span class="line">        &quot;abc&quot;,</span><br><span class="line">        &quot;def&quot;,</span><br><span class="line">        &quot;ghi&quot;,</span><br><span class="line">        &quot;jkl&quot;,</span><br><span class="line">        &quot;mno&quot;,</span><br><span class="line">        &quot;pqrs&quot;,</span><br><span class="line">        &quot;tuv&quot;,</span><br><span class="line">        &quot;wxyz&quot;</span><br><span class="line">    &#125;;</span><br><span class="line">public:</span><br><span class="line">    vector&lt;string&gt; res;</span><br><span class="line">    string s;</span><br><span class="line"></span><br><span class="line">    void backtracking(string&amp; digits,int index) &#123;</span><br><span class="line">        &#x2F;&#x2F; 终止条件</span><br><span class="line">        if (digits.size() &#x3D;&#x3D; index) &#123;</span><br><span class="line">            res.push_back(s);</span><br><span class="line">            return;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F;字符转换为数字</span><br><span class="line">        int digit &#x3D; digits[index] - &#39;0&#39;;</span><br><span class="line">        &#x2F;&#x2F; 通过对应的数字找到映射的字母</span><br><span class="line">        string Letters &#x3D; phone[digit];</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; 横向遍历</span><br><span class="line">        for (int i &#x3D; 0;i &lt; Letters.size(); i++) &#123;</span><br><span class="line">            &#x2F;&#x2F; 存入字母</span><br><span class="line">            s.push_back(Letters[i]);</span><br><span class="line">            &#x2F;&#x2F; 递归，纵向遍历，index+1指向下一层数字</span><br><span class="line">            backtracking(digits,index+1);</span><br><span class="line">            &#x2F;&#x2F; 返回上一层，即回溯</span><br><span class="line">            s.pop_back();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    vector&lt;string&gt; letterCombinations(string digits) &#123;</span><br><span class="line">        s.clear();</span><br><span class="line">        res.clear();</span><br><span class="line"></span><br><span class="line">        if (digits.size() &#x3D;&#x3D; 0) return res;</span><br><span class="line"></span><br><span class="line">        backtracking(digits,0);</span><br><span class="line">        return res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>The Annotated STL Sources</title>
    <url>/2021/03/25/The_Annotated_STL_Sources/</url>
    <content><![CDATA[<h1 id="1、阅读准备"><a href="#1、阅读准备" class="headerlink" title="1、阅读准备"></a>1、阅读准备</h1><ul>
<li>gcc –version : 9.3.0</li>
<li>SGI-STL源码</li>
<li>C++ 基础</li>
<li>具有STL使用经验</li>
<li>少量算法与数据基础</li>
</ul>
<h1 id="2、STL简介"><a href="#2、STL简介" class="headerlink" title="2、STL简介"></a>2、STL简介</h1><h2 id="STL版本"><a href="#STL版本" class="headerlink" title="STL版本"></a>STL版本</h2><p>&emsp;&emsp;STL仅仅是一个标准，只规定了STL接口，内部实现没有要求。STL有许多版本，PJ STL(被Visual C++采用),RW STL等。<br>&emsp;&emsp;我们使用的是SGI STL版本，这个版本的注释最丰富、结构清晰、可读性最强，且被GCC采用，所以是目前最流行的版本。但是SGI STL并不是原封不动的被用于GCC，所以GCC中的STL与SGI STL仍存在一些微小的区别。</p>
<h2 id="STL组件"><a href="#STL组件" class="headerlink" title="STL组件"></a>STL组件</h2><ul>
<li><p>STL分为六大组件</p>
<ul>
<li>容器(container): 常用数据结构，大致可以分为两类：序列容器如{vector、list、deque};关联容器如{set, map}。在实现上，是类模板(class template)</li>
<li>迭代器(iterator): 一套访问容器的接口，行为类似于指针。它为不同的算法提供的相对统一的容器访问方式，使得设计算法时无需过多关注数据</li>
<li>算法(algorithm): 提供一套常用的算法，如sort、search、copy、erase…在实现上，可以认为是一种函数模板(function template)</li>
<li>配置器(allocator): 为容器提供空间配置与释放，对象构造与析构的服务，也是一个class template</li>
<li>仿函数(functor): 作为函数使用的对象，用于泛化算法中的操作。</li>
<li>配接器(adapter): 将一种容器修饰为功能不同的另一种容器，例如STL提供的queue和stack，虽然看似容器，其实只能算是一种容器配接器，因为它们的底部完全借助deque，所有操作都由底层deque供应。</li>
</ul>
</li>
<li><p>六大组件的交互关系</p>
<ul>
<li>Container 通过 Allocator取得数据储存空间，Algotithm通过 Iterator存取Container内容，Functor可以协助Algorithm完成不同的策略变化，Adapter可以修饰或套接Functor。可以参考下图:<br><img src="https://i.loli.net/2021/03/25/l81tQeYIvqFKkRm.png" alt="avatar"></li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>Read Notes</category>
      </categories>
      <tags>
        <tag>theory</tag>
      </tags>
  </entry>
  <entry>
    <title>The Annotated STL Sources</title>
    <url>/2021/03/25/The-Annotated-STL-Sources/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Read Notes</category>
      </categories>
      <tags>
        <tag>theory</tag>
      </tags>
  </entry>
  <entry>
    <title>Leetcode-29</title>
    <url>/2021/03/25/Leetcode-29/</url>
    <content><![CDATA[<h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>&emsp;&emsp;给定一个链表，删除链表的倒数第<code>n</code>个结点，并且返回链表的头结果。进阶:<font color=red>尝试使用一趟扫描实现</font></p>
<h1 id="输入输出样例"><a href="#输入输出样例" class="headerlink" title="输入输出样例"></a>输入输出样例</h1><p style = "background-color:rgba(240,248,255)">
  <strong>Input:</strong> head = [1,2,3,4,5], n = 2 <br>
  <strong>Output:</strong> [1,2,3,5]
</p>

<h1 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h1><p>对于这道题目我们需要解决的问题两个:</p>
<ul>
<li>如何获取倒数第n个结点的信息</li>
<li>如何删除节点</li>
</ul>
<ol>
<li>首先是获取倒数第n个结点的的信息,这个我们可以通过快慢指针来进行一个实现。<ul>
<li>快慢节点都开始于前哨节点处</li>
<li>快节点先移动n个距离，之后fast与slow保持n个间距向后移动。</li>
<li>当fast移到tail-&gt;next时，即fast为空时，停止移动。</li>
</ul>
</li>
<li>删除节点是通过找到n的上一个节点，并改变其next指向，从而实现删除的目的。<br>具体代码如下所示：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;**</span><br><span class="line"> * Definition for singly-linked list.</span><br><span class="line"> * struct ListNode &#123;</span><br><span class="line"> *     int val;</span><br><span class="line"> *     ListNode *next;</span><br><span class="line"> *     ListNode() : val(0), next(nullptr) &#123;&#125;</span><br><span class="line"> *     ListNode(int x) : val(x), next(nullptr) &#123;&#125;</span><br><span class="line"> *     ListNode(int x, ListNode *next) : val(x), next(next) &#123;&#125;</span><br><span class="line"> * &#125;;</span><br><span class="line"> *&#x2F;</span><br><span class="line">class Solution &#123;</span><br><span class="line">public:</span><br><span class="line">    ListNode* removeNthFromEnd(ListNode* head, int n) &#123;</span><br><span class="line">        &#x2F;&#x2F; 设置前哨节点</span><br><span class="line">        ListNode* dummy &#x3D; new ListNode(-1);</span><br><span class="line">        dummy-&gt;next &#x3D; head;</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; 设置快慢指针</span><br><span class="line">        ListNode* slow &#x3D; dummy;</span><br><span class="line">        ListNode* fast &#x3D; dummy;</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; fast 移到到距离slow size&#x3D;n的地方</span><br><span class="line">        for (int i &#x3D; 0; i &lt; n+1; ++i) &#123;</span><br><span class="line">            fast &#x3D; fast-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; 当fast不为空时，进行左右指针的移动</span><br><span class="line">        while (fast) &#123;</span><br><span class="line">            slow &#x3D; slow-&gt;next;</span><br><span class="line">            fast &#x3D; fast-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; 删除倒数第n个节点</span><br><span class="line">        ListNode* delNode &#x3D; slow-&gt;next;</span><br><span class="line">        slow-&gt;next &#x3D; delNode-&gt;next;</span><br><span class="line">        delete(delNode);</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F;返回头节点</span><br><span class="line">        return dummy-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
</ol>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
</search>
